# ROADMAP — SS14 Chemistry Database (Increment Workbook)

**Версия:** 1.0 · **Дата:** 2026-07-12 · **Схема данных:** 3.4.2
**Основание (спрос):** [research/ss14-community-demand/SUMMARY.md](research/ss14-community-demand/SUMMARY.md) — 5 источников, 57+ инсайтов, ранжированные возможности O1–O11 + императив M1.
**Vision-документ:** отсутствует (gap по иерархии Vision > Roadmap > Tasks). Рабочая замена: [README.md](README.md) (продукт) + SUMMARY §1 (ниша/валидация). Создание Vision — в Backlog.

Статусы: `[ ]` не начат · `[~]` в работе · `[x]` done (верифицирован e2e) · `[!]` заблокирован.

---

## 0. Правила выполнения (Definition of Done для каждого инкремента)

1. **Верификация e2e в браузере** (preview): фича работает кликом, скриншот; для data-инкрементов — regen + спот-чек значений против YAML апстрима.
2. **Cache-bust:** при правке `app.js`/`style.css` — bump `?v=N` в `index.html` (иначе preview отдаёт старый код).
3. **Параллельные сессии:** перед правкой `app.js`/`config.py` — `git status` + mtime-чек; коммитить только свои файлы. Инкременты, трогающие `app.js`, вести последовательно, не в параллельных сессиях.
4. **Коммит** после верификации (conventional commits), **CHANGELOG** при изменении схемы данных; push только по запросу.
5. **M1 (сквозной, при каждом regen):** просмотреть warnings экстрактора; проверить переименования апстрима (Solution→entity #43192/#43194/#43384, EdibleComponent #40046, условия EntityEffects #38643, газы #39685 — ExudeGasses/ConsumeGasses были перепутаны). Дрейф схемы = тихая порча данных.
6. **Каждые 4 инкремента** — сокращённый /project-audit + сверка с этим roadmap.

---

## Серия A — «Фокус и быстрые победы» (чистый фронтенд, данные готовы)

### A1. Advanced-выпадашка: скрыть Stats / Reactions / Graph `[ ]` — HAE 1h (frontend)
**Спрос:** фокус UX; ни одного запроса на графы/статистику во всех 5 источниках. Решение об окончательном удалении — по Метрике (goal-события на табы уже стоят, коммит `62c964c`).
**Шаги:**
1. [index.html:185-192](index.html:185) — кнопки `btn-reactions`, `btn-graph`, `btn-stats` перенести в дропдаун «⚙ Advanced» в конце tab-bar (селекторы `.tab-btn[data-tab=…]` сохранить — на них завязан `app.js`).
2. Скрыть блок Stats в меню фильтров (найти его контейнер в `index.html`/`app.js`).
3. [index.html:89](index.html:89) — `vis-network` с unpkg перевести на ленивую загрузку при первом открытии Graph (инжект script по требованию). Готовит B2-offline.
4. Deep-links: проверить, что share-URL с `tab=stats|graph|reactions` по-прежнему открывает таб.
**DoD:** все 8 табов открываются (из Advanced и по deep-link); первая загрузка страницы — без запроса к unpkg (вкладка Network); скриншот. **Зафиксировать дату** → Метрика-чекпойнт через 2–4 недели: <1–2% открытий → удалить совсем.

### A2. Пресеты «начало смены» / стартовые наборы `[ ]` — HAE 2h (curation + frontend)
**Спрос:** O7 (role-guides, forum-07), «стартовые наборы новичка».
**Шаги:**
1. Кураторски собрать 3–5 пресетов на форк (мед-хим стартер, ботаник, бар-стартер; тир «новичок» отдельно от «мета») — в `config.py` по образцу `ANTAG_STRATEGIES` (с `sources`, провенанс-модель сохраняется) → passthrough в `data.json`.
2. Обобщить [`loadStrategyIntoBatch` (app.js:2495)](app.js:2495) → `loadPresetIntoBatch` (механика идентична).
3. UI: кнопки пресетов в Calculator (фильтруются активным форком).
**DoD:** клик по пресету наполняет Batch Planner; количества сверены с рецептами вручную для 2 пресетов; скриншот.

### A3. Режим «Чем лечить?» v1 `[ ]` — HAE 4h (frontend)
**Спрос:** O2+O6 — forum-03 («в medicine tab нет поиска»), медики = кратно большая аудитория, чем химики.
**Шаги:**
1. Новый режим (в Reagents или Calculator): селектор типа урона Brute / Burn / Toxin / Airloss / Genetic / Radiation (+ учёт активного форка).
2. Ранжирование препаратов из `effectTags`/`effects`: лечение за юнит ↓, порог передоза (`overdose`), бейдж доступности из `accessibility` (диспенсер → варка → ботаника).
3. Каждая строка → ссылка на рецепт/craft-tree.
**DoD:** 3 спот-чека рейтинга против `data.json` значений; скриншот; Метрика-событие на использование режима.
**Примечание:** v2 этого режима = D2 (селектор расы).

---

## Серия B — «Companion & миграция» (фронтенд)

### B1. Fork diff UI («переехал на Goob — что поменялось?») `[ ]` — HAE 4h (frontend)
**Спрос:** O8, steam-17 (игроки жонглируют форками), Reddit-хедмин («играйте разные форки»).
**Шаги:**
1. Экран «Fork A vs Fork B»: added / removed / modified по реагентам и реакциям (инфраструктура first-wins merge + auto-diff уже вычисляет различия — нужен UI-разворот).
2. Для modified — конкретика: что изменилось (реагенты рецепта, minTemp, продукты).
3. Deep-link на пару форков.
**DoD:** выбор пары форков выдаёт корректные списки (спот-чек Vanilla↔Goob по 3 известным различиям); скриншот.

### B2. PWA offline + companion-лэйаут `[ ]` — HAE 4h (frontend) — **зависит от A1**
**Спрос:** O4 (устойчивость к инфра-сбоям 2026, steam-11) + «второй экран» (5.5).
**Шаги:**
1. Self-host `vis-network` (в `libs/`) — убрать последнюю внешнюю зависимость (lazy-загрузка уже из A1).
2. Service worker: cache-first для статики + `data.json`, инвалидация по `schemaVersion` + `?v=N`.
3. Компактный лэйаут `?mode=companion`: одна колонка, крупный шрифт, чекбоксы шагов рецепта («cook mode» для телефона/второго монитора).
4. Проверить `manifest.json` (standalone, иконки — уже есть).
**DoD:** DevTools offline → reload работает полностью; installable (Lighthouse PWA); скриншот companion-режима на mobile-вьюпорте.

### B3. «📌 Поверх игры» — Document Picture-in-Picture `[ ]` — HAE 2h (frontend) — **зависит от B2**
**Спрос:** твоя идея №3 (плавающий калькулятор поверх полноэкранной игры).
**Шаги:**
1. Кнопка в companion-режиме: `documentPictureInPicture.requestWindow()` (Chromium 116+) → перенос компакт-калькулятора в always-on-top окно.
2. Fallback: `window.open` мини-окно + блок подсказок «нет PiP»: Steam Overlay (Shift+Tab → браузер), PowerToys Always-on-Top (Win+Ctrl+T).
3. Graceful degrade в Firefox/Safari (кнопка скрыта, подсказки остаются).
**DoD:** PiP-окно висит поверх другого полноэкранного приложения (скриншот); фолбэк-ветка проверена.

---

## Серия C — «Симулятор стакана» (флагман O1)

### C1. Сериализация `priority` реакций `[ ]` — HAE 1h (data) — предпосылка C2
**Факт:** в `data.json` 3.4.2 `priority` отсутствует у всех 1003 реакций (проверено), при этом drift-аудит экстрактора его уже сравнивает. Условия есть: minTemp — 168 реакций, maxTemp — 11, mixer — 207, катализаторы — 37.
**Шаги:** добавить `priority` в сериализацию реакций; regen; M1-чек warnings; schemaVersion → 3.4.3; CHANGELOG.
**DoD:** реакции с приоритетом в YAML несут его в `data.json` (спот-чек 3 шт.); фронтенд не сломан.

### C2. Симулятор «что будет, если смешать» `[ ]` — HAE 8h (frontend) — **зависит от C1**
**Спрос:** O1 — топ-возможность исследования (gh-01: #22575 условия реакций, #15905 ghetto chem; steam-01/03).
**Шаги:**
1. Движок: fixed-point цикл по `reactions` с учётом priority, minTemp/maxTemp, mixer, катализаторов и **порядка добавления** ингредиентов.
2. UI «стакан»: содержимое + объёмы + температура → Mix → пошаговый лог каскада с предупреждениями (дым/газ/взрыв/выхлоп тепла), опция «нагреть до T°».
3. Отличие от «What Can I Make?» зафиксировать в UI-тексте: тот — «какие рецепты доступны», этот — «что физически произойдёт».
**DoD:** 3 эталонных каскада сверены с YAML вручную (в т.ч. один smoke/foam); скриншот; Метрика-событие.

---

## Серия D — «Медленная полоса» (расширение экстрактора; строго по одному, после C1)

### D1. Ботаника: сущности растений + дерево эволюции + swab-гайд `[ ]` — HAE 8h (data + frontend)
**Спрос:** O5, gh-04 (#25454 +5 «мутации бессмысленны»), forum-04 (botany×chem), твоя идея №2.
**Шаги:**
1. Расширить Phase 8b ([ss14_chem_extractor.py:1721](ss14_chem_extractor.py:1721)) — сейчас парсит семена только в маппинг «реагент→растения»; извлекать растения как сущности: id, имя (+локали), `chemicals`, **`mutationPrototypes`**, exude/consume gasses, idealHeat/вода → новый ключ `plants` в `data.json`.
2. Спрайты растений в `sprites/plants/` (харвест из репо форков — по прецеденту реагентных).
3. UI: подтаб Botany → «Evolution»: направленный граф «что→во что мутирует» — **переиспользовать craft-tree renderer**; условия на рёбрах.
4. Гайд по genetic swabs (кросс-опыление): механика в C#-коде, не в YAML → **curated-блок** (янтарная плашка, `sources.py` type `maintainer-test`). Дерево мутаций — зелёное «Verified».
**DoD:** цепочки мутаций 5 растений сверены с YAML; дерево кликабельно; скриншот; M1-чек.

### D2. Расы: species-слой + фильтр в «Чем лечить?» `[ ]` — HAE 8h (data + frontend) — **зависит от A3, после D1**
**Спрос:** O6, gh-06 (#39659 Diona умирает от 1u абсента), gh-07 (#34319 диета видов), твоя идея №4.
**Шаги:**
1. Экстрактор: парсить species-прототипы (дыхательный газ, метаболайзеры, набор органов) → ключ `species`; связать с `OrganType`-условиями эффектов (частично парсятся, [ss14_chem_extractor.py:784](ss14_chem_extractor.py:784)).
2. UI: бейджи опасности на карточках реагентов («☠ токсично для Vox»), селектор вида в «Чем лечить?» (A3 v2).
3. Спот-чеки: Vox+кислород, Slime-специфика, Diona мульти-орган.
**DoD:** 3 вида сверены с YAML; скриншот; M1-чек.

---

## Серия E — «Поиск предметов по картам» (Maps tab)

**Спека:** [docs/design/2026-07-12-map-item-finder.md](docs/design/2026-07-12-map-item-finder.md) · **План:** [docs/superpowers/plans/2026-07-12-map-item-finder.md](docs/superpowers/plans/2026-07-12-map-item-finder.md)
**Спрос:** идея пользователя (воссоздание старого текстового поиска по картам, теперь с картой-схемой); смежно O7 (навигация новичка).
**Решения брейнсторминга (2026-07-12):** таб Maps в сайте; карта-центричный UI; рендер пол+стены; все предметы (Item-фильтр); все 18 форков; per-map PNG+JSON c ленивой загрузкой.

### E1. Пайплайн данных: vanilla Bagel end-to-end `[x]` 2026-07-12 — HAE 4h (data)
`ss14_map_extractor.py`: discovery (gameMap/пул), реестр прототипов (Item-фильтр, StorageFill, вендоматы+контрабанда, маяки+ftl), парс карты (7-байтные чанки), PNG (цвета из текстур) + JSON. План: Tasks 1–8.
**DoD:** `--selfcheck` зелёный; PNG узнаваем глазами (гейт порядка тайлов); 3 спот-чека (пол/шкаф/вендомат) против YAML в коммит-сообщении.
**Верификация:** selfcheck зелёный (10 751 прототип, 15 567 тайлов); PNG Bagel — узнаваемый силуэт станции (осмотрен + H-run coherence 0.634 против шумового порога 0.15); Crowbar сверен по всем 3 источникам с YAML: пол (−106.5, 24.5) = round(uid 23156), шкаф `ClosetToolFilled` p=0.7 = FillClosetTool GroupSelector, вендомат `VendingMachineYouTool` ×5 = youtool.yml. Коммиты `9bced26`…`8d40702`.
**Отклонения от плана (по живым данным):** `StorageFill` в актуальном апстриме мёртв → вставлен Task 3.5 (`EntityTableContainerFill` + `entityTable`-селекторы, 620 использований); `amount` бывает `!type:NumberSelector`-словарём (падало на Bagel) → коэрция `_amount`; маяк = `Medical`, не `Medbay`.

### E2. Фронтенд: таб Maps на Bagel `[x]` 2026-07-12 — HAE 4h (frontend) — **зависит от E1**
`maps.js`: селектор карт, canvas (подложка+маяки+маркеры), зум/пан, поиск с подсказками, список локаций по ближайшему маяку, deep-links (`map=`, `item=`), стили+мобильный стек. `app.js` — 2 строки (validTabs, mapsURLState). Tasks 9–14. **Правки index.html/app.js/style.css — последовательно в одной сессии; cache-bust `?v=N` на каждый шаг.**
**DoD:** e2e в preview кликами + скриншот; share-URL восстанавливает карту+предмет.
**Верификация:** каждая задача 9–14 проверена в preview: таб активируется + `index.json` 200; PNG рендерится на canvas; зум колесом/кнопками + драг-пан + fit; поиск «crow» → 6 подсказок, выбор Crowbar → маркеры (зелёные 24px) + метки маяков (cyan 4082px); список 19 групп по маякам с «in maintenance closet (~2%)»; десктоп side-by-side / мобилка flex-column без оверфлоу; deep-link `map=&item=` восстанавливает всё. Ноль ошибок консоли. Коммиты `5fee97c`…`772faf7`.
**Ловушка (стоила ~10 вызовов):** одинаковый `?nocache=1` кэшируется браузером → отдаёт старый index.html со старым `maps.js?v=N`. Нужен **уникальный** параметр на каждую перезагрузку + сверка `?v=` в Network. Записано в память проекта.

### E3. Все карты vanilla `[x]` 2026-07-12 — HAE 1h (data) — **зависит от E1** (E2 для браузер-чека)
Полный regen vanilla, index.json, браузер-чек не-Bagel карт, вес `maps/`. Task 15.
**Верификация:** 14 станций запечено (из 18 обнаруженных); браузер-чек Box + Marathon — рендерятся, Crowbar находится (25 групп на Box). Коммиты `d8c14a6`, `a83423f`, `699c50a`.
**Находки:** мусорные карты (Empty=0, MeteorArena=0, dm01-entryway=57, TestTeg=85 предметов) → data-driven фильтр `MIN_MAP_ITEMS=100` вместо ручного блоклиста; CentComm+Dev проходят порог, но не игровые станции → `MAP_BLOCKLIST`; блоклистнутые карты **не пекутся → не удалялись** (сироты в репо) → прунинг в `process_fork`.

### E4. Форки + ship `[~]` — HAE 3h (data+ops) — **зависит от E2+E3**
`--all-forks`, `MAP_BLOCKLIST` в config.py, deploy.yml (+maps.js, +maps/), README/CHANGELOG, e2e, Метрика-события. Tasks 16–17.
**Сделано (ship ванили, 2026-07-12):** deploy.yml копирует `maps.js` + `maps/`; README (Features + Architecture); CHANGELOG (maps schema 2); блоклист + format-drift guard + прунинг в экстракторе.
**Отложено на следующий релиз — форки.** Бейк вскрыл: форки копируют upstream-`Resources` целиком, их пулы перечисляют **ванильные же станции** (пул carpmosia == ванильный), т.е. одна Bagel пеклась ~8 раз → 72 MB на 11 форках. Смягчено дедупом via-строк (схема v2, −26%/карту) и капом `MAX_MAPS_PER_FORK=8` (ротация-первыми, ваниль не капается), но не доверифицировано → ваниль вышла в прод отдельно. У rucm/trauma в пуле только мусор, у starlight пул пуст. Часть форков на 6-байтном формате тайлов (guard: warn+skip, дыры в PNG, предметы целы).
**Остаток:** доверифицировать капнутый бейк форков → размер → e2e на 2 RU-форках → Метрика-события. **Бэклог:** кросс-форковый контент-диф вместо капа (Bagel у Goob законно отличается содержимым шкафов — нужен диф, не хеш файла).

---

## Порядок, параллельность, чекпойнты

```
A1 ─┬─→ B2 ─→ B3          C1 ─→ C2
A2 ─┤                     C1 ─→ D1 ─→ D2 (D2 также ← A3)
A3 ─┴─(v2 → D2)
B1 — независим
E1 ─→ E2 ─→ E4;  E1 ─→ E3 ─→ E4
```
- Серия E независима от A–D по данным, но **E2 правит app.js/index.html/style.css** → не параллелить с A1–A3 (общее правило одной сессии для app.js).
- A1/A2/A3/B1 логически параллельны, но **все правят `app.js`** → в одной сессии последовательно.
- Экстрактор-инкременты (C1, D1, D2) — строго по одному (M1-риск).
- **Чекпойнт-1** после A3+B1 (4 инкремента): сокращённый аудит + первые цифры Метрики по новым фичам.
- **Чекпойнт-2** (календарный, +2–4 нед после деплоя A1): решение по окончательному удалению Stats/Reactions/Graph.
- **Чекпойнт-3** после C2: аудит + Метрика симулятора.

## Backlog (кандидаты, не в работе)

- **O4-монетизация:** Ko-fi/Boosty «поддержать» + страница support (ядро всегда free — SUMMARY §7). HAE 1h.
- **O11 RU-first:** аудит RU-терминов/поиска (51% Steam-аудитории — RU).
- **B2B-виджет для серверов:** встраиваемый per-fork справочник (единственный канал экосистемы, где платят за инструмент — SUMMARY §7).
- **Газовыделение реакций** в симуляторе v2 — после фикса #39685 апстримом (данные сейчас ненадёжны).
- **Role-guides** (O7) сверх пресетов: страницы «как играть химика/ботаника».
- **Vision-документ** проекта (закрыть gap иерархии).

## Ревизии

| Дата | Версия | Что изменено |
|---|---|---|
| 2026-07-12 | 1.0 | Создан из demand-исследования (SUMMARY O1–O11, M1) + идей пользователя (интерфейс, ботаника, PiP-калькулятор, расы, 5 идей прошлого чата) |
| 2026-07-12 | 1.1 | Серия E (Maps tab: поиск предметов по картам) — спека+план после брейнсторминга; правило непараллельности E2 с A1–A3 |
