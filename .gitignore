__pycache__/
cache/
cache_maps/
*.pyc
*.xlsx
*.env
.claude/
.DS_Store
Thumbs.db
.gstack/
# Community-demand research: raw scraped third-party UGC kept local (not republished)
research/ss14-community-demand/raw/

# Metrika Logs API raw dumps: visit-level rows (clientID, city, UA); never publish
research/**/logs/

# Card screenshot sources (re-shot by scripts/shoot_cards.mjs); only promo/cards/*.webp ships
promo/cards-src/

# Cloudflare Worker local state and secrets (worker/)
worker/.wrangler/
worker/.dev.vars
worker/node_modules/
