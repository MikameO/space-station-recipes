# T1 — Data layer audit (adversarial DBA lens, risk-averse)

Audit date 2026-09-11. Repo `D:/Space Station Recipes` @ e777079. Evidence types: `runtime_verified` = a script/command was run in this session; `code_verified` = read the cited lines; `http_fetch_only` = read-only GET to GitHub. All ad-hoc scripts live in the scratch dir (`integrity.py`, `ownership.py`, `ownership2.py`, `drift_checks.py`, `fork_agnostic.py`, `reagent_removal.py`) and their outputs (`*_out.txt`, `run_*.txt`, `regen_log.txt`).

> **ORCHESTRATOR ACTION REQUIRED (side effect of a "read-only" script):** `python ss14_map_extractor.py --selfcheck` — listed in RULES.md as writing only under `cache/` — re-baked and **overwrote two tracked files**: `maps/vanilla/Bagel.json` and `maps/vanilla/Bagel.png` (`git status` now shows ` M` for both). I did not revert (rules forbid state-changing git). Restore with `git checkout -- maps/vanilla/Bagel.json maps/vanilla/Bagel.png`. See finding 4.3. Also `python ss14_ordnance.py --verify` rewrites `ordnance/stories_cm.json` in place (byte-identical this time, so the tree is clean there).

## 1. Referential integrity of data.json (runtime_verified via `integrity.py` unless noted)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 1.1 | Reaction reactant / catalyst ids resolve to reagents | FUNCTIONAL | HIGH | — | runtime_verified | data.json (1104 reactions) | 0 unresolved reactant ids, 0 unresolved catalyst ids across 1104 reactions. | All reactant refs resolve. | — | — |
| 1.2 | Reaction product ids resolve | PARTIAL | HIGH | P2 | runtime_verified | ss14_chem_extractor.py:2563-2576 (edges built from same refs) | 2 products point at reagents that exist nowhere in `cache/`: `BeastBlood` (reaction `BeastBloodLing@goob`), `MilkChoco` (`MilkChoco@goob`) — the defining Goob files are not in the manifest. 5 `edges` entries dangle on them. 144 reactions have `products: {}` — legitimate effect-only reactions (explosions, entity spawns, metal foam: `AluminiumMetalFoam@vanilla`, `CreateDough@vanilla`, …). | 1102/1104 reactions fully resolve; zero-product reactions are a documented SS14 pattern. | Two Goob reactions produce phantom reagents; no pipeline check refuses a product that is not a reagent. | Add a post-merge assertion in `export_json` that every product/reactant id exists (or is explicitly whitelisted) and fail the regen otherwise; add Goob's missing reagent file(s) to `FORK_REGISTRY["goob"]["reagent_files"]`. |
| 1.3 | `reagent.recipe` ↔ reactions consistency | PARTIAL | HIGH | P2 | runtime_verified + code_verified | ss14_chem_extractor.py:2388-2389 (`rxn = reaction_lookup[rid][0]`) | Globally consistent (0 reagents with a recipe but no producing reaction, 0 producing reactions without a recipe). But `recipe` is fork-agnostic: the first reaction in MERGE order wins. Result: 41 vanilla-visible reagents carry a recipe owned by another fork (`Coffee`←frontier, `JuiceApple`…`JuiceWatermelon`←deltav ×16, `Aluminium`/`Barozine`/`Honk`←goob, `Grenadine`←rmc14, …); 96 (reagent, fork) pairs where the recipe's reaction is invisible in that fork while another producing reaction IS visible (`Ethanol` on 19 forks shows `PestKillerBreakdown` while `WeldingFuelBreakdown` is the visible one; `Coffee` on adt shows Frontier's recipe). | Card/tree data for single-recipe vanilla reagents is right. | Any consumer reading `recipe` directly (calculator, craft trees, ordnance `load_cost_model` ss14_ordnance.py:794-820) gets a recipe from the wrong server. UI impact is T2's call; data-side the field is wrong for 41 vanilla reagents. | Either drop `recipe` and let consumers pick from `reactions` filtered by `forkVisible`, or serialize `recipe` per fork (`recipeByFork`) / restrict the global `recipe` to reactions from the reagent's own lineage. |
| 1.4 | `edges` consistency | PARTIAL | HIGH | P3 | runtime_verified | app.js:150 (only consumer) | 2826 edges = Σ products×reactants exactly; 5 bad refs (from 1.2). `DATA.edges` is referenced in app.js only by the load guard — the Graph tab that consumed it was removed in A1.1. | Count/shape correct. | 295 KB (7.7% of data.json) of dead payload that the load guard nevertheless REQUIRES. | Drop `edges` from the export and from the app.js:150 guard in the same increment (schema minor bump). |
| 1.5 | Plants: mutation/evolution targets | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified | ss14_chem_extractor.py:1420-1428 (target validation) | 47/47 mutation targets resolve; 152 plants, 0 without products/rsi. Side note: 2 plant chemicals reference unknown reagents (`CactusBubblePlant` → `Panacemycin`, `Septomycin`). | Validation exists and works. | Chemical ids inside `plants[].chemicals` are not validated. | Validate `chemicals` keys like mutation targets. |
| 1.6 | antagStrategies reagent ids / byFork | FUNCTIONAL | HIGH | — | runtime_verified | ss14_chem_extractor.py:2607-2635 | 21 strategies, 0 missing reagent ids, `byFork` keys == all 21 forks, no "feasible tier with invisible reagent" cases. | — | — | — |
| 1.7 | `sources` references resolve | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified | sources.py:400-474; ss14_chem_extractor.py:2686-2735 | 86 string refs (reagent antagTipsSources, strategies, botanyGuide, species, presets) all resolve to `data.sources` (27 entries, every one carries `authorityWeight`). Unresolved ids are fatal (`RuntimeError`, 2731). Side notes: catalog entry `code-goob-licoxide-lead` is never referenced; attribution coverage 16/68 (23%), 52 mk-only, 24 "mutable branch URL" warnings embedded in `meta.sourceAttribution.warnings`. | Fatal check for unknown ids works. | — | Prune unused catalog entry; treat attribution warnings as a tracked backlog. |
| 1.8 | shiftPresets ids + fork visibility | PARTIAL | HIGH | P3 | runtime_verified | config.py:1930-1996; ss14_chem_extractor.py:2637-2648 | All 18 preset reagent ids exist (validated at export). Presets have no `fork` field; all reagents are vanilla-owned so they are *visible* everywhere, but every recipe is `blocked` on rmc14/cmu/rucm/stories_cm (total conversion), the 5 med-chem reagents are blocked on funky (Funky removed Bicaridine/Kelotane/Dylovene/Dermaline/DexalinPlus recipes — verified against its vanilla-path copy), `DexalinPlus` on deltav, `SodaWater`/`Vodka` on misfits. | Typo protection works. | A preset can be selected on a fork where nothing in it can be crafted; no per-fork applicability data. | Emit `unavailableIn: [forks]` per preset at export (reuse `fork_diffs`) so the UI can grey presets out. |
| 1.9 | deliveryMechanisms / syndicateItems sprite refs | FUNCTIONAL | HIGH | — | runtime_verified | config.py:2279-2347, 2349-2366 | All 15 `SPRITE_MANIFEST` sprites exist in `sprites/`; every `sprite` referenced by delivery/syndicate entries resolves. | — | — | — |
| 1.10 | `meta.forks[f].reagentCount` == owned reagents | FUNCTIONAL | HIGH | — | runtime_verified | ss14_chem_extractor.py:2285-2318 | 21/21 forks match (`source==f` counts); `reactionCount` 1104 and `plantCount` 152 match. | — | — | — |
| 1.11 | `categories` vs reagent.category | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified | ss14_chem_extractor.py:1280-1330 | `categories` == sorted set of used values. 75 reagents land in `Other` (misfits 37, stories_cm 13, funky 7). | — | `Other` is a silent catch-all. | Print a per-regen count of `Other` and add keywords for the misfits/stories files. |
| 1.12 | effectTags vocabulary | PARTIAL | HIGH | P2 | runtime_verified + code_verified | ss14_chem_extractor.py:1126-1188 (tags), 604-673 (summaries) | 80 distinct tags, all lower-case, sane. BUT upstream drift has already hollowed some out: vanilla YAML now has 10× `!type:ModifyBleed` and 0× `ModifyBleedAmount`; code matches the old name (1164, 661) → **0 vanilla reagents carry the `bleed` tag** (the 14 that do are fork copies with the old name). 56 vanilla status effects use the new `effectProto:` shape → summarized as the useless "Status: ModifyStatusEffect" (609-610) and get no tag (1178-1187). 487 items overall fall to the generic `Effect: <Type>` path (Polymorph 125, ModifyBleed 33, MovespeedModifier 19, ModifyParalysis 14, ModifyKnockdown 11 …). | Health/thirst/hunger/temperature tags are accurate (spot-checked). | Filter chips (`bleed`, `paralyze`, status keys) and antag auto-scores silently under-count. | Add `effectProto` to the status-key lookup; accept `ModifyBleed`, `MovespeedModifier`, `ModifyParalysis`; make the regen print (and fail above a threshold) the count of generic fallbacks. |
| 1.13 | obtainSources / accessibility vocabulary | PARTIAL | HIGH | P2 | runtime_verified + code_verified | ss14_chem_extractor.py:1728-1768 (global `build_all_sources`), 1845-1934 | `accessibility.tier` uses the 8 designed values; `obtainSources` labels follow the keyword conventions. BUT (a) 372 reagents are `unobtainable` and 174 `unknown` = 39% of the database has no resolved acquisition path; (b) the top-level `obtainSources`/`accessibility` are a global union across forks: vanilla `Cola`/`SodaWater` list Sunrise's vendor "ВОДА", `THC`/`SpaceDrugs` list "Тритиевая Конопля (plant)"; per-fork views exist only inside `antagStrategies.byFork`. | Per-fork accessibility is computed correctly for strategies. | Vanilla players see Russian-fork vendors/plants as sources; 39% of reagents carry a tier the UI renders as "unobtainable/unknown". | Filter the global `obtainSources` by the reagent's own lineage (reuse `allowed_channel_forks` from `build_per_fork_views`), and expose the per-fork tier map on reagents, not only on strategies. |
| 1.14 | nameRu/descRu coverage per fork | FUNCTIONAL | HIGH | — | runtime_verified | ss14_chem_extractor.py:2445-2455 | Current: nameRu 755/1387, descRu 757, physicalDescRu 1321, plants 88/152. CHANGELOG 3.10.0 (line 65-68) said 725/1369 — the delta is exactly the Series O additions (+18 stories_cm, +12 rmc14 names from the stories ru-RU files); plants 88/152 unchanged. 2 nameRu values equal the EN name (`Left4Zed`, `PlantBGone` — untranslated upstream). | Strict resolvers behave as documented. | — | Refresh the CHANGELOG coverage line at the next regen. |

## 2. Fork lineage & visibility (runtime_verified via `integrity.py`, `ownership.py`, `ownership2.py`, `reagent_removal.py`, `drift_checks.py`; live upstream via curl)

Per-fork counts replicating `app.js:233-237 forkVisible()` (visible = source vanilla or in parent chain, minus `forkStatus[f]=='blocked'`):

| fork | owned reagents/reactions (README) | visible reagents | visible reactions | blocked reactions | modified |
|---|---|---|---|---|---|
| vanilla | 407/316 | 407 | 316 | 0 | 0 |
| rmc14 | 113/68 | 520 | 113 | 273 | 3 |
| cmu | 22/16 | 541 | 103 | 297 | 13 |
| rucm | 0/0 | 542 | 105 | 295 | 2 |
| stories_cm | 18/12 | 538 | 102 | 294 | 5 |
| goob | 100/85 | 507 | 395 | 6 | 26 |
| starlight | 27/10 | 434 | 320 | 6 | 17 |
| deltav | 124/125 | 531 | 432 | 9 | 21 |
| deadspace | 22/14 | 429 | 327 | 3 | 15 |
| frontier | 33/21 | 440 | 324 | 13 | 30 |
| funky | 103/101 | 610 | 472 | 30 | 39 |
| trauma | 44/43 | 551 | 443 | 1 | 16 |
| omu | 20/16 | 527 | 404 | 13 | 29 |
| carpmosia | 12/15 | 419 | 328 | 3 | 2 |
| monolith | 28/25 | 468 | 314 | 48 | 22 |
| harmony | 8/7 | 415 | 320 | 3 | 2 |
| corvax | 8/8 | 415 | 322 | 2 | 2 |
| adt | 100/77 | 515 | 398 | 3 | 23 |
| sunrise | 37/46 | 444 | 355 | 7 | 17 |
| fish | 10/11 | 454 | 366 | 7 | 18 |
| misfits | 151/88 | 558 | 330 | 74 | 65 |

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 2.1 | README "Supported Forks" table vs data | FUNCTIONAL | HIGH | P3 (doc) | runtime_verified | README.md:43-65; ss14_chem_extractor.py:2285-2291 | All 21 rows equal the OWNED counts (`source==fork`) in data.json — zero mismatches. The README does not say the numbers are exclusive content, and "Russian Marine Corps 0/0" reads as "empty fork" although 542 reagents are visible there. | Numbers are exact. | Semantics undocumented. | Add a "(fork-exclusive; visible = vanilla + lineage)" note or a second "visible" column. |
| 2.2 | GitHub issue #2 — Oxandrolone (Goob vs Funky) | BROKEN | HIGH | P1 | runtime_verified + http_fetch_only | ss14_chem_extractor.py:2939-2962 (`merge_pass`, first-wins), 3011-3037 (Phase 4c) ; cache/goob/…/_Goobstation/Recipes/Reactions/medicine.yml:310, cache/funky/…/_Funkystation/Recipes/Reactions/medicine.yml:39 | Both forks define reagent AND reaction `Oxandrolone` in their **custom** layers with different recipes: Goob `SodiumCarbonate+Dermaline+Phenol+Leporazine → 4`, Funky `Carbon×2+Lenturi+Phenol → 4` (identical on live upstream, fetched 2026-09-11). Registry order puts goob before funky, so Goob owns both ids (`source: goob`) and Funky's copy is dropped with a stdout line only. Funky users (chain funky→goob) therefore see **Goob's** recipe with `forkStatus: None` — no "modified" marker. The issue title is inverted for the current data (Goob overrides Funky); a Goob-only user sees the right recipe. Generalised: **12 child-fork custom redefinitions of an ancestor-owned id are silently dropped** — funky: `Oxandrolone`, `SalicylicAcid`, `SilverSulfadiazine`, `Multiver`, `Probital`, `TripleCitrus`; rucm: `RMCMethylphenidate`, `RMCCitalopram`, `RMCParoxetine` (unflagged; cmu's identical three ARE flagged `modified` only because cmu declares `parent_override_reaction_files`, config.py:341). | Phase 4c catches parent-layer copies when a fork declares `parent_override_*` manifests. | Redefinitions inside a child's OWN custom directory are never diffed; Funky/Trauma/Omu declare no parent-override manifests. Wrong recipe shown, no marker, issue open since 2026-05-08. | In `merge_pass`, when a skipped copy differs from the owner's (compare with `compare_reaction`), record it into `fork_diffs[fork]` as `modified` (or serialize the child's recipe as a per-fork override). Close #2 with the corrected explanation. |
| 2.3 | Ownership-transfer trap (owner outside the shipping fork's lineage) | BROKEN | HIGH | P1 | runtime_verified | ss14_chem_extractor.py:2883-2907 (Phase 2b harvest), 2939-2956 (first-wins), 2075-2117 (`fork_ancestry`) | 170 (fork, id) pairs — **55 distinct reagents and 26 reactions** — where a fork ships the id (custom or vanilla-path copy) but cannot see it because the owner is not in its parent chain. Headline cases: `Necrosol` owned by **carpmosia** (its custom `_Carpmosia/Reagents/medicine.yml`) while Goob — the originator — and 13 other forks carry it only in vanilla-path copies → invisible on goob/funky/trauma/omu/deltav/frontier/…; `Saxoite`, `JuicePotato`, `KiraSpecial`, `CreateSoap`, `CreateSoapHomemade` owned by **rmc14** purely by registry order (harvest pass) → invisible on goob/funky/deltav/misfits/…; `Warfarin` (goob) invisible on deltav/fish/starlight/sunrise; `BZ`/`Healium`/`Nitrium`/`Pluoxium` (goob) invisible on adt/sunrise. 22 of the reaction pairs have DIFFERENT content in the losing fork (e.g. `Barozine` goob vs carpmosia/monolith/harmony). Per losing fork: sunrise 41, misfits 18, omu 15, funky 14, starlight 12, monolith 11, frontier 10. Concrete user effect: Funky has two soap recipes (`CreateSoap`, `CreateSoapHomemade` in its fun.yml copy) yet sees none — the six vanilla `CreateSoap*` are correctly blocked (Funky's soap.yml is gone upstream) and the two it has are rmc14-owned. No marker of any kind exists in data.json. | Single-owner merge keeps ids unique and regen deterministic. | Silent per-fork data loss; the 5 "non-lineage cross pairs" (`NecrosolLing@goob → Necrosol@carpmosia`, `UnholyWaterLing@goob → UnholyWater@deltav`, …) even produce reagents the same fork cannot see. | Replace single ownership with a `forks: [..]` (shipping set) per reagent/reaction computed from all layers, and make `forkVisible` test membership; at minimum emit `alsoIn` and have the regen fail when a shipping fork cannot see an id. |
| 2.4 | Vanilla reagents removed/abstracted in a fork's vanilla-path copy stay visible | PARTIAL | HIGH | P2 | runtime_verified | ss14_chem_extractor.py:3039-3072 (Phase 4d only diffs `parent_override_reagent_files`) | 196 (fork, id) pairs: misfits 52, monolith 37, funky 17, omu 11, rmc14/cmu/rucm/stories_cm 10 each, frontier 9, goob/starlight/deltav 5 … e.g. `Lye`, `Brass`, `SulfurBlood`, `Felinase`, `Caninase`, `Heparin`, `MilkSheep`; Delta-V marks `DexalinPlus` `abstract: true` in its copy of Reagents/medicine.yml (i.e. removed it) — it stays visible on deltav with `forkStatus: None` (only its reaction is blocked). Reactions get this diff (Phase 4b) but reagents do not. | Reaction-level blocking works; parent-layer reagent removal works (RMCUltrazine on cmu). | Reagent cards exist for forks that deleted them. | Add a reagent-level vanilla-path diff mirroring Phase 4b (absent or `abstract: true` in the copy, not re-contributed elsewhere → `forkStatus[fork]='blocked'`). |
| 2.5 | Grandchild forks do not inherit parent diffs | PARTIAL | HIGH | P2 | runtime_verified | ss14_chem_extractor.py:3018-3023 (`parent_rxns = parsed[parent_id]["reactions"]`), 2519-2532 (per-fork status, no inheritance); regen_log.txt "Russian Marine Corps vs parent cmu: 0 blocked, 0 modified" | rucm's `_RMC14` copies are diffed against **cmu's custom parse**, which holds no `_RMC14` files → empty diff. Data: of rmc14-owned reactions, 5 are `blocked` on cmu but unmarked on rucm (`RMCInstantApple/Grape/Orange/Pineapple/Watermelon`), 11 `modified` on cmu but unmarked on rucm; reagent `RMCUltrazine` blocked on cmu only. RuCM is CMU-based, so RuCM users see recipes CMU removed. | cmu vs rmc14 diff is correct (5 blocked / 16 modified). | Any depth-2 lineage loses the intermediate fork's diffs. | Compare a child's parent-path copies against the true owner layer (walk the chain to the fork that manifests the file) and/or inherit `forkStatus` down the chain in `export_json`. |
| 2.6 | Total-conversion shadowed twins still exported as dispenser chems | PARTIAL | HIGH | P2 | runtime_verified | ss14_chem_extractor.py:2296-2318 (`renamedReagents` count only), 2440-2441 (`isBase`/`isDispenser` global) | `meta.forks.rmc14.renamedReagents = 20` but the shadowed ids are not exported; `Carbon`, `Fluorine`, `Oxygen`, `Hydrogen`, `Ethanol` … remain visible on the RMC family with `isDispenser: true` (the union of all forks' `dispenser_chemicals`) next to their `RMC*` twins, `forkStatus: None`. The D5 decision (docs/decisions/2026-07-12_rmc-renamed-reagents.md) accepted a banner instead. | Dead-recipe blocking (Phase 4e) works. | Base-chemical filter on rmc14 lists 20 vanilla twins that do not exist in that game. | Export `shadowedReagents: [...]` per fork and set `forkStatus[fork]='blocked'` on the twins. |
| 2.7 | rmc14 `dispenser_chemicals` union leaks | FUNCTIONAL (as designed) | MEDIUM | — | runtime_verified | config.py:1595-1597 (`BASE_DISPENSER_CHEMICALS` union) | 23 `RMC*` reagents are `isDispenser: true` globally; harmless because they are rmc14-owned and only visible in that lineage. 4 listed ids do not exist in data (`RMCHydrochloricAcid`, `RMCIodine`, `RMCTungsten`, `RMCWater`, config.py:284) — see 8.8. | — | — | — |

## 3. Dead reactions / total conversion (Phase 4e)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 3.1 | Phase 4e rename map + transitive dead set | FUNCTIONAL | HIGH | — | runtime_verified (regen_log.txt) + code_verified | ss14_chem_extractor.py:2075-2160, 3081-3107 (`TOTAL_CONVERSION_MIN_RENAMES = 5` at 3089) | 20 renamed base reagents on rmc14 → 274 dead reactions (cmu/rucm 275, stories_cm 274); misfits' 2 collisions correctly ignored by the threshold; counts in data.json: 273/297/295/294 blocked. Threshold 5 vs observed 20/2 is a safe margin. Fixpoint restricts to base reagents, catalysts excluded — sane. | Deterministic, threshold documented. | — | — |
| 3.2 | Blocked reactions hidden in consumers — data side | FUNCTIONAL | HIGH | — | runtime_verified + code_verified | app.js:233-237 | `forkStatus[f]='blocked'` on 1387 (reaction, fork) entries; the shared `forkVisible()` honours it for reagents and reactions; no reaction is blocked in its own source fork. 260 rmc14-visible reagents have every recipe blocked but stay visible (D5 design). | Data carries what the UI needs. | Whether every consumer (calculator, trees, medbay) routes through `forkVisible` is T2's scope. | — |
| 3.3 | `scripts/audit_dead_reactions.py` | PARTIAL | HIGH | P3 | runtime_verified (run_audit_dead_reactions.txt) | scripts/audit_dead_reactions.py:70-100 | Reports 0–9 dead reactions per fork (existence-based) while Phase 4e blocks 274 on rmc14; the script's own docstring (docs/decisions D5) admits existence ≠ obtainability. Also prints "�" for em-dashes under cp1251 despite `reconfigure`. | Runs offline, no writes outside cache/. | A diagnostic that contradicts the shipped data misleads the next maintainer. | Fold Phase 4e's rename map into the script or delete it. |
| 3.4 | Blocked reactions hidden in calculator / trees / medbay | UNCERTAIN | LOW | P3 | code_analysis_only | app.js:1557, 1571, 1756 (call sites of `forkVisible`) | Not in scope for a data auditor; T2 verifies. Data-side prerequisites are met (3.2). | — | — | T2 to confirm with the browser. |

## 4. Determinism & reproducibility (all runtime_verified)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 4.1 | data.json regen determinism (scratch copy of pipeline + cache) | FUNCTIONAL | HIGH | — | runtime_verified | scratch `regen/data.json` vs repo `data.json` | Full regen in the scratch dir (config.py, sources.py, extractor, ordnance, cache/, sprites/ copied; 3 min) produced a file equal to the committed one ignoring `meta.generated`, identical byte size 3,845,416; `ordnance/stories_cm.json` identical. 319 first-wins collisions logged to stdout. | Byte-identical regen (invariant 6 holds). | — | — |
| 4.2 | `python ss14_ordnance.py --verify` | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified | ss14_ordnance.py:1328-1338, 1305-1310, 613-615 | "reference: 16 passed, 1 known drift, 0 failed (of 17); targets: 8 passed". Side note: `--verify` is not read-only — it rebuilds and rewrites `ordnance/stories_cm.json` (byte-identical today) and performs 17 network GETs for target `meta.json` on every run (no cache). | Golden set guards the formula. | RULES/README describe it as verify-only. | Add a `--no-write` path or verify against the committed file; cache `meta.json` under `cache/`. |
| 4.3 | `python ss14_map_extractor.py --selfcheck` | BROKEN | HIGH | P1 | runtime_verified | ss14_map_extractor.py:864-1003 (`selfcheck`), 960 (`bake_png` → writes maps/vanilla/Bagel.png), 969 (`build_map_json` → writes maps/vanilla/Bagel.json) | The self-check re-bakes Bagel INTO the repo. After my run `git status` shows ` M maps/vanilla/Bagel.json` and ` M maps/vanilla/Bagel.png`. Diff of the committed (baked 2026-07-13, commit 699c50a) vs re-baked (from cache_maps fetched 2026-07-23): items 2098 → 2088, 10 protos gone (`CrateFoodCooking`, `CrateServiceJanitorialSupplies` no longer exist in the registry; `EmergencyLight`, `PoweredSmallLight` ×3, `MopBucketCubeWrapped`, `BoxCleanerGrenades`, `JanitorServiceLight` dropped), 2 vias removed, 1226 rows renumbered, PNG 11849 → 12367 B. The assertions all passed, so the "self-check" green light says nothing about the committed artifact. | Assertions cover kinds, fills, prices, sizes, chunk decoding. | A verification command mutates tracked artifacts; the committed vanilla bake is not reproducible from the current cache (the ROADMAP "vanilla map drift" note is real). | Make `selfcheck()` write to a temp dir (parametrise `OUT_DIR`), and assert equality against the committed Bagel.json to turn drift into a failing check. Restore the two files now. |
| 4.4 | `scripts/audit_fork_manifests.py` | FUNCTIONAL | HIGH | — | runtime_verified (run_audit_fork_manifests.txt) | scripts/audit_fork_manifests.py:203-300 | Ran with the gh token in ~10 min. Result: **23 MISSING manifest paths, 413 files CHANGED since cache, 70 untracked files with chem prototypes** (misfits 19, starlight 9, adt 8, deadspace 7, omu 7, deltav 6, monolith 6, rucm 3 …). Writes only `cache/fork_manifest_audit.json`. | The only drift detector in the repo works. | It is manual, not part of the regen, and its findings are not consumed anywhere (see 5.2). | Run it in CI weekly like check-links.yml and fail on MISSING > 0. |
| 4.5 | `scripts/audit_dead_reactions.py` | FUNCTIONAL | HIGH | — | runtime_verified | scripts/audit_dead_reactions.py:197-201 | Ran offline in <1 min; writes only `cache/dead_reactions_audit.json`. (Usefulness: see 3.3.) | — | — | — |
| 4.6 | Regen needs the network even with a warm cache | PARTIAL | HIGH | P3 | runtime_verified (regen_log.txt lines 179-277) | ss14_chem_extractor.py:210-213 (404 not cached) | 7 manifest paths 404 upstream (`Recipes/Reactions/soap.yml` for rmc14, rucm, stories_cm, funky, monolith, misfits; rucm `_RMC14/…/powdered_mixes.yml`) are re-fetched on every regen because 404 results are not cached; ordnance fetches 17 meta.json. | Warm-cache regen otherwise offline. | Offline regen impossible; 404s masquerade as "fork removed the file" (see 5.1). | Cache negative results with a timestamp (e.g. `.404` marker) and list them in the regen summary. |

## 5. Extractor robustness & upstream drift (code_verified unless noted)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 5.1 | `fetch_file` / `fetch_all_files` failure semantics | PARTIAL | HIGH | P1 | code_verified + runtime_verified | ss14_chem_extractor.py:194-238 (196 cache hit; 210-213 404/403 → `""`; 221-227 3 retries → `""`); 178 (`rid not in fork_vanilla_rxns → blocked`) | A 404, a 403 (rate limit), a timeout ×3 or a non-UTF-8 byte (205 strict decode) all yield `""`; the path is then simply absent from the fork's file dict. Downstream, Phase 4b treats "file absent" as "fork deleted every reaction in it" → every vanilla reaction of that file is **blocked** for that fork; Phase 2b harvests nothing from it; Phase 4d has a guard (3060-3062) but 4b does not. The cache is immortal (196), so on the maintainer's machine a stale copy keeps serving and the loss appears only on a fresh clone. No partial-file caching (write happens after full read) — good. | Retries, no partial writes, per-file granularity. | Transient failures are indistinguishable from upstream deletions and silently reshape `forkStatus`; a fresh-clone regen would differ from the committed data.json (23 paths already 404). | Distinguish 404 from failure (raise on failure; record 404s), and have Phase 4b skip files that were not fetched, like Phase 4d does. |
| 5.2 | Upstream drift right now (vanilla seeds refactor + missing manifests) | BROKEN (latent — masked by the immortal cache) | HIGH | P1 | runtime_verified + http_fetch_only | config.py:113 (`seed_files: Hydroponics/seeds.yml`); ss14_chem_extractor.py:1357-1379, 1381-1438 (`type == "seed"`, `mutationPrototypes`, `chemicals`, `productPrototypes`) | Upstream `Resources/Prototypes/Hydroponics/seeds.yml` is **HTTP 404** (verified 2026-09-11); seeds became `type: entity` prototypes in `Hydroponics/plants.yml` (72 entities with `Plant`/`PlantData`/`PlantChemicals` components — the M1 "upstream-drift" class). The extractor only parses `type: seed`, so a cold-cache regen yields **0 vanilla plants**, drops 115 plant-derived `obtainSources`, and flips ~367 `cross-botany` accessibility tiers. Also missing upstream: 9 cmu reagent/reaction files + 4 locale files (`_CMU14/Economy/…`, `_CMU14/Medical/…`), rucm 3, sunrise `_Sunrise/Recipes/Reactions/gases.yml`; 413 manifested files changed since the cache (vanilla 34 incl. every reagent file; cache mtimes: 793 files from 2026-07-11, only stories_cm from 2026-09-10). Nothing in the pipeline compares cache vs upstream or surfaces this. | The audit script (4.4) can see it. | `meta.generated = 2026-09-10` implies fresh data; the content is a 2026-07-11 snapshot and the next fresh regen silently loses the botany layer. | Port the seed parser to the new plant-entity schema (or pin the manifest to a commit SHA), run the manifest audit before every regen, and abort the regen on MISSING paths. |
| 5.3 | Unknown YAML tags | PARTIAL | HIGH | P2 | code_verified | ss14_chem_extractor.py:302-322 (`SS14Loader` handles only `!type:`), 325-331 (`yaml.YAMLError → return []`) | Any non-`!type:` tag (e.g. a bare `!Foo` or a new engine tag) raises inside PyYAML and the WHOLE file's prototypes are dropped with a single stdout line. `scripts/_ss14_yaml.py:16-31` already has a tolerant `IgnoreTagLoader` (also handles `!`), but the extractor does not use it. No such error in the current cache (regen log clean), so this is a latent vector, not a current corruption. | `!type:` mapping/scalar/sequence handled. | One unknown tag = one file silently gone. | Register a catch-all `!` constructor on `SS14Loader` (mirroring `_ss14_yaml.py`) and count files that failed to parse in the regen summary. |
| 5.4 | Parent inheritance cycles / `abstract` handling | FUNCTIONAL | HIGH | — | code_verified + runtime_verified | ss14_chem_extractor.py:484-543 | Cycle guard (`resolving` set), non-inheritable keys popped (`abstract`, `parent`, `type`, `id`), `_fork` kept from the child; 16 abstract bases excluded from export; reactions referencing abstract ids only involve `DexalinPlus` (abstract only in Delta-V's copy — see 2.4). | — | — | — |
| 5.5 | Pipeline warnings surfaced in data.json | MISSING | HIGH | P2 | code_verified + runtime_verified | ss14_chem_extractor.py:2962-2964 (collision log → stdout), 2587 (`data["warnings"] = DANGEROUS_INTERACTIONS`), 2721-2725 (only attribution warnings reach `meta.sourceAttribution`) | 319 first-wins collisions, 7 HTTP 404s, 16/21 strategy mismatches, dropped mutation refs, RU coverage — all stdout-only. `data.json.warnings` is the curated interaction table, not pipeline warnings. The M1 rule ("просмотреть warnings экстрактора") therefore depends on a human reading a 770-line console log. | Attribution warnings are persisted. | Nothing machine-readable records what the regen skipped or blocked-by-absence. | Add `meta.pipeline: {fetchedFiles, missingFiles, collisions, parseErrors, genericEffectFallbacks, upstreamHeads}` and diff it in CHANGELOG at each regen. |
| 5.6 | Encoding / BOM / Windows console | FUNCTIONAL | HIGH | P3 (side note) | code_verified | ss14_chem_extractor.py:40-41 (utf-8 stdout), 373 (FTL BOM strip); scripts/audit_fork_manifests.py:179-181 (CRLF-aware SHA) | FTL BOM handled; PyYAML skips a leading BOM for str input; stdout reconfigured so the "→" in the Phase 4e summary cannot kill the run. `ss14_ordnance.py` standalone has no `reconfigure` and prints Cyrillic reference names (survives cp1251, would die on cp866/ASCII). | — | Minor. | Add the same two-line guard to `ss14_ordnance.py`. |
| 5.7 | Locale fallback (prettified ids) & EN locale manifests | PARTIAL | HIGH | P1 | runtime_verified + http_fetch_only | ss14_chem_extractor.py:440-450 (fallback), config.py:680 (goob `"locale_files": []`), 1141 (carpmosia), 1204 (harmony), 1071-1073 (trauma: seeds.ftl only), 1177-1180 (monolith: seeds only) | **583 reagents (42%) have a prettified-id name and 523 (38%) an empty description.** Goob: 98/100 empty desc, 98 prettified — yet upstream has 21 reagent FTL files under `Resources/Locale/en-US/_Goobstation/` (`reagents/meta/medicine.ftl`, `toxins.ftl`, `narcotics.ftl`, … verified via the GitHub trees API). Funky (53/103), Trauma (42/44), Omu (15/20) inherit the same hole. Other gaps: deltav 56, sunrise 34 EN, vanilla 34 (RU-only forks' vanilla copies?), monolith 28, deadspace 22, starlight 22, stories_cm 17 (no EN upstream — documented). | Fallback never crashes; RU names cover the RU forks. | The second-largest fork family ships nameless, description-less cards; the "Suggest a source"/search UX degrades. | Populate `locale_files` for goob/harmony/carpmosia/trauma/monolith from the upstream trees; have `audit_fork_manifests.py` treat `Locale/**/reagents/` as relevant; print prettified/empty counts per fork at regen. |
| 5.8 | M1 upstream-drift risks (Solution→entity, Edible, EntityEffects, gases #39685) | PARTIAL | HIGH | P2 | code_verified + runtime_verified | ss14_chem_extractor.py:1538-1561 (both solution schemas), 1126-1188, 604-673 | Guarded: `extract_entity_fill` reads both `Solution` and `SolutionContainerManager` (12 `type: Solution` and 5 `type: Edible` in the cached item-fill files — no dependency on Food/Drink components). Gases: `seeds.yml` carries no exude/consume fields and `growth` exports none → no corruption possible. NOT guarded and already drifted: effect renames (`ModifyBleedAmount`→`ModifyBleed`, `effectProto` status shape, `ModifyKnockdown`, `ModifyLungGas`, `AdjustAlert`) — see 1.12; seeds→plant entities — see 5.2. Detectability: zero — no schema fingerprint of the parsed YAML, no fallback counters, no upstream head SHA recorded. | Solution migration handled. | Two of the four listed risks have already materialised silently. | Record per-fork upstream head SHA + counts of unknown effect/condition types in `meta.pipeline`; fail the regen when a counter jumps. |
| 5.9 | Spot-check of reagent effects/metabolism vs cache/vanilla YAML | FUNCTIONAL | HIGH | — | runtime_verified | cache/vanilla/Resources/Prototypes/Reagents/medicine.yml, toxins.yml | `Bicaridine` (heals Brute 1.5 even; OD 15 → poison/asphyx; vomit >30u @2%; jitter; drunk), `Epinephrine` (histamine purge >45u; crit-only heals <20u; OD 20; heartbreaker interactions; adrenaline status) and `Lexorin` (asphyx 5 + bloodloss 5) match the YAML in every value, tag and overdose threshold. Epinephrine also demonstrates the `effectProto` degradation ("Status: ModifyStatusEffect", "Effect: ModifyKnockdown"). | Core summariser correct. | — | — |
| 5.10 | Melting/boiling point units | BROKEN | HIGH | P2 | runtime_verified | ss14_chem_extractor.py:1042-1044 (`f"melts at {mp}K"`) | Upstream values are Celsius (`Water: boilingPoint 100.0, meltingPoint 0.0`; `Acetone -50/55.5`; `Oxygen -218.4/-183`) but `verifiedMechanics` prints "melts at 0.0K, boils at 100.0K"; 35 reagents show negative "Kelvin". 121 reagents carry the phase-change line; app.js does not render the raw fields, so the string is the only surface. | Raw `boilingPoint`/`meltingPoint` numbers are faithfully copied. | Wrong unit label in a "YAML-verified ✓" section. | Change the label to °C (or convert +273.15 consistently with the reaction `minTemp` Kelvin fields, which ARE Kelvin — and say which). |
| 5.11 | Provenance of the snapshot (`meta.generated` vs cache age) | MISSING | HIGH | P2 | runtime_verified | ss14_chem_extractor.py:2334 (`"generated": time.strftime(...)`) | `meta` records only the run time. The cache is 793 files from 2026-07-11, 118 from 07-13, 194 from 07-15, 76 from 07-23, 86 from 09-10 (stories_cm only). Upstream vanilla head is 5186b8f 2026-09-10 with 34 manifested files changed since the cache. Users and the CHANGELOG treat "generated 2026-09-10" as data currency. | — | No per-fork snapshot date/SHA; staleness invisible. | Store per-fork `{head_sha, fetched_at}` in `meta.forks[f]` (the audit script already fetches heads) and show it in the UI footer. |

## 6. Maps pipeline (runtime_verified unless noted)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 6.1 | Per-map JSON structure & `p[]` semantics | FUNCTIONAL | HIGH | — | runtime_verified + code_verified | ss14_map_extractor.py:770-821 (`add`, kinds 0 floor / 1 container fill / 2 vendor / 3 off-grid / 4 vendor contraband; `p[3]` = index into `vias`; `p[4]` = prob (<1) or amount/count) | All 114 map files: schemaVersion 2, every `p` entry has 3–5 elements, kind ∈ {0..4}, via indices within `vias`, extras numeric. | Schema v2 consistent across forks. | — | — |
| 6.2 | `maps/index.json` vs files on disk | FUNCTIONAL | HIGH | — | runtime_verified | ss14_map_extractor.py:1022-1032 (prune), 1035-1047 (`write_index`) | 15 forks / 114 maps listed = 114 JSON + 114 PNG on disk; 0 orphans, 0 missing, 1:1 PNG↔JSON; `prices.json` present in all 15 fork dirs; deploy.yml copies `maps/` wholesale (line 33). | Prune logic keeps disk and index aligned. | — | — |
| 6.3 | `MAX_MAPS_PER_FORK` / `MIN_MAP_ITEMS` / `MAP_BLOCKLIST` | FUNCTIONAL | HIGH | P3 (side note) | code_verified + runtime_verified | ss14_map_extractor.py:28, 33-34, 1008-1016; config.py:2397-2402 | Cap 8 (vanilla 99), rotation-first sort, junk filter <100 items, blocklist for CentComm/Dev/Misfits CentComm variants. Index: vanilla 14 maps (all in pool), forks 6–8. Side note: monolith's pool is empty (`inPool=0` for all 7) so the cap picks alphabetically — could admit test maps above 100 items (min observed 354). | Behaves as documented. | Arbitrary selection when a fork has no pool. | Prefer maps referenced by the fork's `gameMapPool`/config when the pool is empty, or log the choice. |
| 6.4 | `price()` vs upstream `PricingSystem` | FUNCTIONAL | HIGH | — | runtime_verified | ss14_map_extractor.py:453-490; cache_maps/vanilla `Entities/Objects/Tools/*.yml`, `Materials/*.yml` | Spot-checks against cache_maps YAML: `Crowbar` = Steel 100 × 0.15 + StaticPrice 22 = **37** ✓; `IngotGold1` = Gold 100 × 0.75 = **75** ✓; `IngotGold` = 30 × 75 = **2250** ✓ (C# default count 30); `SheetSteel` 450 and `MaterialToothSpaceCarp` 1500/50 asserted by the selfcheck run. Caveat: verified against the 2026-07-23 cache, not live upstream. | Mirrors composition×count + StackPrice XOR StaticPrice. | Solutions/MobPrice out of scope (documented). | — |
| 6.5 | `prices.json` v3 layout & trimmed trailing −1 | FUNCTIONAL | HIGH | — | runtime_verified + code_verified | ss14_map_extractor.py:823-856 | vanilla: 11 classes, 6 sizes, 5996 items; entry-length histogram 3:5313 / 2:510 / 1:173 confirms trailing −1 trimming; maps.js:254 documents the same contract. | — | — | — |
| 6.6 | Vanilla map drift / reproducibility | PARTIAL | HIGH | P2 | runtime_verified | maps/vanilla/Bagel.json (commit 699c50a 2026-07-13) vs re-bake (4.3) | Re-baking from the current cache changes Bagel (−10 protos, −2 vias, renumbered indices). `CrateFoodCooking`/`CrateServiceJanitorialSupplies` no longer exist in the 07-23 registry although `bagel.yml` still spawns them; lights (`PoweredSmallLight` …) now classify differently. The vanilla maps were last baked 2026-07-13; forks 07-21..28. `index.json` carries no snapshot SHA/date. | Fork bakes from 07-27 match their cache (no evidence of drift there). | The committed vanilla artifacts are older than their own cache and cannot be regenerated identically. | Re-bake vanilla, then commit `cache_maps/<fork>/_tree.json` SHA + date into `index.json.forks[].snapshot`. |
| 6.7 | Staleness of baked maps vs upstream | PARTIAL | HIGH | P2 | runtime_verified | cache_maps/*/_tree.json mtimes 2026-07-21..27; maps git log (latest data commit 0d88dbf 2026-07-28) | ~7 weeks old on 2026-09-11 while every upstream repo has moved (vanilla head 2026-09-10). Station maps change less than chemistry, but sell-list prices/classes track prototype files that changed (e.g. Goob 14 changed reagent-related files; item prototypes not measured). No cadence/CI. | — | No scheduled re-bake, no age indicator. | Monthly `--all-forks` bake in CI (or at least surface the snapshot date in the Maps tab). |

## 7. Ordnance pipeline

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 7.1 | `compute_stats()` / `engine_params()` vs reference set | FUNCTIONAL | HIGH | — | runtime_verified + code_verified | ss14_ordnance.py:313-361, 364-374; ordnance_reference.py:33-60 | 16/17 mixtures reproduce; the welding-fuel outlier is declared `known_drift` (ordnance_reference.py:56-59) and printed as DRIFT, not FAIL; 8/8 hand-worked target cases pass (armour formula 1.1^(armor/5), coefficient 2 for xenos). | Golden set is a real regression guard. | — | — |
| 7.2 | Parent-chain resolution & `IExplosionModifierEffect` expansion | FUNCTIONAL | HIGH | — | code_verified | ss14_ordnance.py:49-55 (`EFFECT_MODIFIERS`), 243-273 (`_resolve`: later parents override, `abstract` not inherited, `metabolisms` whole-field override), 279-297 (`_reagent_deltas`) | Mirrors SS14 semantics; abstract bases excluded; `via` records explicit vs effect-derived contributions. | — | — | — |
| 7.3 | `schema` vs `schemaVersion` naming; undocumented growth | PARTIAL | HIGH | P3 | runtime_verified + code_verified | ss14_ordnance.py:43 (`SCHEMA = 1`), 1293 (`"schema": SCHEMA`); CHANGELOG.md:16 ("schema 1, ~13 KB") | data.json uses `meta.schemaVersion`, maps/prices use top-level `schemaVersion`, ordnance uses `schema`. The file is now 43.8 KB with `fires`, `targets`, `recipes`, `nonFillable` and 12 fire/HE constants added across O11–O18 — still `schema: 1`, no CHANGELOG entry. | Consumers are in-repo, so nothing breaks today. | Three conventions; silent contract growth. | Rename to `schemaVersion`, bump to 2, document the added keys. |
| 7.4 | Determinism & ordering coupling | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified + code_verified | ss14_ordnance.py:774-792, 794-820, 833-838 (read `data.json` from disk), 1305-1310 (`sort_keys=True`) | Scratch regen produced an identical file; `sort_keys` + deterministic sweep. Side note: `obtainable`, costs and `steps` are read from the on-disk `data.json`, so a standalone `python ss14_ordnance.py` run before a chem regen bakes stale costs (Phase 10 ordering hides this). | — | Undocumented ordering invariant. | Assert `data.json` `meta.generated` ≥ cache mtime or pass the freshly built dict from Phase 10. |
| 7.5 | Targets, sprites, reagent references | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified | ss14_ordnance.py:596-676; sprites/xenos (119 files, git-tracked 134 total) | 17 targets with alive/crit/dead + wound overlays; all 51 sprite refs resolve under `sprites/xenos/`; 33/34 ordnance reagents exist in data.json (`RMCNapalmWeak` does not — it has `nameKey`, so a name fallback is needed client-side). `nonFillable` shipped in the JSON (1174-1177). | — | One reagent outside the chem DB. | Add the napalm file to the stories_cm reagent manifest or drop it from the ordnance pool. |
| 7.6 | Regex-based YAML scanning | PARTIAL | MEDIUM | P3 | code_verified | ss14_ordnance.py:148-165 (`_split_prototypes`, `_top_field` require `^\s{2}key:`), 197-220 | Fields are read with fixed-indent regexes; an upstream reformat (4-space indent, flow mappings) silently drops `power`/`falloffModifier`/`explosive` → mixtures compute as inert. Currently caught only because the golden set would fail. | Reference set mitigates. | Fragile by construction. | Parse with `SS14Loader` (already imported for targets at 402-415) and keep the regex path only as a fallback. |

## 8. Curated data in config.py / sources.py

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 8.1 | ANTAG_STRATEGIES / ANTAG_DATA / DANGEROUS_INTERACTIONS reagent ids | FUNCTIONAL | HIGH | — | runtime_verified | config.py:2061-2277, 1741-1928, 1691-1739 | 21 strategies, 41 ANTAG_DATA keys, 17 warnings — every reagent id exists in data.json. | — | — | — |
| 8.2 | Authored `difficulty` vs `computedDifficulty` | PARTIAL | HIGH | P2 | runtime_verified (regen_log.txt:727-742) | ss14_chem_extractor.py:1980-2058, 2607-2625 | **16/21 strategies mismatch** (silent-kill easy→hard, slow-poison expert→easy, happy-honk-chaos easy→expert, tear-gas-smoke medium→**impossible** because `TearGas` is `unobtainable` — it only exists inside grenade items the manifests do not cover). Mismatches are printed, kept as tooltip data, never reconciled. | The computed model is explicit and per-fork. | Authored tiers are wrong more often than right; the UI shows both. | Either re-author the 16 tiers or drop `difficulty` and show only the computed tier; add TearGas's item source. |
| 8.3 | ANTAG_DATA attribution | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified | config.py:1741-1928; ss14_chem_extractor.py:2416-2421 | 41 tips; 1/41 has an explicit source, 40 carry the `mk-general-antag-playtime` placeholder (amber pill). Coverage report: 16/68 attributed. | Placeholder policy is honest and rendered. | Attribution backlog. | Track in the backlog; issue template exists. |
| 8.4 | SHIFT_PRESETS | PARTIAL | HIGH | P3 | runtime_verified | config.py:1930-1996 | See 1.8 — valid ids, not fork-aware. | — | — | — |
| 8.5 | SPECIES_DATA / BOTANY_GUIDE / SPECIES_GUIDE_SOURCES | FUNCTIONAL | HIGH | — | runtime_verified | config.py:1998-2058 | 8 species curated, 21 reagents with organ-conditional effects lifted; sources resolve (`mk-species-guide`, `mk-botany-guide`). | — | — | — |
| 8.6 | DELIVERY_MECHANISMS / SYNDICATE_ITEMS / method matching | FUNCTIONAL | HIGH | P3 (side note) | runtime_verified | config.py:2279-2347; ss14_chem_extractor.py:1050-1123 | Sprites resolve. 3 strategies' free-text `method` ("Grenade (two-chamber)", "Foam grenade", "Splash or foam-deploy…") match no DELIVERY key → `loreWarnings` and `verificationStatus: partial` (7 partial, 14 all-verified). | Verification model works as designed. | Free-text method vs keyed mechanisms. | Add a `delivery: [keys]` field to strategies. |
| 8.7 | ALLOWED_DOMAINS enforcement | FUNCTIONAL | HIGH | — | code_verified + runtime_verified | sources.py:83-93, 326-361 (warnings only), 400-474; ss14_chem_extractor.py:2703-2707 (owner whitelist from FORK_REGISTRY) | Domain/owner/branch/reddit-scope/youtube-embed checks emit warnings (non-fatal by design); unresolved catalog ids are fatal. 24 warnings today, all "mutable branch". | — | — | — |
| 8.8 | Stale curated ids | PARTIAL | HIGH | P3 | runtime_verified | config.py:1675 (`NukieCola`), 284 (`RMCHydrochloricAcid`, `RMCIodine`, `RMCTungsten`, `RMCWater`) | `OTHER_REAGENT_SOURCES["NukieCola"]` and 4 rmc14 `dispenser_chemicals` reference reagents that no longer exist in data.json; no validation for these two tables (presets/strategies are validated). | — | Dead curated rows accumulate. | Validate `OTHER_REAGENT_SOURCES` and `dispenser_chemicals` keys at export like presets (warning + drop). |

## 9. Data hygiene

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 9.1 | Ids differing only by case | PARTIAL | HIGH | P3 | runtime_verified | data.json | Reagents: `DarkAndStormy`(sunrise, "dark & stormy") / `DarkandStormy`(vanilla, same name); `FrostOil`(vanilla) / `Frostoil`(misfits); `HolyWater`(misfits) / `Holywater`(vanilla). Reactions: `DarkAndStormy`(sunrise) / `DarkandStormy`(vanilla). Distinct upstream prototypes, but they render as duplicate cards on sunrise/misfits. | No true id collision. | Duplicate-looking entries. | Regen warning for case-insensitive collisions; consider per-fork dedupe by locale name. |
| 9.2 | Empty names / descriptions | (counted in 5.7) | — | — | — | — | 0 empty names, 523 empty desc, 583 prettified names. | — | — | — |
| 9.3 | Recipe-without-reaction / zero-product reactions | (counted in 1.2/1.3) | — | — | — | — | 0 / 144 (legitimate effect reactions). | — | — | — |
| 9.4 | NaN / negative numbers | FUNCTIONAL | HIGH | — | runtime_verified | — | No NaN/inf. 297 negatives: 236 `plantEffects.amount` (weed/pest killers), 7 `priority` (Smoke/Foam −10), 54 melting/boiling points — negative because the values are Celsius (see 5.10), not corruption. | — | — | — |
| 9.5 | Suspicious unicode | FUNCTIONAL | HIGH | — | runtime_verified | — | No U+FFFD, no BOM inside strings, no control chars, no oversized (>4000 char) strings; no Cyrillic inside EN `name`. | — | — | — |
| 9.6 | Bloat / redundant fields | PARTIAL | HIGH | P3 | runtime_verified | ss14_chem_extractor.py:2563-2576 (edges), 2363-2370 + 999-1047 (verifiedMechanics), 2607-2635 (byFork breakdowns) | 3.85 MB: reagents 1.86 MB (verifiedMechanics 231 KB — duplicates the `effects` string for 1280 reagents; recipe 202 KB; plantEffects 169 KB; accessibility 137 KB of repeated reason strings), edges 295 KB (unused, 1.4), antagStrategies 255 KB (21×21 `byFork` breakdowns). Fields present-but-empty on most reagents: `boilingPoint` 1266/1387 null, `criticalOverdose` 1359 null, `overdose` 1195 null, `meltingPoint` 1274 null. ≈20% is removable without information loss. | Nothing malformed. | Payload on a 29%-mobile audience. | Drop `edges`; emit only the non-metabolism part of `verifiedMechanics`; omit null scalars; shorten `accessibility` to `{tier}` with reasons in a lookup. |

## 10. Schema / versioning discipline

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 10.1 | `meta.schemaVersion` vs CHANGELOG | FUNCTIONAL | HIGH | — | runtime_verified | ss14_chem_extractor.py:2333; CHANGELOG.md:6-14, 59 | `3.10.0` in data.json; the Series O entry correctly says the data.json schema is unchanged and the regen was additive (1369→1387 verified). | — | — | — |
| 10.2 | Cross-artifact versioning & consumer-side checks | MISSING | HIGH | P3 | runtime_verified (grep) + code_verified | app.js:150 (presence check only); maps.js:11, 215, 254 (comments only); ordnance.js (no reference) | maps `schemaVersion: 2`, prices `schemaVersion: 3`, ordnance `schema: 1`, data `meta.schemaVersion "3.10.0"` — four artifacts, three conventions, and no JS reads any of them (grep confirms only comments). A stale service-worker cache (sw.js precache) serving an old JSON against new JS would fail silently. | Presence guard exists for data.json keys. | No version negotiation anywhere. | One `schemaVersion` key everywhere + a single `expectSchema(artifact, major)` check in the loaders. |
| 10.3 | Ordnance contract growth without bump | (counted in 7.3) | — | — | — | — | — | — | — | — |

## Files examined
- Read (code): `ss14_chem_extractor.py` (entire file in chunks: 1-3162), `config.py` (1-120, 1595-2402 + FORK_REGISTRY dumped programmatically), `sources.py` (1-120, 320-474), `ss14_map_extractor.py` (1-1100), `ss14_ordnance.py` (1-1342), `ordnance_reference.py`, `scripts/_ss14_yaml.py`, `scripts/audit_fork_manifests.py`, `scripts/audit_dead_reactions.py`, `app.js` (lines 148-156, 226-262 + greps), `maps.js` (greps), `ordnance.js`/`i18n.js` (greps), `.github/workflows/deploy.yml` (greps), `.gitignore`, `README.md` (41-90), `CHANGELOG.md` (1-110), `ROADMAP.md` (section 0, D5, E4, L1, greps), `docs/decisions/2026-07-12_rmc-renamed-reagents.md`.
- Read (data): `data.json`, `maps/index.json`, all 114 `maps/<fork>/<Id>.json`, all 15 `maps/<fork>/prices.json`, `ordnance/stories_cm.json`, `sprites/`, `sprites/xenos/`; `cache/**` (goob/funky/vanilla/*_vanilla_overrides/*_parent_overrides YAML, `cache/vanilla/Resources/Prototypes/Hydroponics/seeds.yml`), `cache_maps/vanilla/**` (Tools, Materials, Maps/bagel.yml, `_tree.json` mtimes).
- Ran: `python ss14_ordnance.py --verify` (repo root), `python ss14_map_extractor.py --selfcheck` (repo root — **wrote maps/vanilla/Bagel.*; see 4.3**), `python scripts/audit_fork_manifests.py`, `python scripts/audit_dead_reactions.py`, full `python ss14_chem_extractor.py` in a scratch copy (`scratchpad/regen/`), scratch scripts `integrity.py`, `ownership.py`, `ownership2.py`, `drift_checks.py`, `fork_agnostic.py`, `reagent_removal.py`, ad-hoc map/ordnance/price checks; `git status/diff/log/show`.
- Fetched (read-only HTTP): GitHub issue #2 body; live `funky-station` and `Goob-Station` `Recipes/Reactions/medicine.yml`; live vanilla `Hydroponics/seeds.yml` (404) and `Hydroponics/plants.yml`; GitHub trees API for `Goob-Station` `Resources/Locale/en-US/_Goobstation`.

## Summary counts
- Rows classified: 68 — FUNCTIONAL 35 · PARTIAL 24 · BROKEN 5 · MISSING 3 · UNCERTAIN 1 (cross-reference rows 9.2, 9.3, 10.3 not double-counted).
- Priorities (non-FUNCTIONAL rows): **P0 0 · P1 6 · P2 15 · P3 12**.
- P1: 2.2 (issue #2 / dropped child redefinitions), 2.3 (ownership-transfer trap, 170 pairs), 4.3 (`--selfcheck` writes tracked maps; vanilla bake not reproducible), 5.1 (fetch failure = silent mass-block), 5.2 (upstream seeds refactor + 23 missing manifest paths, masked by the immortal cache), 5.7 (42% prettified names / 38% empty descriptions; Goob locale manifest empty).
- Determinism: data.json and ordnance regen are byte-identical (runtime_verified); maps are NOT reproducible from the current cache.
- Repo state after this audit: `maps/vanilla/Bagel.json` and `maps/vanilla/Bagel.png` modified by `--selfcheck` — restore before committing anything.

## Top findings
1. **P1 — `ss14_map_extractor.py --selfcheck` re-bakes Bagel into the repo (ss14_map_extractor.py:960, 969); the tree is now dirty and the committed vanilla maps (baked 2026-07-13) cannot be regenerated from the 2026-07-23 cache (−10 protos, −2 vias).** Restore the two files; make selfcheck write to a temp dir and compare against the committed artifact.
2. **P1 — Upstream has already drifted under the immortal cache: vanilla `Hydroponics/seeds.yml` is 404 (seeds are now plant entities), 23 manifested paths are missing, 413 changed; a fresh-clone regen silently ships 0 vanilla plants and reshapes `forkStatus` (ss14_chem_extractor.py:196, 210-213, 178; 1357-1438).** Nothing in the pipeline detects it; `meta.generated` claims 2026-09-10 for a 2026-07-11 snapshot.
3. **P1 — Single-owner first-wins merge hides content from the forks that ship it: 170 (fork,id) pairs (55 reagents, 26 reactions) are invisible to a shipping fork, and 12 child-fork redefinitions (incl. Funky's `Oxandrolone` — GitHub issue #2, inverted: Goob's recipe overrides Funky's) are dropped with no `modified` marker (ss14_chem_extractor.py:2883-2962, 3011-3037).**
4. **P1 — 583 reagents (42%) have machine-split names and 523 (38%) empty descriptions because EN locale manifests are missing: Goob's `locale_files` is `[]` (config.py:680) while upstream ships 21 reagent FTLs; Funky/Trauma/Omu inherit the hole.**
5. **P2 — M1 drift has materialised silently in the effect layer: upstream renamed `ModifyBleedAmount→ModifyBleed` and moved status effects to `effectProto` → 0 vanilla reagents carry the `bleed` tag, 56 vanilla status effects render as "Status: ModifyStatusEffect", 487 generic fallbacks overall (ss14_chem_extractor.py:604-673, 1126-1188); and phase-change temperatures are Celsius labelled as K (1042-1044).**
6. **P2 — Fork-agnostic global fields corrupt per-fork views: `recipe` (2388-2389) shows other forks' recipes to 41 vanilla reagents (Coffee←frontier, 16 juices←deltav); `obtainSources`/`accessibility` are cross-fork unions (Russian vendors on vanilla Cola); 196 vanilla reagents removed by a fork stay visible (no reagent-level Phase 4b); RuCM never inherits CMU's 16 diffs (3018-3023); RMC's 20 shadowed twins stay `isDispenser: true`.**
7. **P2 — No machine-readable pipeline provenance or warnings: 319 collisions, 7 HTTP 404s, 16/21 difficulty mismatches and parse failures go to stdout only; no upstream head SHA per fork; no consumer-side schema check across four artifacts with three version-key conventions (`schemaVersion`, `schema`, `meta.schemaVersion`).**
