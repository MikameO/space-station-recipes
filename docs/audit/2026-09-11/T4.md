# T4 — Cross-layer contract audit (systems architect)

Audit date 2026-09-11 · repo `D:\Space Station Recipes` @ e777079 · read-only · no browser.
Method: every contract table below was produced by scripts in the scratch dir (`i18n_check.py`, `contract_check.py`, `final_counts.py`, plus inline `python -` runs) over the real files; "code_verified" rows are script-computed unless marked "code_analysis_only" (manual read). Nothing was run in a browser, so CSS-cascade findings are deterministic by spec but not runtime-verified.

Lens: for every gap — what breaks downstream, and which existing guard/convention should have caught it.

Legend: F = FUNCTIONAL, P = PARTIAL, B = BROKEN, M = MISSING, U = UNCERTAIN.

---

## 1. Asset / version contract

### 1a. Versioned shell assets (index.html ↔ sw.js ↔ deploy.yml)

| Asset | index.html `?v=` | sw.js PRECACHE | deploy.yml cp | Referenced from |
|---|---|---|---|---|
| style.css | v56 (index.html:75) | `./style.css?v=56` (sw.js:14) | :29 | index.html:75 |
| i18n.js | v26 (:758) | v26 (:15) | :29 | index.html:758 |
| app.js | v33 (:759) | v33 (:16) | :29 | index.html:759 |
| tutorial.js | v3 (:760) | v3 (:17) | :29 | index.html:760 |
| maps.js | v16 (:761) | v16 (:18) | :29 | index.html:761 |
| ordnance.js | v41 (:762) | v41 (:19) | :29 | index.html:762 |
| manifest.json | — (:40) | yes (:20) | :30 | index.html:40 |
| sw.js | — | (self) | :30 | app.js:126 |
| favicon.svg / .ico / apple-touch-icon.png | v2 (:37-39) | no (by design) | :29,:31 | index.html:37-39 |
| favicon-192/512.png | — | no | :31 | manifest.json:12,17 |
| og-image.png | — | no | conditional (:35) | index.html:21,33,67 |
| logo.svg, logo-antag.svg, logo-header.svg, logo-header-antag.svg | — | no | **not in cp list** | **referenced nowhere** (header logo is inline SVG, index.html:102-103) |
| CACHE name | — | `chemdb-v74` (sw.js:6) | — | — |

### 1b. Runtime-fetched URLs (JS) vs deploy whitelist vs CI guard

| URL (source) | Served locally | deploy.yml | Precached | Covered by CI guard (deploy.yml:44-46)? |
|---|---|---|---|---|
| `data.json` (app.js:112) | yes | file :29 | no — runtime `chemdb-data` SWR cache (sw.js:52-63) | **no** |
| `./sw.js` (app.js:126) | yes | file :30 | n/a | **no** |
| `maps/index.json` (maps.js:35) | yes | dir :33 | no (network-first, cached after first visit) | **no** |
| `maps/<fork>/<Id>.json` + `.png` (maps.js:80-81) | 114 + 114 | dir :33 | no | **no** |
| `maps/<fork>/prices.json` (maps.js:250) | 15/15 | dir :33 | no | **no** |
| `ordnance/stories_cm.json` (ordnance.js:319, FORK const :17) | yes | dir :34 | no | **no** |
| `sprites/<id>.png` (app.js:2938,2958) | 15/15 | dir :32 | no | **no** |
| `sprites/xenos/<file>` (ordnance.js:2261-2262) | 119/119 | dir :32 (recursive) | no | **no** |
| `./?mode=companion` (app.js:3258) | shell | — | `./` precached (sw.js:12) | n/a |
| `https://fonts.googleapis.com/...` (style.css:13 `@import`) | external | — | never (sw.js:46 same-origin only) | n/a |
| `https://mc.yandex.ru/...` (index.html:83) | external | — | never | n/a |

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 1.1 | Shell version triplet (index ?v= / sw PRECACHE / CACHE) | F | HIGH | — | code_verified | index.html:75,758-762; sw.js:6,11-21 | All six `?v=` values identical in both files; CACHE bumped to v74. | In sync. | — | — |
| 1.2 | Deploy whitelist vs runtime fetches | F | HIGH | — | code_verified | deploy.yml:29-35 | Every runtime-fetched path (1b) is inside a copied file or directory. | All 8 runtime paths ship. | — | — |
| 1.3 | CI "verify every referenced asset shipped" guard scope | P | HIGH | P2 | code_verified | deploy.yml:44-46 | Guard regexes `src|href` in index.html only; 0 of the 8 JS-fetched paths (data.json, maps/, ordnance/, sprites/, sw.js) are checked. The three historical incidents (sw.js/manifest, i18n.js, ordnance.js) were script-tag cases; the next one (a renamed `ordnance/<fork>.json`, a dropped `sprites/xenos`) is invisible to it. | Script/link tags are guarded. | Runtime data files can be missing from `_site` with a green build; page loads, tab shows "Failed to load … Retry". | Extend the guard: grep `fetch('…')`, `.src = '…'` literals and directory prefixes from `*.js` (or hard-assert `_site/data.json`, `_site/maps/index.json`, `_site/ordnance/stories_cm.json`, `_site/sprites/xenos`). Convention 2 in MANIFEST should be turned into this check. |
| 1.4 | Ordnance fork binding | P | HIGH | P3 | code_verified | ordnance.js:17 vs ss14_ordnance.py:1313-1326 | Extractor writes `ordnance/<fork>.json` for every fork with an `ordnance` manifest; frontend hardcodes `FORK = 'stories_cm'`. | Works for the single ordnance fork today. | A second ordnance fork would be baked, deployed and never fetched; `ordnanceForkGate` would keep pointing at stories_cm. | Derive the ordnance fork list from `DATA.meta.forks[*].ordnance` (emit a flag in forks_meta) or ship `ordnance/index.json`. |
| 1.5 | Orphan tracked assets | P | HIGH | P3 | code_verified | repo root: logo.svg, logo-antag.svg, logo-header.svg, logo-header-antag.svg | Tracked, never referenced by index.html/style.css/app.js/manifest/README, not deployed. | Nothing depends on them. | Repo noise; a future `<img src="logo-header.svg">` would 404 on prod (not in cp list) and pass the guard only if placed in index.html. | Delete or add to cp list when used. |
| 1.6 | sw.js data.json comment | P | HIGH | P3 | code_verified | sw.js:4-5 vs app.js:132-133 | sw.js says data.json is requested with `?v=Date.now()`; app.js fetches `data.json` with no buster (SWR design). | Behaviour is correct (ignoreSearch fallback still harmless). | Comment misleads the next editor into re-adding a buster that would defeat the SWR cache. | Fix the comment. |

---

## 2. Data contract (extractor → JSON → frontend)

Union of keys actually present in data.json (script): reagents 33 keys, reactions 12, plants 9, antagStrategies 11, shiftPresets 7, deliveryMechanisms 5, syndicateItems 4, sources 8, warnings 4, edges 5, meta 6 (+ forks entries 6). JS property reads collected from all five JS files (`.key`, `['key']`).

**Keys read by JS but possibly absent — all verified emitted (13 nested suspects: computedDifficulty.{tierVariesAcrossForks,mismatchReason,byFork,authoredTier,effortScore}, plants.chemicals[].{potencyDivisor,min,max}, species.physiology[].{toxicGas,breathes,note,name}, antagTipsSources, sources[].title, botanyGuide.sections[].{h,body}, meta.forks[].parent, reactions[].forkNotes).** No undefined-at-runtime key found.

**Keys emitted but never read (exact, with bytes):**
- `edges` (top level) — 295,219 B — last consumer removed in e523e46 (A1.1, 2026-07-28, Graph tab); still hard-required by `init()` (app.js:150)
- `reagents[].metabolismPaths` — 50,248 B (1387×)
- `reagents[].boilingPoint` — 32,044 B; `reagents[].meltingPoint` — 31,995 B
- `plants[].growth.{production,idealLight,nutrientConsumption,idealHeat,waterConsumption}` — 9,268 B
- `plants[].rsi` — 8,856 B
- `meta.{schemaVersion,generated,reactionCount,plantCount,sourceAttribution}` — 3,660 B (`reactionCount` was consumed by the removed Stats tab, e523e46)
- `antagStrategies[].loreWarnings` — 1,207 B (README:123 advertises them)
- `sources[].authorityWeight` — 641 B (JS recomputes from `type`, app.js:2588-2591)
- `meta.forks[].reagentCount` — 382 B; `reagents[].plantEffects[].probability` (22×)
- **Total 433,520 B = 11.3 % of data.json (3,845,416 B, uncompressed).**

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 2.1 | Keys read ↔ keys emitted (data.json) | F | HIGH | — | code_verified | contract_check.py | 13 nested suspects all present; no read of a missing key. | Runtime never sees `undefined` for a documented key. | — | — |
| 2.2 | Dead data in data.json | P | HIGH | P2 | code_verified | app.js:150; ss14_chem_extractor.py (emitters) | 433 KB / 11.3 % emitted and never read; `edges` alone is 7.7 % and only survives because `init()` refuses to start without it. | Everything the UI needs is present. | Every first visit and every SW background refresh moves ~430 KB of unused JSON; the extractor cannot drop `edges` without a coordinated app.js edit (else "Invalid data.json structure"). Second-order: the SWR data cache (sw.js:52-63) keeps re-downloading the bloat on every visit. | 1) remove `edges` from the required-field check (app.js:150); 2) stop emitting edges/boilingPoint/meltingPoint/metabolismPaths (or move to `extras.json`), bump major schema, CHANGELOG. Guard that was missing: a producer/consumer key manifest (see 2.5). |
| 2.3 | `loreWarnings` feature | M | HIGH | P3 | code_verified | README.md:123 vs app.js (0 refs) | README: "debunking `loreWarnings` — carries a `sources` list … for frontend consumption". Emitted for all 21 strategies, rendered nowhere. | Data exists. | Documented feature invisible to users; curator effort wasted. | Render in the strategy card (near `renderSources`, app.js:2907) or drop the README claim. |
| 2.4 | Authority ladder duplicated | P | HIGH | P3 | code_verified | sources.py:59-68 ↔ app.js:2588-2591; data `sources[].authorityWeight` unread | Two hand-copied weight tables; emitted weight ignored. | Values identical today. | Editing sources.py alone changes pill tiers in data but not in the UI. | `const weight = src.authorityWeight ?? _AUTHORITY_WEIGHTS_JS[src.type]`. |
| 2.5 | data.json version skew (SW) | P | MEDIUM | P2 | code_analysis_only | sw.js:7-10,52-63; app.js:132-136; `meta.schemaVersion` unread | Shell is versioned (`?v=`), data.json is not; SW serves the cached data.json first and refreshes in background; DATA_CACHE deliberately survives shell bumps. `meta.schemaVersion` is never checked. | Additive schema changes (all so far) are harmless. | A breaking regen (e.g. dropping `edges` per 2.2) runs new app.js against the previous data.json for one full session; no guard exists. | Read `meta.schemaVersion` in `init()`; on major mismatch force a network refetch (`cache:'reload'`) or SW-message a DATA_CACHE purge. |
| 2.6 | maps/<fork>/<Id>.json ↔ maps.js | F | HIGH | — | code_verified | maps.js:134-136,142,156-160,215-218; Box.json | Keys {schemaVersion,id,name,fork,bounds,beacons,vias,offgrid,items{n,c,p[3..5]}}; tuple lengths 3/4/5 (3380/1552/13506), kinds 0/1/2/4 — all read paths defined. Unread: schemaVersion,id,offgrid (1,795 B over 114 maps; kind-3 labels come from `vias`, ss14_map_extractor.py:795-796), bounds.minY/maxX. | Renders. | — (offgrid dict is redundant, trivial). | Drop `offgrid` from build_map_json or read it. |
| 2.7 | maps/index.json ↔ maps.js | F | HIGH | — | code_verified | maps.js:50-57; ss14_map_extractor.py:1037-1045 | forks[{key,label,maps[{id,name,file,inPool,items,beacons,px}]}], 15 forks / 114 maps; every `file` has json+png, no orphans on disk, no missing pngs. | — | — | — |
| 2.8 | prices.json v3 ↔ maps.js | F | HIGH | — | code_verified | ss14_map_extractor.py:822-857; maps.js:254-261 | 15/15 forks schemaVersion 3; value lengths {3:5313, 2:510, 1:173}; 1,537 entries `[price,-1,sizeIdx]`, 2,047 price≤0 entries; maps.js handles `-1`, short arrays and `ent[0] > 0`; class tables ⊆ CLASS_ORDER. | Trimmed-`-1` convention round-trips. | — | — |
| 2.9 | ordnance/stories_cm.json ↔ ordnance.js | P | HIGH | P3 | code_verified | ordnance.js:328 vs formula.costBase; recipes[].labels vs ROLE_LABEL (ordnance.js:2078,2102-2106) | All read keys exist. Unread: formula.{armorBase,armorStep,firePatStacks,fireTick,costBase}, casings.{cone,fuel,fuelAmount,mode,sticky}, reagents.nameKey, targets.{armor,rsi}, recipes.labels (51 explanatory labels), fires.armorMultiplier. `costBase` duplicated as the literal `'RMCPhoron'`. `RMCNapalmWeak` exists in the ordnance JSON but not in data.json (silently dropped by `reagentChoices`, ordnance.js:379-383). | UI works. | Two label vocabularies for the same recipe rows (Python "90% of the best radius for the least material" vs JS "Near max reach"); the richer baked text is never shown. | Show `labels` as the row tooltip or stop baking them; read `formula.costBase`. |

---

## 3. i18n contract (index.html + JS chrome ↔ i18n.js T/RX)

Counts (i18n_check.py / final_counts.py, multi-key lines parsed): **T = 559 entries / 552 unique / 7 duplicate keys; RX = 34 rules.**

**(a) UI strings with no dictionary entry (untranslated in RU mode):**
- index.html — 278 strings, 244 covered, **34 untranslated**: 21 ordnance aria-labels (`Add a reagent`, `Blend from`, `Blend to`, `Chart mode`, `Cost basis`, `Distance from the centre`, `How the third reagent is supplied`, `Left axis metric`, `Mask mode`, `Objective`, `Right axis metric`, `Seconds spent standing in the fire`, `Seconds still alight after leaving the fire`, `Surface colour metric`, `Surface height metric`, `Surface view`, `Surface x axis`, `Surface y axis`, `Swap the height and colour metrics`, `Swept reagent`, `Third reagent amount` — index.html:493-733), `Temp` (:274), `⚙ Filters` (:95 — T has `Filters`, node text is `⚙ Filters`), two disclaimer-panel fragments (:134 `, an open-source multiplayer game. All content — …` and `. Real chemical names …`) → RU disclaimer panel renders half English; 9 proper nouns (ChemDB, NanoTrasen, title, Space Station 14, Syndicate, GitHub, Feedback, © line) — acceptable.
- JS chrome (app.js/maps.js/tutorial.js) — 114 static strings, 73 covered, **35 untranslated** (noise removed): `All targets are base chemicals` (app.js:2533), `Breathes` (:1665), `CAT` (:1258,:1803), `Check the connection — the app works offline only after one successful visit.` (:144), `Clear the search, effect chips, or sidebar filters.` (:690), `Collapse or expand` (:1824), `Curator's playtime notes. …` (:2706), `Empty beaker — everything was consumed.` (:1535), `Final beaker` (:1539), `In-game Delivery` (:2728), `Invalid data.json structure — missing required fields` (:152), `No mixing needed` (:2086), `No plant-affecting chemicals match the current filters` (:689), `No recipe, no plant, no dispenser source — not available in normal vanilla play. …` (:1275), `No source cited for this entry — …` (:2619), `No strategies match the active filters.` (:2923), `Nothing reacts at this temperature.` (:1526), `Open a pre-filled GitHub issue suggesting a source` (:2623), `Retry` (:145; maps.js:42,101), `Suggest a source` (:2623), `This is a base chemical` (:2082), `Try removing some filters from the sidebar.` (:859), `Warnings` (:2378), `no products` (:1520), `not found on this map` (maps.js:188), `reaction priority` (:1519), `with` (:2385), `← Back` (:1265 — T has `Back`, the node is `← Back`), `☠ Antag Intel` (:2722), `⚠ Danger Warnings` (:2406), `⚠ Your beaker mixes reagents from different forks …` (:1527), `⚠ needs attribution` (:2619), `✓ Verified in SS14 code` (:2696), `Failed to load data.json after 3 attempts` (:143), `Loading map index…`/`Loading <file>…` (RX covers).
- Composite (interpolated) strings not matched by T or RX — **19**: `Mixing Steps (N)` (:2085,:2532), `OD Nu` (:907), `Reaction cascade (stopped at 30 steps)` (:1537), `Shopping List for Nu X` (:2081), `Total Shopping List for: …` (:2528), `X is a total conversion …` (:493), `N of N match filters` (:2921), `X physiology` (:1662), `with X: …` (:2385), `☠ X is TOXIC to them` (:1660), `⚗ N steps` (:908), `Filters (N)` (:418), `Min: NK` / `Max: NK` (:1217-1218), `Computed: … Curator says: …` / `Scope: …` / `Tier varies across forks …` tooltips (:2880-2887), `in X (~N%)` (maps.js:227), `Collected N / N` (:1861).
- ordnance.js own RU map — 93 entries / 92 unique (duplicate `Burn time`: `Длительность горения` → overridden by `Горение`, ordnance.js:123,151); **all 76 `tr('…')` literals covered**; 4 RU keys never used (`Intersection of`, `Masks`, `No masks yet. Add one …`, `Without` — the first two are reached only through `tr(S.maskMode === …)` and T); 17 keys exist in both RU and T, **2 with different translations** (`Burn time`, `Without`: `Без` vs `Без материала`); 10 ordnance aria-labels untranslated (`Clear pin`, `Enable mask`, `Mask comparison`, `Mask metric`, `Mask threshold`, `Remove mask`, `Remove requirement`, `Required metric`, `Required value`, `Retry`); 7 `CASING_LABEL` values EN-only (`M15 grenade — 180u` in the RU casing dropdown).

**(b) Dictionary entries matching no current UI string (dead, not data vocabulary) — 10:** `Base Chemicals`, `Composting`, `Fertilizer`, `Flash`, `MOD`, `Mix in beaker — triggers automatically`, `Nothing to optimise for this metric.` (Pareto section, O10), `Show Anyway`, `Uncategorized`, `Switch to English` (set by i18n.js:52 itself). Dead RX stems — 3: `Effect: `, `Requires Botany: … (plant)`, `N (N rxn)` (Stats tab). Duplicate T keys — 7: `From` (`От` → **`Из`**), `To` (`К` → **`В`**) — semantic collision: the fork-diff entry (i18n.js:245-246) overrides the ordnance blend labels (:156-157) since the later object key wins; `Filters`, `Load this mix`, `Cost in`, `Target`, `All species` — identical values.

**(c) Tutorial:** 18/18 step strings + Skip/Next/Back/Close covered (tutorial.js:24-104 ↔ i18n.js:340-357). All 7 tutorial targets exist in index.html.

**(d) lang handling:** `document.documentElement.lang = lang` (i18n.js:26) ✓; `?lang=` wins over localStorage (i18n.js:21-22) ✓; `encodeURLState` has no `lang` (app.js:3036-3059) and the share URL is `origin + pathname + hash` (app.js:3203) → a RU user's shared link opens in the recipient's default (EN unless their localStorage says ru).

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 3.1 | Static chrome coverage | P | HIGH | P3 | code_verified | i18n.js:73-585; index.html:493-733 | 244/278 HTML strings and 73/114 JS strings covered; the Series-O aria-labels (21+10) were never added. | Visible labels of all 8 tabs translate. | Screen-reader labels of the whole Ordnance tab, error/empty states, and the disclaimer panel stay English in RU mode. | Add the 31 aria-labels + 35 chrome strings; the `window.__i18nMiss` hook (i18n.js:638) already collects them — dump it once per tab in RU mode. |
| 3.2 | Composite strings | P | HIGH | P3 | code_verified | i18n.js:596-634 | 34 RX rules; 19 interpolated patterns have none. | Plural rules for counts. | "Mixing Steps (3)", "OD 30u", "X physiology" etc. stay English. | Add RX rules or wrap the numbers in `<span>` so the text node is a pure key. |
| 3.3 | Duplicate keys `From`/`To` | P | HIGH | P3 | code_verified | i18n.js:156-157 vs :245-246 | Last duplicate wins: ordnance "From/To" reagents show `Из`/`В` instead of `От`/`К`. | Both features translate to *something*. | Wrong preposition on the ordnance blend selector. | Give one side distinct keys (e.g. `Blend from`/`Blend to` already exist as aria-labels). |
| 3.4 | Two RU dictionaries | P | HIGH | P3 | code_verified | ordnance.js:93-166 vs i18n.js T (17 overlaps, 2 differ) | Ordnance keeps its own map for number-built strings; overlaps drift (`Burn time`, `Without`). | Documented rationale (ordnance.js:90-92). | Same English label renders two different Russian words in one tab. | Let ordnance.js consult `window.__i18nT` (export T) before its local map, or dedupe. |
| 3.5 | Dead dictionary entries / rules | P | HIGH | P3 | code_verified | i18n.js (10 T keys, 3 RX rules) | Leftovers of the removed Reactions/Stats/Pareto sections. | Harmless. | Maintenance noise. | Delete. |
| 3.6 | `<html lang>` + `?lang=` deep link | P | HIGH | P3 | code_verified | i18n.js:17-26; app.js:3036-3059,3203 | lang attribute correct; share link drops language. | Deep link works when typed. | Shared RU links open EN. | `if (window.I18N_LANG==='ru') params.set('lang','ru')` in encodeURLState and read it from the hash too. |

---

## 4. Analytics contract (code ↔ registry ↔ docs)

Code sends **52 goal ids** (43 literal `track()`/`tTrack()` + `search_used`/`search_zero` via ternary app.js:721 + `tab_<id>` for 8 non-default tabs, app.js:513). Registry `GOALS` (scripts/create_metrika_goals.py:46-109) = **53**. Diff: code ∖ registry = ∅; registry ∖ code = {`ordnance_pareto_use`} (retired, documented :93-95). research/…/raw/goals_registry.json = 21 ids (07-28 export before the +18 creation; 36 registry ids absent — a dated snapshot, not drift).

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 4.1 | track() ids ⊆ registry | F | HIGH | — | code_verified | app.js, maps.js, ordnance.js, tutorial.js ↔ create_metrika_goals.py:46-109 | 52/52 present; only the retired Pareto goal is extra. | Convention 3 held for Series O. | — | — |
| 4.2 | Registry synced to the counter | U | LOW | P2 | code_analysis_only | create_metrika_goals.py (mtime 2026-09-11 01:53); export_metrika_stats.py:213 | 14 `ordnance_*` ids were appended on 09-10/11; whether the script was actually run (owner token) is unverifiable here. The 16-day Maps blind spot (ROADMAP:185) is exactly this state. | Registry file is complete. | If not run, every Ordnance conversion since 09-10 is dropped by Metrika. | Owner: `python scripts/create_metrika_goals.py` (idempotent) and confirm `missing=[]` from export_metrika_stats. |
| 4.3 | app.js header comment registry | P | HIGH | P3 | code_verified | app.js:69-97 | Lists 11 of 16 ordnance goals (missing ordnance_recipe_use, _gallery_range, _burn_window, _surf_swap, _req_exclude). | — | The comment claims to be the "registry of record" list. | Point the comment at the script instead of duplicating the list. |
| 4.4 | Script docstring / handoff doc | P | HIGH | P3 | code_verified | create_metrika_goals.py:11 ("21 goals"); research/site-analytics-2026-07/handoff-analytics-goals.md:50-53 | Stale counts and an unchecked "owner: create 21 goals" checklist. | Historical value. | Misleads a new contributor about the registry size. | Update docstring; mark handoff §5 done. |
| 4.5 | README Privacy vs Metrika init | F | HIGH | — | code_verified | README.md:156 ↔ index.html:85 | webvisor/clickmap/trackLinks/goal events all disclosed. | — | — | — |

---

## 5. Documentation ↔ code

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 5.1 | README Supported Forks table | F | HIGH | — | code_verified | README.md:43-65 ↔ data.json meta.forks / reactions.source | 21/21 rows match reagentCount and reactions-by-source exactly; badges 1387/1104/21 match. | — | — | — |
| 5.2 | README Features "Multi-fork" row | P | HIGH | P3 | code_verified | README.md:29 | Names 20 forks; Space Stories missing. | — | Doc/code count mismatch on the first screen. | Add it. |
| 5.3 | README Ordnance row | P | HIGH | P3 | code_verified | README.md:36 vs index.html:522-677 | "two-reagent heatmap" and "cheapest mixture at a share of the ceiling" → now 3D/flat mixture surface, masks, spec search with cost ladder, recipes, xeno gallery. | — | Under-describes the tab. | Rewrite after O19. |
| 5.4 | README "Adding a New Fork" | P | HIGH | P3 | code_verified | README.md:86 | `pip install pyyaml openpyxl pillow` — `openpyxl` imported nowhere (xlsx export removed, ROADMAP rev 1.2 / 82704ce). `CATEGORY_SHEET_MAP` (config.py:2368) is still used (ss14_chem_extractor.py:1328) despite its name. | Steps otherwise valid. | Installs a dead dependency; also omits `python ss14_map_extractor.py --all-forks` / `ss14_ordnance.py` for map/ordnance forks. | Drop openpyxl; add the two extra regen steps; rename the map to CATEGORY_FILE_MAP. |
| 5.5 | README Architecture block | P | HIGH | P3 | code_verified | README.md:92-105 | Missing i18n.js, tutorial.js, ordnance.js, sw.js, manifest.json, ss14_ordnance.py, ordnance_reference.py, ordnance/, sources.py, scripts/. `sprites/` described as "reagent sprite assets" (they are delivery items + xenos). | — | New contributor map is 10 files short. | Regenerate the block. |
| 5.6 | README Data Provenance claims | P | HIGH | P3 | code_verified | README.md:113-123 | `verifiedMechanics`, `computedDifficulty`, `verificationStatus`, `sources` ✓ emitted and rendered; `loreWarnings` ✓ emitted, ✗ rendered (2.3). | 4/5 claims true. | 1 claim describes a non-feature. | See 2.3. |
| 5.7 | Issue-template ids ↔ prefilled URLs | F | HIGH | — | code_verified | app.js:2621 (`entry_id`) ↔ attribution.yml:19; app.js:2912 (`title`,`strategy_id`,`fork`) ↔ strategy-inaccuracy.yml:18,41 | All query params match field ids. | Prefill works. | attribution.yml:15 links `docs/attribution-ladder` (does not exist); strategy-inaccuracy.yml:45 placeholder lists 8 of 21 forks. | Fix link to README §Source attribution; regenerate placeholder. |
| 5.8 | CHANGELOG top entry vs shipped state | P | HIGH | P3 | code_verified | CHANGELOG.md:6-41 ↔ data.json meta 3.10.0, ordnance/stories_cm.json | Schema claim ✓ (3.10.0 unchanged). "~13 KB" (:16) → 43.8 KB; "Pareto search" (:32) removed in O10; no entry for O6–O19 (surface, masks, pick, spec search, targets, recipes, fire model, denial); ordnance `schema: 1` unchanged while the payload gained `fires`, `targets`, `recipes`, `nonFillable` and 13 formula keys. | Data-schema history for data.json is accurate. | A consumer of ordnance JSON cannot detect the shape change; changelog reader believes a removed feature exists. | Add an O6–O19 entry; bump ordnance `SCHEMA` to 2 (ss14_ordnance.py:43). |
| 5.9 | ROADMAP statuses vs code | P | HIGH | P3 | code_verified | ROADMAP.md:3,14,57,164-168,259,286,310-314,318 | Header "Схема данных: 3.4.2" (now 3.10.0); §0 rule 2 omits the sw.js PRECACHE/CACHE bump that S3 (:216) learned; E4 `[~]` text says fork maps deferred while 15 forks/114 maps ship (harmony/corvax/adt/sunrise/fish/stories_cm still absent → status legitimately partial, text stale); **A5 numbered twice** (:57 Fewest Steps, :286 State survives reload); revisions table has two `1.5` rows (:310,:313) and two `1.7` rows (:312,:314); last revision 2.1 (:318) covers O1–O5 only, O6–O19 have sections but no revision row; O10 (:259) matches code (no Pareto section) but README/CHANGELOG contradict it. Backlog items: none already done. | Increment sections themselves are accurate. | The workbook's own DoD rule (§0.2) does not encode the sw.js invariant that bit S3. | Renumber A5→A6; fix header; add sw.js to §0.2; add revision 2.2. |
| 5.10 | docs/design/2026-09-10 ordnance ↔ ordnance.js | P | HIGH | P3 | code_verified | :47-52, :114-129, :728-729 vs index.html:487-737, ordnance.js:2280-2299 | Item 5 "Справка по механике" (formula/casing/modifier reference panel) — **not implemented** (no such section). JSON sketch (`cost`, `burnColor`) ≠ real keys. "Таб должен скрываться на остальных 20 форках" + ROADMAP O3 (:239) + MANIFEST + app.js comments (:459, :3086-3087) say hidden tab; code shows the tab always and a fork note instead (ordnance.js:2280-2284) — and that note is the broken element (10.7). Addenda O6–O18 otherwise match the implementation (mask semantics, pin-as-ratio, breach, denial, nonFillable single source :550-556). | Formula section (:61-77) matches Python/JS (see §6). | Spec item promised and never delivered; four comments describe a mechanism that no longer exists. | Either implement the reference panel (data already in JSON: formula + casings + reagents.via) or strike item 5; update the four comments. |
| 5.11 | docs/design/2026-07-27 sell-list ↔ maps.js | F | HIGH | — | code_verified | maps.js:247-426 | v3 addendum (sizes, per-slot, min price/max size, multi-show) matches; non-goals (deep-link `list=1`, solutions, MobPrice) correctly absent. | — | — | — |
| 5.12 | docs/decisions/2026-07-12 RMC renames | P | HIGH | P3 | code_verified | :17,:32 vs data meta.forks.rmc14.renamedReagents = 20, CHANGELOG 3.9.0 | "21 renamed reagents" vs 20 everywhere else. Mechanism (Phase 4e, forkStatus blocked, totalConversion banner app.js:492-494) still true and now also flags stories_cm automatically as promised. | Decision holds. | Off-by-one in the record. | Fix the number. |
| 5.13 | docs/decisions/2026-07-26 RU | F | HIGH | — | code_verified | :16-18 vs config (7 forks with locale_files_ru), data (755/1387 nameRu across 16 source forks) | "Six forks / 725/1369" is the 07-26 snapshot; stories_cm added since; shared ids let RU names reach 9 more forks (goob 9, misfits 6, starlight 3 …). | Architecture unchanged. | Numbers dated (expected for a decision record). | — |
| 5.14 | index.html meta / JSON-LD | P | HIGH | P3 | code_verified | index.html:13,20,32,49,68 | "1369 reagents … 1092 reactions … 20 forks" vs 1387/1104/21; featureList still lists "Reaction lookup" (tab removed A1.1) and lacks Ordnance/RU/Sell list. | Canonical/og:url correct. | Link previews advertise stale numbers and a removed feature. | Generate these numbers from data.json at regen (a `--stamp-html` step) or update by hand. |
| 5.15 | manifest.json description | F | HIGH | — | code_verified | manifest.json:4 | Generic, still true. | — | — | — |
| 5.16 | promo/steam-guide-ru.md (untracked) | P | MEDIUM | P3 | code_verified | :23, :49 | "20 форков" (21); "кнопка 📌 в компактном режиме" — the pin button is in the main header (index.html:122). No removed tabs referenced. | Tab names match. | Minor. | Fix before publishing. |
| 5.17 | NOTICES attribution coverage | P | HIGH | P2 | code_verified | NOTICES:13-24 | Lists 8 forks; data.json/sprites redistribute content from 21 (sprites/xenos are Space Stories/RMC14 art; RuCM, CMU, Stories, Trauma, Omu, Carpmosia, Monolith, Harmony, Corvax, ADT, Sunrise, Fish, Misfits unlisted). | MIT notice for the 8. | MIT requires the copyright notice for every redistributed source; 13 forks (incl. the fork whose xeno sprites ship) are unattributed. Guard: none — NOTICES is hand-written, registry is code. | Generate the fork block of NOTICES from `FORK_REGISTRY[*].repo` at regen; add the sprite provenance line for `sprites/xenos`. |
| 5.18 | check-links.yml ↔ .gitignore | P | HIGH | P3 | code_verified | .github/workflows/check-links.yml:22,44-48 ↔ .gitignore:2 | Workflow auto-commits `cache/link_health.json`; `cache/` is ignored → the commit step silently no-ops every week (file untracked, 32 green runs). README:142 only promises the issue, which does work. | Issue filing works. | The intended persisted history never lands. (Infra detail — T3 owns; listed here as a gitignore↔workflow disconnect.) | Move the file to `research/link_health.json` or drop the commit step. |
| 5.19 | .gitignore ↔ tracked .claude/launch.json | P | HIGH | P3 | code_verified | .gitignore:7 vs `git ls-files .claude/launch.json` | Tracked file inside an ignored directory. | Works. | Confusing for contributors. | Untrack or un-ignore explicitly. |
| 5.20 | Maps legend ↔ marker kinds | P | HIGH | P3 | code_verified | index.html:480-484 vs maps.js:132 (kind 4 red) and CHANGELOG:127-135 | Legend shows floor/container/vendor; contraband slot (kind 4, red) undocumented in UI. | 3 of 4 kinds explained. | Red markers unexplained. | Add the fourth legend entry (`.maps-dot-contraband`). |

---

## 6. Formula parity (upstream ↔ ss14_ordnance.py ↔ stories_cm.json ↔ ordnance.js ↔ design doc)

| Constant / branch | C# (per design doc :61-77) | ss14_ordnance.py | stories_cm.json `formula` | ordnance.js | Verdict |
|---|---|---|---|---|---|
| power = Σ q·power (explosive only) | ✓ | :323-330 | — | :200 | ✓ |
| falloff = base + Σ q·falloffMod | ✓ | :319,:330 | — | :191,:200 | ✓ |
| hidden `IExplosionModifierEffect` deltas (Explosive/Oxidizing/Fueling/Flowing/Viscous) | table :91-97 | `EFFECT_MODIFIERS` :50-56, applied in `_reagent_deltas` :298-310 and **baked** into JSON i/d/r/power/falloff (:1203-1216) | reagents.i/d/r pre-summed, `via` for display | consumes baked values (:200-203) | ✓ parity by construction |
| shards = Σ floor(q_iron·0.25); 0 if power ≤ 0 | ✓ | `int(q*0.25)` :338, :341 | shardsPerUnit 0.25, ironReagent | `Math.floor(qty*F.shardsPerUnit)` :212, :218 | ✓ |
| power clamp maxP; falloff clamp minF; dampener ×2 after clamp | ✓ | :343-346 | — | :219-221 | ✓ |
| shards clamp casing.shards | ✓ | :347 | — | :222 | ✓ |
| fire axes clamped only if Σ intensity > 0 | ✓ | :349-355 | — | :224-228 | ✓ |
| maxIntensity = P/5, slope = max(F/5, 0.1), radius = maxI/slope, total = π/3·slope·max(0,r−1)³ | ✓ | `engine_params` :374-386 | intensityDivisor 5, minSlope 0.1 | `engineParams` :242-249 | ✓ |
| star = allowStar ∧ intensity > 30 | ✓ | :358 | starIntensity 30 | :231 | ✓ |
| **star ray = min(round(radius·1.5), fr.max)** | .NET `Math.Round` = round-half-to-even | `int(round(radius*1.5))` :361 — **banker's** | — | `Math.round(radius*1.5)` :232 — **half-up** | **✗ diverge at exact .5**: fire radius 3.0 → 4.5 → Python/.NET 4, JS 5. Radius 3.0 is the clamp floor of both 80 mm mortar warheads (fr [3,8]) and reachable on M15/M40/base (fr floor 1, Σ q·r = 3). Downstream: `reach` feeds fire-area, denial ranking, gallery "out of reach" at 5 tiles, catalogue Fire column. |
| peak damage = damagePerIntensity·P/5 = 2P | ✓ (:79-80) | `blast_damage_at(…,0)` :466-481; dpi parsed from explosion proto :388-403 | damagePerIntensity 10 | :640, :2157-2163 | ✓ |
| explosion coefficient 2 / 1.1^(armor/5) | ✓ | :459-464 → baked `coefficient` | armorBase/armorStep (unread by JS) | uses baked `target.coefficient` | ✓ |
| flame colour = Σ max(q,1)·burncolormod·rgb | (upstream ExecuteExplosion) | **not modelled** | color/colorWeight | :205-211, :238 | ⚠ JS-only; `--verify` cannot check it |
| fire: contact + min(secIn,D)·(rate(stacks)+I·tileHeat/3) + Σ_after rate(stacks−0.25·s); stacks = min(D, fire.maxStacks, 45) | doc O13 | `fire_damage` :870-893 | fireBurn*/fireStackDecay/fireMaxStacks | `fireOutcome` :2125-2155 | ✓ maths identical; **branch differs**: Python ignores `fire.bypass` for fireImmune/igniteImmune (:873-877), JS honours it (:2136-2138). Latent — no fire has bypass=true today. |
| HE = dead/crit at distances (2,3), tier ≥ 2, burn window closed | doc O15 | `he_effect` :896-916 | heDistances/heMinTier | `heEffect` :2208-2223 | ✓ |
| Kills column | — | `count_kills` default **closed** window (:919-940), used for ranking in build_recipes :1033 | — | `killCount` → `targetOutcome(st,t,0)` → `fireOutcome` with the **user window** (default 2 s/3 s) :2225-2228 | ⚠ the Kills shown next to a baked recipe are not the kills it was ranked by (open vs closed burn window). |
| `formula.costBase` | — | conf `cost_base` → JSON | `RMCPhoron` | literal `'RMCPhoron'` :328 | ⚠ duplicated constant |

`price()` (ss14_map_extractor.py:453-483) vs maps.js display: price is per spawned entity (full stack, `unit_count`), materials×count then StackPrice×count XOR StaticPrice — maps.js multiplies by the entity count `q` (floor 1 / container `amount` / vendor `count`, maps.js:286-294) ✓. Position `extra` (ss14_map_extractor.py:805-808): `prob` if < 1 else `amount` if > 1 → maps.js:288-292 treats `<1` as probability and `≥1` as amount ✓. Limitation: a container slot with **both** prob < 1 and amount > 1 keeps only prob (amount lost) → expected count under-reports by ×amount. prices v3 trailing `-1` trimming (:840-841) ↔ maps.js:257-259 ✓ (verified on 5,996 vanilla entries).

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 6.1 | Core explosion formula (3 copies) | F | HIGH | — | code_verified | ss14_ordnance.py:313-386 ↔ ordnance.js:188-249 ↔ doc :61-77 | 12 constants/branches identical; hidden modifiers baked once. | `--verify` 16/17 reference mixtures (CHANGELOG:25). | — | — |
| 6.2 | Star-ray rounding | P | HIGH (divergence) / MEDIUM (which side matches upstream) | P2 | code_verified (final_counts.py: 6 casings hit at radius 3.0) | ordnance.js:232 vs ss14_ordnance.py:361 | JS rounds half-up, Python rounds half-to-even (as .NET does by default). | All non-.5 cases agree. | Mortar warheads at the fire-radius floor: UI says reach 5, catalogue/upstream 4 → fire area, denial role, gallery hit/miss at 5 tiles all off by one tile for the most common mortar star mixtures. Guard that should catch it: `--verify` only replays 17 mixtures through Python, never through JS. | Use banker's rounding in JS (`r = x*1.5; f = Math.floor(r); (r-f===0.5) ? (f%2?f+1:f) : Math.round(r)`) and add a JS-side replay of `ordnance_reference.py` (node script in CI). |
| 6.3 | Fire immunity `bypass` branch | P | HIGH | P3 | code_verified | ss14_ordnance.py:873-877 vs ordnance.js:2136-2138 | Python ignores `fire.bypass`. | No bypass fire exists in data. | Silent divergence the day a bypass fire appears (it is already parsed: `bypass` key in fires). | Mirror the JS condition in Python. |
| 6.4 | Kills column vs recipe ranking window | P | HIGH | P3 | code_verified | ss14_ordnance.py:919-940,1033 vs ordnance.js:2225-2228 | Ranking uses closed burn window; display uses user window. | Consistent inside the UI. | The number a user reads is not the number the row was chosen by. | Show Kills with (0,0) in the catalogue or state the window in the column header. |
| 6.5 | Flame colour only in JS | P | HIGH | P3 | code_analysis_only | ordnance.js:205-211 | Third copy lacks the branch. | — | Invariant 7 ("three copies agree") cannot be tested for colour. | Add colour to compute_stats + a reference colour. |
| 6.6 | price()/positions ↔ maps.js | F | HIGH | — | code_verified | ss14_map_extractor.py:453-483,805-808,840-841 ↔ maps.js:254-303 | Per-entity full-stack price × entity count; prob/amount encoding round-trips; v3 trimming handled. | — | prob∧amount>1 slots lose `amount` (P3 data-model limit). | Encode `[prob, amount]` when both present. |

---

## 7. Config ↔ extractor

FORK_REGISTRY: 21 forks, **27 distinct keys**. Consumer of each (grep counts, `'key'` literal): name (chem 22 / map 18 / ord 5), repo (chem 2 + derives raw_url in config), branch (config derivation + audit script), custom_dir (chem 1), color (chem 2, ord 4), reagent_files (chem 4, ord 1), reaction_files (4), locale_files (4), raw_url (7/7/2), vanilla_override_{reaction,reagent}_files (1 each + audit scripts), seed_files (7), parent_fork (7 + 2 scripts), locale_files_ru (1), dispenser_chemicals (consumed inside config.py:1595-1600 → BASE_DISPENSER_CHEMICALS), item_fill_files/vending_inventory_files/vending_machine_files (2 each), blocked_reactions (4), blocked_categories (2), modified_reactions (3), parent_override_{reaction,reagent}_files (1 each), produce_files (2), botany_locale_files (1), dispenser_files (3), ordnance (chem 1, ord 5; nested keys reagent_files/casing_files/explosion_files/fire_files/default_fire/target_files/targets/target_sprite_states/explosion_proto/iron_reagent all read; `cost_base` read with default, never set).

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 7.1 | Registry keys ↔ reads | F | HIGH | — | code_verified | config.py:62-1584 ↔ extractors | 27/27 set keys are consumed; 0 unknown keys. | — | — | — |
| 7.2 | Keys read but never set | P | HIGH | P3 | code_verified | ss14_chem_extractor.py:3050 (`blocked_reagents`); ss14_ordnance.py:1265 (`cost_base`) | Manual channels with defaults. | Harmless. | Undocumented in README "Adding a New Fork". | Document or remove. |
| 7.3 | Key-name validation | M | HIGH | P3 | code_verified | config.py / extractors (no KNOWN_KEYS) | A typo'd manifest key (`reagent_fies`) is silently ignored → fork ships with missing content and no warning. | `.get()` defaults keep the build green. | Exactly the "silent failure" class the repo keeps hitting. | `assert set(fconf) <= KNOWN_KEYS` at import in config.py. |
| 7.4 | SPRITE_MANIFEST ↔ sprites/ | F | HIGH | — | code_verified | config.py:2349-2367 ↔ sprites/ (15 files) + sprites/xenos (119 = 17 targets × 7 frames) | 15/15 manifest ids present, no orphans; all deliveryMechanisms/syndicateItems `sprite` refs resolve. | — | — | — |
| 7.5 | CATEGORY_SHEET_MAP | F | HIGH | — | code_verified | config.py:2368 ↔ ss14_chem_extractor.py:1328 | Still the YAML-filename→category map (not an Excel leftover; only the name is). | — | Misleading name. | Rename (5.4). |
| 7.6 | MAP_BLOCKLIST ↔ maps on disk | F | HIGH | — | code_verified | config.py MAP_BLOCKLIST ↔ maps/vanilla, maps/misfits | None of the 4 blocklisted maps exist on disk; prune logic (ss14_map_extractor.py:1024-1031) holds. | — | — | — |
| 7.7 | locale_files_ru forks ↔ nameRu | F | HIGH | — | code_verified | config (7 forks) ↔ data.json (16 source forks carry nameRu, 755/1387) | RU-first forks 100 % of what upstream translates (adt 100/100, sunrise 37/37, fish 10/10, corvax 8/8, stories_cm 18/18, rmc14 112/113, cmu 20/22, deadspace 20/22); spill-over to 9 additive forks via shared ids is by design (first-wins ownership, corvax merges last). | — | — | — |
| 7.8 | `ordnance` manifest ↔ ordnance/ | F | HIGH | — | code_verified | config (stories_cm) ↔ ordnance/stories_cm.json | 1 ↔ 1. | — | — | — |
| 7.9 | Duplicated `fetch_file` | P | HIGH | P3 | code_verified | ss14_map_extractor.py:36-50 ("copied from ss14_chem_extractor.py:191, keep in sync manually") vs ss14_chem_extractor.py:194 | Hand-copied fetcher; the referenced line already drifted (191 → 194). | Both work. | Retry/backoff fixes must be applied twice. | Import it (ss14_ordnance.py already does: `from ss14_chem_extractor import fetch_all_files`). |

---

## 8. Dead code

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 8.1 | JS functions never referenced | F | HIGH | — | code_verified | app.js 131, ordnance.js 113, maps.js 27, tutorial.js 19, i18n.js 7 functions/arrow-consts | 0 unreferenced (the one hit, `$` in ordnance.js:74, is a regex artefact — it is used 60+ times). `openDetail`/`goBackDetail`/`loadPresetIntoBatch` are reached only via inline `onclick=` (must stay global). | — | — | — |
| 8.2 | CSS class selectors | F | HIGH | — | code_verified | style.css: 488 class selectors; 40 without a literal match, 38 of them template-composed (`badge-tier-${}`, `badge-stealth-${}`, `coll-${}`, `effect-${}`, `effect-row--${}`, `effect-category__head--${}`, `ord-xeno-${}`, `preset-tier-${}`), 1 CDN false positive (`googleapis`) | **1 truly dead: `.badge-modified` (style.css:875)** → dead ratio 0.2 %; random sample of 60: 0 dead after exclusions; 18 "dead ids" are hex colours (0 real). T1 cleanup (ROADMAP rev 1.3) held. | — | — | Delete `.badge-modified`. |
| 8.3 | Tutorial targets | F | HIGH | — | code_verified | tutorial.js:29,41,52,68,87,95,101 ↔ index.html | 7/7 targets exist (`.batch-section` resolves to the first = Batch/Shift Planner). | — | — | — |
| 8.4 | i18n leftovers of removed tabs | P | HIGH | P3 | code_verified | i18n.js (10 T keys + 3 RX rules, §3b) | Reactions/Stats/Pareto strings remain. | — | — | Delete. |
| 8.5 | sw.js precache of removed files | F | HIGH | — | code_verified | sw.js:11-21 | No `libs/`/vis-network entries remain. | — | — | — |
| 8.6 | Scripts referencing removed features | F | HIGH | — | code_verified | scripts/export_metrika_stats.py:50 (`from create_metrika_goals import GOALS`) | Single source of truth for goals; audit scripts read live config keys. | — | — | — |
| 8.7 | EFFECT_COLLECTIONS ↔ design doc | F | HIGH | — | code_verified | app.js:2184-2213 ↔ docs/design/2026-07-12-effect-collections.md:21-76,80-92; ss14_chem_extractor.py:1776-1785 | 5 collections, same buff/debuff/other sets, `cure`→healing, fallback→other, collapsed by default; all 7 antag weights present. Only the struct field `cls` from the doc is replaced by `coll-${id}`. | — | — | — |
| 8.8 | Orphan repo files | P | HIGH | P3 | code_verified | logo*.svg ×4 (§1.5), `logo (1).png`, logo-steam.png (untracked) | Unreferenced. | — | — | Delete / move to promo/. |

---

## 9. Environment parity (localhost preview ↔ GitHub Pages; Windows ↔ CI Linux; Python versions)

| # | Feature | Class | Conf | Prio | Evidence | File:Line | Description | What works | What fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 9.1 | Relative fetch/asset paths | F | HIGH | — | code_verified | app.js:112,126; maps.js:35,80-81,250; ordnance.js:319; index.html:75,758-762 | All relative → identical under `/` and `/space-station-recipes/`. | — | — | — |
| 9.2 | Absolute URLs | F | HIGH | — | code_verified | index.html:17,21,25,33,67; robots.txt:7; sitemap.xml:4 | Prod-absolute where SEO requires it. | — | — | — |
| 9.3 | manifest.json start_url/scope | P | HIGH | P3 | code_verified | manifest.json:5-6 (`/space-station-recipes/`) | On localhost the manifest scope does not contain the page → PWA installability/scope warnings in DevTools; prod correct. | Prod install works. | Local PWA testing (B2 DoD "Lighthouse PWA") cannot pass on the preview server. | Use relative `"./"` for both (Pages resolves against the manifest URL). |
| 9.4 | SW registration gate | F | HIGH | — | code_verified | app.js:124-126 | `https:` or hostname `localhost` — preview uses `localhost:64306` ✓ (a `127.0.0.1` URL would skip the SW silently). | — | — | — |
| 9.5 | Fonts CDN vs offline claim | P | HIGH | P3 | code_verified | style.css:13 (`@import fonts.googleapis.com`), sw.js:46 (same-origin only), index.html:20 ("works offline") | Fonts are never cached by the SW; offline renders fallback fonts. NOTICES:27-31 discloses the CDN. | App logic offline ✓. | Visual regression offline; one external request per load. | Self-host the three fonts (they are OFL) and add to PRECACHE. |
| 9.6 | Python syntax vs CI 3.11 | F | HIGH | — | code_verified | contract_check.py `ast.parse(feature_version=(3,11))` on 12 files; local 3.14.3 | All parse; no 3.12-only syntax. | check-links.yml (3.11) and the deploy guard (ubuntu python3) are safe. | — | — |
| 9.7 | Windows path / console encoding | P | HIGH | P3 | code_verified | ss14_chem_extractor.py:40-41,234 (reconfigure + `os.sep`); scripts/audit_fork_manifests.py:60; **ss14_map_extractor.py (3 non-ASCII `print()` lines, no reconfigure)**; ss14_ordnance.py (no reconfigure) | Chem extractor and manifest audit set UTF-8 stdout; map/ordnance extractors do not (cp866 em-dash trap from project memory). | Runs on UTF-8 consoles. | `UnicodeEncodeError` on a default Windows console when the map extractor prints a fork label or an em dash. | Copy the 2-line reconfigure guard into both entry points. |

---

## 10. Memory-file claims (verify each) + convention guards

| # | Claim | Class | Conf | Prio | Evidence | File:Line | Verdict |
|---|---|---|---|---|---|---|---|
| 10.1 | `.maps-list-panel[hidden]{display:none}` rule exists | F | HIGH | — | code_verified | style.css:2864 (`.maps-list-panel[hidden], .maps-list-filters[hidden] { display: none; }`) | True. |
| 10.2 | `if (out !== raw)` guard in i18n.js | F | HIGH | — | code_verified | i18n.js:681 | True (observer-loop guard present). |
| 10.3 | `forks_meta` lists a fork when count>0 or parent_fork | F | HIGH | — | code_verified | ss14_chem_extractor.py:2300 `if fid == "vanilla" or count > 0 or fconf.get("parent_fork")` | True (+ vanilla). |
| 10.4 | Ordnance `--verify` Phase 10 in the chem extractor | F | HIGH | — | code_verified | ss14_chem_extractor.py:3144-3156; standalone ss14_ordnance.py:1328-1340 | True. |
| 10.5 | Deploy guard step | F | HIGH | — | code_verified | deploy.yml:41-62 | True — scope limited to index.html (1.3). |
| 10.6 | `decodeURLState` tab whitelist with fallback to reagents | F | HIGH | — | code_verified | app.js:3073-3077 | True; fallback is implicit (no click → default tab). Note the sibling `validSort` whitelist (app.js:3110) lacks `steps-asc` while `encodeURLState` (:3044) writes it → shared "Fewest Steps" links silently revert to name order (P3). |
| 10.7 | Convention 5 "[hidden] loses to author display rules" applied everywhere | **B** | HIGH | **P1** | code_verified (CSS cascade; not runtime-verified) | style.css:3107 `.ord-fork-note { display: flex; … border-left: 3px solid var(--amber); padding: 7px 12px }` vs ordnance.js:2288 `note.hidden = true` / :2290; index.html:489 | The override exists for `.disclaimer-panel` (:2414), `.maps-list-panel/.maps-list-filters` (:2862-2864, with a comment naming the trap) and `.ord-field/.ord-pair` (:3000-3003, same comment) — but **not** for `.ord-fork-note`. Author `display:flex` beats the UA `[hidden]{display:none}` rule, so the fork note can never be hidden: on stories_cm an empty amber-bordered bar sits above the toolbar; after switching fork and back, the stale "These numbers come from … Switch the app to it" note stays visible. This is the replacement for the removed tab-hiding (5.10), so the fork-scoping UX of the flagship tab is defective. Guard that should have caught it: the convention is documented in MANIFEST and twice in style.css, but nothing checks new `hidden` toggles against CSS. |
| 10.8 | same, mask-mode chips | **B** | HIGH | **P2** | code_verified (CSS cascade) | style.css:3154 `.ord-mask-modes { display: flex }` vs ordnance.js:1219 `$('ordMaskMode').hidden = !any`; index.html:657-658 (`hidden`) | Added in the last commit (e777079). In flat view with zero masks the three chips "Masks off / Overlay masks / Intersection only" render although O10 (ROADMAP:260, design doc :273) promises the block collapses to the "+ Add mask" button; in 3D view the parent `.ord-mask-block` (no display rule) hides correctly, which is why it was not noticed. Fix both with one rule: `.ord-fork-note[hidden], .ord-mask-modes[hidden] { display: none !important; }` and add a scripted check (`grep "\.hidden\s*=" *.js` → for each id assert a `[hidden]` rule or no `display` rule) to the deploy guard. |
| 10.9 | RULES/MANIFEST: `--selfcheck` and `--verify` "only write under cache/" | **B** (as a guard statement) | HIGH | P2 | code_verified + `git status` | ss14_map_extractor.py:960,969 → bake_png :752 / build_map_json :818 write `maps/vanilla/Bagel.{png,json}`; ss14_ordnance.py:1328-1340 runs `build_all` before `verify` → `write()` :1305-1310 rewrites `ordnance/<fork>.json` and `fetch_target_sprites` :595-660 writes `sprites/xenos/` | False. During this audit `git status` shows `maps/vanilla/Bagel.json` and `Bagel.png` modified (03:21:47) and `ordnance/stories_cm.json` rewritten byte-identically (03:21:15) — a sibling agent's "read-only" run. Bagel HEAD→worktree: 2,098 → 2,088 items, 1,236 item records differ, vias 154 → 152, PNG 11,849 → 12,367 B, bounds identical. Two disconnects: (a) the audit rules and the MANIFEST invariant list treat these commands as safe — a developer who runs selfcheck then `git add -A` ships a one-map partial re-bake (Bagel from today's cache, 13 other vanilla maps from 07-13), breaking the "regen byte-identical" invariant (#6) for the map set; (b) the committed vanilla Bagel is stale versus the current cache/upstream (ROADMAP rev 2.0 noted "дрейф ванильных карт"). **Do not revert from this audit** (read-only); the orchestrator should `git checkout -- maps/vanilla/Bagel.json maps/vanilla/Bagel.png` before any commit and decide on a full vanilla re-bake. Fix: point `selfcheck()` at a temp OUT_DIR (`cache/selfcheck/`) and make `--verify` compare against the on-disk JSON instead of rewriting it. |

---

## Files examined

Read (full or by line range): index.html, app.js, ordnance.js, i18n.js, maps.js, tutorial.js, sw.js, manifest.json, robots.txt, sitemap.xml, .gitignore, .claude/launch.json, README.md, CHANGELOG.md, ROADMAP.md, NOTICES, .github/workflows/deploy.yml, .github/workflows/check-links.yml, .github/ISSUE_TEMPLATE/attribution.yml, .github/ISSUE_TEMPLATE/strategy-inaccuracy.yml, docs/decisions/2026-07-12_rmc-renamed-reagents.md, docs/decisions/2026-07-26_russian-localization.md, docs/design/2026-07-12-effect-collections.md, docs/design/2026-07-12-item-fill-sources.md, docs/design/2026-07-12-map-item-finder.md, docs/design/2026-07-27-sell-list-mode.md, docs/design/2026-09-10-ordnance-calculator.md, research/site-analytics-2026-07/handoff-analytics-goals.md, research/site-analytics-2026-07-28/SUMMARY.md (grep), research/site-analytics-2026-07-28/raw/goals_registry.json, promo/steam-guide-ru.md, scripts/create_metrika_goals.py, scripts/export_metrika_stats.py (grep), sources.py (head + grep), config.py (imported; lines 17-62, 1584-1600, 2349-2402 via `dir()`/grep), ss14_ordnance.py (41-145, 298-405, 451-520, 685-770, 870-940, 1174-1342), ss14_map_extractor.py (30-50, 440-520, 727-860, 862-990, 987-1047 + grep), ss14_chem_extractor.py (2288-2320, 3040-3060, 3144-3156 + grep), ordnance_reference.py (grep), style.css (grep + 2826-2832, 2941-2947, 3033-3082, 3105-3160, [hidden] rules), data.json, ordnance/stories_cm.json, maps/index.json, maps/vanilla/Box.json, maps/vanilla/Bagel.json (HEAD and worktree), maps/*/prices.json (15), sprites/ (listing).
Ran (read-only): `git status --short`, `git diff --stat`, `git show HEAD:maps/vanilla/Bagel.json`, `git log -S…`, `git ls-files`; scratch scripts `i18n_check.py`, `contract_check.py`, `final_counts.py` (scratch dir), plus inline `python -` analyses (data key union, CSS selector census, function-reference census, hidden-attribute census, star-ray rounding sweep). No extractor was executed by this agent; the Bagel/stories_cm rewrites observed in `git status` were made by another process during the audit window.

## Summary counts

Rows: **85** (§1 6 · §2 9 · §3 6 · §4 5 · §5 20 · §6 6 · §7 9 · §8 8 · §9 7 · §10 9) — FUNCTIONAL 37 · PARTIAL 42 · BROKEN 3 · MISSING 2 · UNCERTAIN 1.
Priorities (non-FUNCTIONAL, 48): **P1 1** (10.7) · **P2 8** (1.3, 2.2, 2.5, 4.2, 5.17, 6.2, 10.8, 10.9) · **P3 39** · P0 0.

## Top findings

1. **P1 BROKEN — `.ord-fork-note{display:flex}` (style.css:3107) defeats `note.hidden` (ordnance.js:2288):** the Ordnance fork note never hides — empty amber bar on stories_cm, stale "Switch the app to it" after fork switching; the convention is documented twice in the same file (:2862, :3000) and missed on the newest element.
2. **P2 BROKEN — `.ord-mask-modes{display:flex}` (style.css:3154) defeats `$('ordMaskMode').hidden` (ordnance.js:1219, commit e777079):** mask-mode chips show with zero masks in flat view, contradicting O10.
3. **P2 PARTIAL — Star fire reach rounding diverges (ordnance.js:232 `Math.round` vs ss14_ordnance.py:361 banker's `round`, matching .NET):** at fire radius 3.0 — the clamp floor of both 80 mm mortar warheads — UI says 5 tiles, catalogue/upstream 4; `--verify` never exercises the JS copy.
4. **P2 PARTIAL — 433 KB (11.3 %) of data.json is never read; `edges` (295 KB) lost its only consumer in A1.1 yet `init()` still refuses to start without it (app.js:150)** — and `meta.schemaVersion` is unread while the SW serves stale data.json first, so the extractor cannot drop it safely.
5. **P2 BROKEN guard — "read-only" `--selfcheck`/`--verify` rewrite tracked artifacts** (maps/vanilla/Bagel.{json,png}: 1,236 item records now differ from HEAD; ordnance/stories_cm.json, sprites/xenos) — RULES/MANIFEST invariants wrong; partial one-map re-bakes can be committed by accident.
6. **P2 PARTIAL — CI asset guard (deploy.yml:44-46) checks index.html `src|href` only: 0 of 8 runtime-fetched paths (data.json, maps/, ordnance/, sprites/xenos, sw.js) are verified** — the next whitelist regression is invisible to it.
7. **P2 PARTIAL — NOTICES attributes 8 of 21 MIT-licensed forks (NOTICES:13-24)**, including none of the forks whose xeno sprites now ship in `sprites/xenos/`; plus **P2 UNCERTAIN** whether the 14 `ordnance_*` Metrika goals added on 09-10/11 were pushed to the counter (create_metrika_goals.py is owner-run; the 16-day Maps blind spot precedent).
