# T3 — SRE / Infrastructure, Deploy, PWA, Observability, Legal-Privacy
Audit date 2026-09-11 · repo HEAD e777079 · prod verified byte-identical to HEAD (`cmp` on index.html, sw.js, manifest.json — all IDENTICAL)

**Framing:** paged at 3 AM. Production is a static PWA on GitHub Pages, deployed on every push to main, plus two Actions workflows and a hand-run Python pipeline. Growth lens applied to the recommendations: acquisition is Steam guides, SEO ≈ 0, mobile = 29% of users.

> **Audit-integrity note (for the orchestrator, not a finding).** At 03:21:47 local, *mid-audit*, another concurrent session regenerated two **tracked** files: `maps/vanilla/Bagel.json` and `maps/vanilla/Bagel.png` (PNG now 12,367 bytes vs 11,849 in the index and on prod — a fresh bake, not a download). `cache/dead_reactions_audit.json` and `cache/fork_manifest_audit.json` were also written (the permitted read-only audit scripts), and 4 `python.exe` processes are live. **This was not me** — my session ran only reads, `git` read-only commands, `curl` to `/dev/null` and `/tmp/`, and one scratchpad script that opens files read-only; I invoked no extractor. I have **not** reverted it (RULES.md forbids state-changing git). Consequences: the working tree is dirty against HEAD, so `maps/vanilla/Bagel.*` no longer matches prod, and any other agent's map-related evidence gathered after 03:21 may reflect a re-bake rather than the shipped artifact. Worth confirming with the owner before anyone commits.

**Headline:** nothing is on fire right now — prod == HEAD, all runtime assets 200, deploys take 24s, gzip is on. But essentially none of it is *observed*. On 2026-09-10, seven consecutive production deploys over 93 minutes shipped an index.html requesting a 404'ing script, which also made the service worker's `addAll` reject, so **the PWA install failed for every visitor for 93 minutes and no signal of any kind existed** — not a failed build, not an alert, not a Metrika goal. The build guard added afterwards would still miss the same class of failure for `data.json`, `sw.js`, `maps/`, `ordnance/` and `sprites/`.

---

## 1. Deploy pipeline (`.github/workflows/deploy.yml`)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 1.1 | Whitelist completeness vs runtime `fetch()` | FUNCTIONAL | HIGH | — | code_verified | deploy.yml:29-35; app.js:112, 2938, 2958; maps.js:35,80,81,250; ordnance.js:319 | I enumerated every runtime-fetched path and resolved each against the cp list. All five roots (`data.json`, `sprites/`, `maps/`, `ordnance/`, plus img `src`) are covered today. | Complete as of e777079. | — | — |
| 1.2 | "Verify every referenced asset" guard — blind spots | PARTIAL | HIGH | P1 | code_verified | deploy.yml:41-62 | The guard regexes `src=`/`href=` out of **index.html only** and skips anything matching `https?://`. I simulated it: it checks exactly 10 paths (app.js, i18n.js, maps.js, ordnance.js, tutorial.js, style.css, manifest.json, 3 favicons). | Catches incident #2 (i18n.js) and #3 (ordnance.js) and the manifest half of #1. | Does **not** check: `data.json` (total outage if dropped — app.js:112 is the only reference), `sw.js` (the other half of incident #1, 2c062f6 — registered from JS at app.js:126, never in index.html `src`), `sprites/`, `maps/`, `ordnance/`, `og-image.png` (absolute URL → skipped by the `https?://` filter), and every `sw.js` PRECACHE entry. A repeat of the sw.js incident passes the new guard green. | Extend the guard to (a) scan JS for `fetch('…')` / `src='…'` literals, (b) validate every `sw.js` PRECACHE entry against `_site`. See §9 `preflight.py`. |
| 1.3 | Actions pinned by major tag only | PARTIAL | HIGH | P2 | http_fetch_only | deploy.yml:24,64,65,68; check-links.yml:29,32,45,82 | All 8 tags resolve (api.github.com `git/ref/tags`): checkout@v6→`d23441a`, setup-python@v5→`a26af69`, git-auto-commit@v5→`b863ae1`, create-issue-from-file@v5→`e8ef132`, configure-pages@v6→`45bfe01`, upload-pages-artifact@v5→`fc324d3`, deploy-pages@v5→`368f825`, checkout@v4→`11d5960`. | Every tag exists; none deprecated. | A mutable major tag means a compromised upstream action is auto-adopted on the next run. `deploy-pages` holds `id-token: write` — a compromised version could mint an OIDC token. Two third-party (non-`actions/`) publishers are in the trust set. | SHA-pin the two third-party actions at minimum (`stefanzweifel/git-auto-commit-action@b863ae1…`, `peter-evans/create-issue-from-file@e8ef132…`) with the tag in a trailing comment; enable Dependabot for `github-actions`. `actions/*` major tags are a defensible risk to keep. |
| 1.4 | `permissions` scope | FUNCTIONAL | HIGH | — | code_verified | deploy.yml:8-11 | `contents: read`, `pages: write`, `id-token: write` — exactly the Pages deployment minimum. | Least privilege, correct. | — | — |
| 1.5 | `concurrency` | FUNCTIONAL | HIGH | — | code_verified + http_fetch_only | deploy.yml:13-15 | `group: pages`, `cancel-in-progress: false` — correct for Pages (cancelling mid-deploy can leave a partial site). Runs 66-85 (20 deploys, 2026-09-10 17:18→23:43 UTC) all succeeded at 20-27s, no queue contention. | — | — | — |
| 1.6 | No staging — every push is direct-to-prod | PARTIAL | HIGH | P2 | http_fetch_only | deploy.yml:3-5 | 85 total deploy runs; **20 production deploys on 2026-09-10 alone**, the tightest gap ~5 min (runs 82→83 at 22:56→23:01). | Fast feedback; 24s deploys make rollback-by-revert cheap. | At that cadence the only test environment *is* production. The ordnance incident (1.7/4.7) is the direct consequence. | Don't add a staging environment (overkill for 15 visits/day). Instead make the *build* the gate — §9 preflight — and add a post-deploy smoke curl (§5.2). |
| 1.7 | No test / lint / HTML-validation / Lighthouse step | MISSING | HIGH | P2 | code_verified | deploy.yml:23-68 | The entire pipeline is: checkout → cp → asset-existence guard → upload → deploy. No repo-wide test dir exists either. | The asset guard is a real gate and blocks the build on miss. | No syntax check on 3,346-line app.js / 2,303-line ordnance.js — a JS parse error ships and the page dies with a blank screen and no build failure. | Add `node --check app.js maps.js ordnance.js i18n.js tutorial.js sw.js` (2 lines, ~3s, zero deps) as the cheapest possible correctness gate. |
| 1.8 | Custom `404.html` | MISSING | HIGH | P2 | http_fetch_only + code_verified | repo root (`ls 404.html` → absent) | `GET /space-station-recipes/definitely-not-a-real-path-xyz` → **404, 5254 bytes, GitHub's default "There isn't a GitHub Pages site here" page**, which links to github.com, not to the app. | Correct 404 status code. | Every mistyped/stale Steam-guide deep link dead-ends on a GitHub-branded page with no route back into the product. Pure acquisition leak on the one channel that actually works. | Add a `404.html` (also to deploy.yml:30 cp list) that reads the path, shows the ChemDB shell, and links back to `/`. ~30 lines. Highest-ROI item in this report. |
| 1.9 | `CNAME` / custom domain | MISSING | HIGH | P3 | code_verified | repo root (absent) | No custom domain; canonical is the github.io path. | Consistent with manifest scope and canonical tag. | Ties the PWA's identity (scope, SW registration, installed-app origin) to `mikameo.github.io`. Moving later resets every install and every cached SW. | Acceptable. If a domain is ever likely, buy it before the install base grows. |
| 1.10 | Deploy duration | FUNCTIONAL | HIGH | — | http_fetch_only | api.github.com deploy.yml runs 66-85 | 20-27s wall clock, consistently. | — | — | — |
| 1.11 | `og-image.png` copied conditionally | PARTIAL | HIGH | P3 | code_verified | deploy.yml:35 | `[ -f og-image.png ] && cp og-image.png _site/ \|\| true` — a deliberate silent skip. | File exists; prod serves it 200, 79,632 bytes. | If it were ever deleted, the build stays green and every social/Steam link preview breaks silently. The guard can't catch it (absolute URL at index.html:21). | Drop the conditional — make it a plain `cp` so a missing file fails the build. One-character fix. |

---

## 2. Link-rot workflow (`.github/workflows/check-links.yml` + `scripts/check_sources.py`)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 2.1 | Auto-commit of `cache/link_health.json` | BROKEN | HIGH | P2 | code_verified | check-links.yml:44-48; .gitignore:2 | `git check-ignore -v cache/link_health.json` → `.gitignore:2:cache/`. `git-auto-commit-action` detects changes with `git status -s -- <file_pattern>`, which returns **nothing** for an ignored path, so the action reports "working tree clean" and exits 0. `git log --all --grep=link_health` returns only the commit that *created* the workflow — **no auto-commit has ever landed in 32 runs**. | Never errors; never breaks the build. | A step that has silently done nothing for 5 months. There is no history of link health anywhere — the only copy of `link_health.json` is the local one (last written 2026-07-12, 23 URLs, 0 broken) and the ephemeral runner copy. | Either delete the step (and then drop `contents: write`, §2.8), or commit to a tracked path like `docs/link_health.json` and remove it from the ignore rule. Deleting is the honest choice. |
| 2.2 | Issue-body build step | FUNCTIONAL | HIGH | — | code_verified | check-links.yml:50-78; check_sources.py:130 | Reads `cache/link_health.json` from the runner workspace, which `check_sources.py` wrote earlier in the same job. Unaffected by 2.1. Issues #3-#6 prove it produced real bodies. | Works. | — | — |
| 2.3 | Issue dedupe / auto-close on recovery | MISSING | HIGH | P2 | http_fetch_only | check-links.yml:80-86 | `peter-evans/create-issue-from-file@v5` is called with no `issue-number`, so every failing run files a **new** issue. api.github.com shows #3 (2026-06-08), #4 (06-15), #5 (06-29), #6 (07-06) — four identical titles, 0 comments each, all still open, and the condition cleared by 2026-07-12. | Alerting fired when the condition was real. | Four duplicate open issues describing a resolved condition. Classic alert fatigue: the next genuine link-rot issue is indistinguishable from the stale pile. | Pass `issue-number` from a `find-issue` lookup (or the action's `update-existing` flow) to reuse one issue, and add a closing step guarded on `exit_code == 0`. ~10 lines. |
| 2.4 | The three forum entries are checked against the **forum root** | BROKEN | HIGH | P1 | code_verified + http_fetch_only | sources.py:261, 270, 279 | All three "forum-post" sources — the ones issue #6 flagged — have `"url": "https://forum.spacestation14.com/"`. `git log -S'forum.spacestation14.com/t/' -- sources.py` returns **nothing**: the deep links never existed; this is original from be7421e (2026-04-19), not a post-incident edit. `curl -I` on that root with the checker's own UA returns **200** today. | The check runs and reports three "healthy" sources. | Vacuous by construction. HEADing a forum homepage three times proves the forum exists, not that `gobbygobbler`'s thread, `Testicular_Man`'s post or `Steelclaw`'s question still do. The UI surfaces these as citations for the antag-strategy claims they underpin. Compounding: `check_url` follows redirects and accepts any 2xx/3xx (check_sources.py:57), so a forum that 302s a deleted thread to its homepage also reads healthy. | Either recover the three thread permalinks and put them in `url` (with `archive_url` Wayback snapshots), or downgrade `type` to `maintainer-knowledge` and drop the URL so the checker stops counting them as verified sources. Honesty is the cheaper fix. |
| 2.5 | Timeouts counted as broken (not "ok") | FUNCTIONAL | HIGH | — | code_verified | check_sources.py:58-63 | `HTTPError` → ok False; `URLError` → ok False; bare `except Exception` → ok False. A read timeout (`TimeoutError` in 3.11) lands in the last branch. Nothing fails open. | Correct fail-closed design. | — | — |
| 2.6 | Why the check turned green — verified, not assumed | PARTIAL | HIGH | P2 | code_verified + http_fetch_only | check_sources.py:137; sources.py (`SOURCES`, 27 entries / 23 with URL) | I tested the dilution hypothesis: denominator is **still 23** (27 sources, 4 maintainer-knowledge without URL), same as the 2026-07-12 snapshot, and `check_sources.py` has **never been modified since 2026-04-19**. So the recovery is genuine — the forum root became reachable from Actions runner IPs — not a threshold artifact. | The green is real, for what it measures. | Nothing was actually fixed; the condition self-healed and the four issues were never triaged. If the runner IPs get Cloudflare-blocked again, the same four-issue pile-up repeats. | Combine with 2.3 (reuse one issue) — the self-healing case then auto-closes. |
| 2.7 | 10% threshold on n=23 | PARTIAL | HIGH | P1 | code_verified | check_sources.py:72, 137 | Arithmetic on the real catalog: 1 broken = 4.3% → no alert. 2 broken = 8.7% → **no alert**. 3 broken = 13.0% → alert. | Fires on a mass outage. | An alert tuned so that up to **two permanently dead sources rot forever in silence**. For a provenance layer whose whole value proposition is citation integrity, "we only tell you when three break at once" is the wrong contract. | Change the default to `--fail-threshold 4` (i.e. any single break alerts), or better, alert on *any* broken URL and rely on 2.3's single-issue reuse to keep the noise down. One-line change at check_sources.py:72. |
| 2.8 | `permissions: contents: write` | PARTIAL | HIGH | P2 | code_verified | check-links.yml:22 | Granted solely for the auto-commit at 2.1, which provably never commits. | — | The workflow holds push-to-main rights it has never used and does not need. It runs a third-party action (`git-auto-commit@v5`, mutable tag) under that permission. | Delete the auto-commit step and downgrade to `contents: read`. Removes the only write-to-main capability in CI. |
| 2.9 | Schedule reliability (60-day inactivity disable) | FUNCTIONAL | MEDIUM | P3 | http_fetch_only | check-links.yml:10-12 | 32 runs; latest 2026-09-07T08:00Z, `schedule`. Repo is highly active (85 deploys, latest 2026-09-10), so the 60-day auto-disable cannot trigger today. Cron drift observed: fires 03:50-09:25Z against a `0 3 * * 1` spec — normal Actions best-effort behaviour. | Running. | If the project goes quiet for 60 days GitHub disables the cron and sends one email. No heartbeat would reveal it. | Low priority given cadence. A "last successful run" line in the README, or a monthly `workflow_dispatch` reminder, is enough. |

---

## 3. GitHub Pages runtime

All rows below: `http_fetch_only`, base `https://mikameo.github.io/space-station-recipes`, fetched 2026-09-11 00:21-00:24Z.

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line / URL | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 3.1 | HSTS | FUNCTIONAL | HIGH | — | http_fetch_only | all paths | `Strict-Transport-Security: max-age=31556952` (1 year) on every response. No `includeSubDomains`/`preload`, which is GitHub's choice and not settable. | Present, correct. | — | — |
| 3.2 | CSP / X-Frame-Options | PARTIAL | HIGH | P2 | http_fetch_only + code_verified | prod headers; index.html:42-88 | No CSP or XFO on any real path (only GitHub's own 404 page carries one). Response headers are genuinely impossible on Pages — **but `<meta http-equiv="Content-Security-Policy">` is not**, and would cover `script-src`/`connect-src`/`img-src` (not `frame-ancestors`). | HTTPS + HSTS; no third-party JS except the Metrika tag. | Residual risk: `mc.yandex.ru/metrika/tag.js` (index.html:83) is a fully-trusted remote script that pulls webvisor bundles at runtime; there are 86 `innerHTML` assignments across app.js/ordnance.js/maps.js fed from generated `data.json`, with `esc()` at app.js:2163 as the only barrier. With no CSP, a compromise of either sink has zero containment and `connect-src` exfiltration is unconstrained. Clickjacking is unmitigated (no `frame-ancestors` possible via meta). | Add a meta CSP: `script-src 'self' 'unsafe-inline' https://mc.yandex.ru; connect-src 'self' https://mc.yandex.ru; img-src 'self' data: https://mc.yandex.ru; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; object-src 'none'; base-uri 'self'`. `'unsafe-inline'` is unavoidable (15 inline `on*=` handlers in app.js, plus the Metrika bootstrap), so this buys `connect-src` + `object-src` + `base-uri`, not XSS immunity. Honest value: moderate. Cost: one line. |
| 3.3 | `Cache-Control: max-age=600` vs the `?v=` scheme | FUNCTIONAL | HIGH | — | http_fetch_only + code_verified | prod headers; index.html:75,758-762; sw.js:11-21 | Every asset — index.html, data.json, sw.js, JSON, PNG — gets a flat `max-age=600`, unconfigurable on Pages. | Worth stating precisely: because *everything* revalidates every 10 min anyway, the `?v=N` scheme buys nothing at the HTTP layer. Its real and necessary job is busting the **service-worker precache** and the in-session memory cache. That's why the discipline in ROADMAP §0.2 matters even though the CDN would eventually catch up. | No immutable long-cache is possible, so repeat visits always pay a revalidation RTT for ~10 assets. | Nothing to fix on Pages. Do not remove `?v=` — it is load-bearing for the SW, not for the CDN. |
| 3.4 | gzip on `data.json` | FUNCTIONAL | HIGH | — | http_fetch_only | `GET /data.json` | With `Accept-Encoding: gzip, br` → `Content-Encoding: gzip`, `Content-Length: 416,316`. With `Accept-Encoding: identity` → `Content-Length: 3,686,598`. **8.9× compression, automatic, already on.** `Vary: Accept-Encoding` present. | The "compress data.json" idea on any backlog is already done by the platform. | — | — |
| 3.5 | Brotli | PARTIAL | HIGH | P3 | http_fetch_only | `GET /data.json` with `Accept-Encoding: gzip, br` | Explicitly offered `br`; Pages returned `gzip`. | — | ~15-20% (≈70KB) left on the table for the main payload. Not controllable on Pages. | Nothing to do short of leaving Pages. Note it so nobody spends a sprint "adding compression". |
| 3.6 | Custom/branded 404 | MISSING | HIGH | P2 | http_fetch_only | (see 1.8) | — | — | — | See 1.8. |
| 3.7 | canonical / OG / Twitter tags | FUNCTIONAL | HIGH | — | code_verified | index.html:17-34 | Canonical, full OG set with 1200×630 image + alt + locale, `summary_large_image` Twitter card, og-image 200 on prod. | Complete and correct for the root URL. | — | — |
| 3.8 | `sitemap.xml` — 1 URL | PARTIAL | HIGH | P1 (growth) | code_verified | sitemap.xml:3-8; robots.txt:7 | One `<loc>`, `lastmod` 2026-04-16 (5 months stale while the app deployed 20× in one day). | Valid XML, correctly referenced from robots.txt, 200 on prod. | Nine tabs and every reagent deep-link are invisible to crawlers. With SEO ≈ 0 and Steam guides as the sole acquisition channel, this is the entire organic-growth surface sitting unused. | Generate the sitemap in the pipeline: one entry per tab (`?tab=calculator`, `?tab=medbay`, `?tab=maps`, …) × 2 languages, plus the top ~200 reagent deep-links, with `lastmod` from `data.json` `meta.generated`. ~40 lines in the extractor. Caveat: these are client-rendered, so indexing value is capped until 3.9/10.2 land. |
| 3.9 | `hreflang` for `?lang=ru` | MISSING | HIGH | P1 (growth) | code_verified | index.html:17-28; i18n.js:7 | No `<link rel="alternate" hreflang>` anywhere; `og:locale` is hardcoded `en_US`. | RU mode works at runtime. | **RU is 34% of users** and the Russian surface is entirely uncrawlable and unshareable-as-Russian: a shared `?lang=ru` link previews in English. | Add `<link rel="alternate" hreflang="en" href="…/">`, `hreflang="ru" href="…/?lang=ru">`, `hreflang="x-default"`, and set `og:locale:alternate` `ru_RU`. Three lines, and the single cheapest thing that addresses a third of the audience. |
| 3.10 | `<html lang>` in RU mode | PARTIAL | HIGH | P2 | code_verified | index.html:8; i18n.js:26 | `<html lang="en">` static; `i18n.js:26` sets `document.documentElement.lang = lang` at runtime. | Correct for real browsers and screen readers once JS runs. | Any non-JS consumer — crawlers, link-preview bots, Steam's own unfurler — always sees `lang="en"`. Combined with 3.9 the RU half of the product is invisible outside the browser. | Read `?lang=` in an inline head script (before render) and stamp `lang` there, so the served DOM is correct at first paint. ~3 lines at index.html:8. |
| 3.11 | Meta / JSON-LD counts vs actual data | BROKEN | HIGH | P2 | code_verified | index.html:13, 20, 32, 49 vs data.json `meta` | Four places claim **"1369 reagents and 1092 reactions across 20 forks"**. `data.json` actually holds **1387 reagents, 1104 reactions, 21 forks** (schemaVersion 3.10.0, generated 2026-09-10T18:26:36). | Tags are syntactically valid and render. | Every search snippet, Steam link preview and the `schema.org/WebApplication` record understate the product and are provably stale — the one thing a crawler reads about scale is wrong, in four places, in the structured data included. | Have the extractor stamp the three numbers into index.html (or a tiny `scripts/sync_meta.py`) at bake time, and add a preflight assertion (§9) so it can never drift again. |
| 3.12 | Google Fonts via CSS `@import` | PARTIAL | HIGH | P1 | code_verified | style.css:13 | `@import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&family=Chakra+Petch…&family=Oxanium…&display=swap');` — three families, nine weights, as the **first rule of a 3,164-line stylesheet**. | `display=swap` avoids invisible text. | Three compounding problems. (a) **Perf:** a CSS `@import` is the slowest possible font load — the browser must download and parse style.css before it even discovers the Google request, serialising two extra round trips ahead of first paint. On mobile (29% of users) this is the render-blocking critical path. (b) **Offline:** the SW precaches style.css but cannot cache a cross-origin `@import`, so the installed PWA falls back to system fonts offline — the offline promise is visually broken. (c) **Privacy:** every visitor's IP goes to Google on every load; this is disclosed in NOTICES:32-37 but **not** in the README privacy section (README.md:154-156) and nowhere in the app. | Self-host the three families as `woff2` under `fonts/` (add to deploy.yml cp list + `sw.js` PRECACHE): kills a render-blocking third-party RTT, makes offline visually correct, and removes one GDPR-relevant data flow. Highest perf/effort ratio on this list. |

---

## 4. PWA

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 4.1 | `start_url` / `scope` hardcoded | PARTIAL | HIGH | P2 | code_verified | manifest.json:5-6 | Both absolute: `/space-station-recipes/`. | Correct for the current host; prod manifest is byte-identical to repo. | Breaks everywhere else: local preview at `http://localhost:64306/` gets a manifest scoped to a non-existent path (install prompt suppressed, PWA behaviour untestable locally), and any future custom domain or fork silently produces an uninstallable manifest with no error. | Use relative values — `"start_url": "./"`, `"scope": "./"` — which resolve correctly on every host. Two-character fix, no downside. |
| 4.2 | Icons | FUNCTIONAL | HIGH | — | code_verified + http_fetch_only | manifest.json:10-26 | 192/512 PNG + SVG `sizes:"any"`; all 200 on prod. | — | Minor: no `"purpose": "maskable"` entry, so Android crops the icon into the adaptive mask. | Optional: add a maskable 512 variant. P3. |
| 4.3 | `screenshots` / `shortcuts` / `id` / `lang` | MISSING | HIGH | P2 (growth) | code_verified | manifest.json (all absent) | Verified absent: `screenshots`, `shortcuts`, `id`, `lang`, `dir`, `orientation`, `display_override`, `launch_handler`. | `display: standalone`, theme/background colors set. | Without `screenshots`, Chrome on Android shows the minimal one-line install mini-infobar instead of the rich installable card — measurably lower install conversion, and mobile is 29% of users. Without `shortcuts`, the installed icon has no long-press jump-list. Without `id`, identity is derived from `start_url`, so changing 4.1 later would orphan existing installs. | Add `id: "/space-station-recipes/"` (do this **before** touching `start_url`), 2-3 `screenshots` with `form_factor: narrow` — **`promo/` already contains 2.9MB of screenshots, untracked** — and 3 `shortcuts` (What Heals?, Calculator, Maps). Roughly 25 lines of JSON against existing assets. |
| 4.4 | SW registration + scope | FUNCTIONAL | HIGH | — | code_verified | app.js:124-127; sw.js | `navigator.serviceWorker.register('./sw.js')`, guarded on `https:` or `localhost`. Script at the app root → default scope matches the manifest scope. | Correct. | The `.catch(() => {})` is the visibility problem — see 4.7. | — |
| 4.5 | PRECACHE `?v=` in sync with index.html | PARTIAL | HIGH | P1 | code_verified | index.html:75,758-762 vs sw.js:11-21 | I diffed them programmatically: `style.css=56, i18n.js=26, app.js=33, tutorial.js=3, maps.js=16, ordnance.js=41` — **identical in both files**, and `CACHE = 'chemdb-v74'`. | In sync today. Also genuinely resilient: Pages ignores query strings, so a mismatched `?v=` still returns 200, and the fetch fallback uses `{ignoreSearch: true}` (sw.js:77) — so a drift degrades cache-hit rate rather than breaking the app. Good design. | **Nothing enforces it.** The invariant lives only in ROADMAP §0.2 — which (see 9.5) documents only `app.js`/`style.css` and doesn't even mention `sw.js`. Six version numbers must be hand-edited in two files on every frontend change, 20 times on 2026-09-10. | Assert it in preflight (§9). My prototype does this in ~12 lines. |
| 4.6 | `CACHE` name bump discipline | PARTIAL | HIGH | P2 | code_verified | sw.js:6 | `chemdb-v74` — 74 manual bumps. Activate (sw.js:35-42) deletes every cache except `CACHE` and `DATA_CACHE`. | The `DATA_CACHE` carve-out is excellent: the 3.5MB data.json survives shell bumps (sw.js:7-10 documents the mobile bug that motivated it). | A forgotten bump means the old shell cache is never evicted and returning users keep stale JS until the 10-min HTTP cache and network-first path happen to refresh it. Purely manual across 74 iterations. | Derive it from the PRECACHE contents, or assert in preflight that `CACHE` changed whenever any PRECACHE `?v=` changed (git-diff based). |
| 4.7 | `addAll` atomicity + silent install failure | BROKEN | HIGH | P1 | code_verified + http_fetch_only | sw.js:27-33; app.js:126; deploy.yml diff at 6a6cde8 | `caches.open(CACHE).then(c => c.addAll(PRECACHE))` — `addAll` is all-or-nothing: **one 404 in nine URLs and the SW never installs**. The registration failure is swallowed by `.catch(() => {})` at app.js:126. **This already happened, verifiably:** `316b2d5` (2026-09-10 18:45) added `ordnance.js?v=` to *both* index.html and sw.js PRECACHE while deploy.yml:29 didn't copy it; `6a6cde8` (20:18) fixed it. `git rev-list --count 316b2d5..6a6cde8` = **7 commits = 7 production deploys over 93 minutes** during which `./ordnance.js?v=…` 404'd, `addAll` rejected, and **the service worker failed to install for every visitor on the site**. Offline mode, the data cache and the PiP companion's instant load were all dead. Every one of those 7 builds was green. | Recovered automatically once the asset shipped. | The failure mode is total (whole PWA), silent (swallowed catch, green build), and already realised once. Nothing about the current setup prevents a recurrence — the new guard doesn't read sw.js. | Three fixes, all cheap: (a) validate PRECACHE against `_site` in CI (§9); (b) replace `addAll` with per-URL `cache.add().catch()` so a single miss degrades instead of killing install; (c) replace `.catch(() => {})` at app.js:126 with `.catch(e => track('sw_install_fail', {msg: String(e).slice(0,100)}))` — turns a total invisible outage into a Metrika alert for the price of one line. |
| 4.8 | `skipWaiting` + `clients.claim`, no update UX | PARTIAL | HIGH | P2 | code_verified | sw.js:31, 41 | `skipWaiting()` on install and `clients.claim()` on activate: a new SW takes over immediately, no waiting phase, no prompt. | Users are never stuck on stale code — defensible at 20 deploys/day. | The page keeps running old JS while the SW now serves new assets. Mid-session, a lazy-loaded `maps.js`/`ordnance.js` can arrive from a *newer* build than the running `app.js` — a real version-skew window, and 29% mobile users on long sessions are the most exposed. No "a new version is available, reload" affordance. | Post a message from `activate` to clients and show a toast (the app already has a toast system). ~15 lines, and it converts a silent skew risk into a user-visible, user-controlled reload. |
| 4.9 | Client-side `schemaVersion` check | MISSING | HIGH | P1 | code_verified | sw.js:52-64; app.js:110-120, 136; data.json `meta.schemaVersion` = 3.10.0 | Grepped all JS: **`schemaVersion` appears nowhere in app.js / maps.js / ordnance.js.** `data.json` is served stale-while-revalidate from an **unversioned** `DATA_CACHE` that survives every shell bump by design (sw.js:7-10). | The unversioned data cache is the right call for the offline story. | Direct consequence: a returning user can run **new `app.js` against an arbitrarily old cached `data.json`** on the very first paint, every time. `CHANGELOG.md` tracks schema history, so breaking shape changes do happen (3.10.0 today). The failure is not a clean error — it's `undefined` propagating into rendering, i.e. the silent-wrong-data class. | At app.js:136, compare `DATA.meta.schemaVersion` against a constant bumped with the schema; on major/minor mismatch, force `fetch('data.json', {cache:'reload'})` and refresh the `DATA_CACHE` entry before rendering. ~10 lines, closes the only data-corruption path in the client. |
| 4.10 | Unbounded map caching into the **versioned** cache | PARTIAL | HIGH | P1 | code_verified | sw.js:66-74, 22-25; maps.js:80-81 | The generic handler caches **every** successful same-origin GET into `CACHE` (the versioned shell cache). Real sizes on disk: `maps/` = 63.4MB JSON + 1.5MB PNG over 114 maps; **largest single file `maps/misfits/Wendover.json` = 5,114,776 bytes** — the sw.js:22-25 comment estimates "~1MB", understating the worst case 5×. | Lazy-loading is correct; a visited map does stay available offline, as documented. | Two compounding defects. (a) **Wrong cache:** map data lands in the *versioned* cache, so all of it is deleted on every `CACHE` bump (sw.js:39) — 20 times on 2026-09-10. Users re-download multi-MB maps constantly; the offline promise for maps is effectively void for anyone visiting more than once a day. (b) **No bound, no error handling:** `caches.open(CACHE).then(c => c.put(e.request, clone));` at sw.js:71 has **no `.catch()`**. A `QuotaExceededError` — plausible on iOS, where a session over a few maps can approach the origin quota — is an unhandled promise rejection visible only in the SW console. On Safari, quota pressure can evict the whole origin's storage, taking `DATA_CACHE`/data.json with it: exactly the "hard Failed to load on mobile" symptom sw.js:7-10 says was already user-reported. | Give maps their own unversioned cache (`chemdb-maps`), exempt from the activate sweep, with a simple LRU cap (keep the N most recent map entries). Wrap the `put` in `.catch()` and evict on quota error. ~20 lines in sw.js; removes the most likely mobile failure mode in the product. |
| 4.11 | Offline coverage of maps / ordnance JSON | PARTIAL | HIGH | P3 | code_verified | sw.js:22-25; ordnance.js:319; maps.js:35 | Deliberately not precached — visit-then-cached only. Documented and reasoned. | Right trade-off for a 63MB corpus. | Combined with 4.10(a) the cached copies don't survive deploys, so in practice maps are rarely available offline. | Fixed by 4.10. |
| 4.12 | iOS / Safari caveats | PARTIAL | MEDIUM | P2 | code_analysis_only | manifest.json; index.html:39; sw.js | `apple-touch-icon` present (200 on prod). Not verified on a real device. | Icon and standalone display should work. | iOS ignores `start_url`/`scope`/`shortcuts`/`screenshots`; storage eviction after ~7 days of non-use can clear the whole origin (killing `DATA_CACHE`); no install prompt — users must use Share → Add to Home Screen with no in-app hint. Document Picture-in-Picture (a headline feature) is unsupported in Safari. | Verify on a real iPhone before treating "works offline" as a mobile claim; consider an iOS-only "Add to Home Screen" hint. UNCERTAIN without device testing — flagging as an assumption to check, not a confirmed defect. |

---

## 5. Observability

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 5.1 | JS error tracking | MISSING | HIGH | P1 | code_verified | grep of app.js/maps.js/ordnance.js/i18n.js/tutorial.js/sw.js/index.html | **Zero hits** for `window.onerror`, `addEventListener('error')`, `unhandledrejection`. | Metrika's own script captures nothing of the app's exceptions. | 5,649 lines of hand-written JS across app.js + ordnance.js with no build step, no type checking and no syntax gate (1.7), shipping 20× a day straight to prod. A `TypeError` in a render path produces a blank panel and the owner learns about it from a GitHub issue, if ever. Combined with 4.7, the two most likely total failures are both invisible. | `window.addEventListener('error', e => track('js_error', {msg: e.message.slice(0,100), src: (e.filename||'').split('/').pop(), line: e.lineno}))` plus the same for `unhandledrejection` — **~6 lines**, reuses the existing `track()` at app.js:100, needs one new goal in `scripts/create_metrika_goals.py`. This is the single highest-leverage line-for-line change in the whole audit. |
| 5.2 | Uptime monitoring | MISSING | HIGH | P2 | http_fetch_only | — | No monitor of any kind. Prod health today was established by me running curl by hand. | Pages is highly available; deploy runs are the only implicit signal. | A bad deploy that yields a 200-but-broken page (the ordnance case) registers as perfectly healthy. So would a `data.json` that ships truncated. | Free and sufficient: a scheduled Actions workflow (daily) that curls `/`, `/data.json` and `/sw.js`, asserts status 200 **and** `python -c "json.load(...)"` on data.json **and** that index.html contains the expected `app.js?v=` string, then opens an issue on failure — reusing the `create-issue-from-file` pattern already proven in check-links.yml. ~30 lines, £0. Optionally UptimeRobot free tier for a 5-min external ping. |
| 5.3 | Metrika error goal | MISSING | HIGH | P1 | code_verified | scripts/create_metrika_goals.py (56 goal ids) | 56 goals registered, all feature-usage; none for failure. | Rich product telemetry. | The analytics stack can tell you which tab is popular but not that the app failed to load for 5% of sessions. The data-load failure path at app.js:140-145 renders a user-visible error screen and **fires no goal**. | Add `js_error`, `sw_install_fail`, `data_load_fail` to GOALS and fire them at app.js:141 (existing catch block), app.js:126, and the new handler from 5.1. |
| 5.4 | CI failure notification | PARTIAL | HIGH | P2 | http_fetch_only | — | Default GitHub email to the actor on workflow failure. All 85 deploy and 32 check-links runs are `success`, so the path is untested in practice. | Something exists. | Email-only, to one person, with no escalation — and irrelevant to the failures that matter here, which are all green-build failures. | Fine as-is once 5.2 exists; the smoke check is what turns a green-but-broken deploy into a notification. |
| 5.5 | SW install failure visibility | BROKEN | HIGH | P1 | code_verified | app.js:126 | `.catch(() => {})`. See 4.7 for the 93-minute realised outage. | — | — | One line: `track('sw_install_fail', …)`. |
| 5.6 | Pipeline warnings console-only | PARTIAL | MEDIUM | P2 | code_verified | data.json `warnings` key; ROADMAP.md:17 (rule M1) | The extractor emits warnings and `data.json` carries a `warnings` array; review is a manual human step per M1, run by hand on Windows. | The warnings are at least persisted into the artifact. | Upstream schema drift is explicitly called out in ROADMAP.md:17 as "тихая порча данных" (silent data corruption), and the only control is a human remembering to read console output. No threshold, no diff-vs-last-run, no CI. | Have the extractor fail non-zero when the warning count rises above the previous run's (store the count in `data.json` meta), so a regen that doubles the warnings can't be committed unnoticed. |

---

## 6. Dependencies & toolchain

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 6.1 | Python deps unpinned, no `requirements.txt` | PARTIAL | HIGH | P2 | code_verified | README.md:86; imports in ss14_chem_extractor.py:253, ss14_map_extractor.py:610,728,961, ss14_ordnance.py:602 | Real third-party deps are exactly two: `pyyaml` and `pillow` (PIL imported lazily in 5 places). No manifest or lockfile anywhere in the repo. | Small, stable surface; only two deps. | The pipeline produces the committed `data.json` and 68MB of map artifacts. A Pillow major bump changing PNG output, or a PyYAML change in loader behaviour, silently alters generated artifacts with nothing recording which versions produced them. ROADMAP §0 requires byte-identical regen (rule 6) — unreproducible without pinned deps. | `requirements.txt` with `pyyaml==X.Y.Z` / `pillow==A.B.C` (4 lines), and record the versions in `data.json.meta` at bake time so any artifact can be traced to its toolchain. |
| 6.2 | README `pip install pyyaml openpyxl pillow` | BROKEN | HIGH | P3 | code_verified | README.md:86 | `openpyxl` appears **nowhere** in the Python sources — only in README.md:86, a plan doc (docs/superpowers/plans/2026-07-12-map-item-finder.md:749) and the stale worktrees. Removed with the Excel export. | Command still works. | Every new contributor installs a dependency the project abandoned. | Drop `openpyxl` from README.md:86 (folds into 6.1's requirements.txt). |
| 6.3 | Python version assumptions | PARTIAL | HIGH | P2 | code_verified + runtime_verified | check-links.yml:34 (`"3.11"`); local `python --version` → **3.14.3** | CI pins 3.11; the machine that actually generates every committed artifact runs 3.14.3. No `python_requires` or version assertion anywhere. | Both work today. | A **three-minor-version gap** between the only CI Python and the Python that bakes production data. Dict ordering, `json` formatting details and stdlib behaviour changes across 3.11→3.14 are precisely the class of thing that breaks rule 6's byte-identical regen, and nothing would flag it. | State the supported version in README + requirements, and bump check-links.yml:34 to match the local interpreter so CI and the bake machine agree. |
| 6.4 | GitHub Actions tag validity | FUNCTIONAL | HIGH | — | http_fetch_only | deploy.yml:24,64,65,68; check-links.yml:29,32,45,82 | All 8 tags resolved to real commits via api.github.com (SHAs listed in 1.3). None deprecated; `checkout@v4` in check-links vs `@v6` in deploy is a minor inconsistency, both current. | — | — | Optionally align on `@v6`. |
| 6.5 | No JS dependencies / no CDN JS | FUNCTIONAL | HIGH | — | code_verified | grep across index.html/app.js/maps.js/ordnance.js/i18n.js/tutorial.js/style.css | **Zero** hits for `unpkg`, `cdnjs`, `jsdelivr`. `vis-network` was genuinely removed in A1.1. No `package.json`, no npm supply chain. | Excellent — this eliminates the largest attack surface a site like this normally carries. | Two remote resources remain: the Metrika tag (index.html:83, intentional) and the Google Fonts `@import` (style.css:13) — see 3.12. Note that a naive "no CDN" grep misses the latter because it lives in CSS. | Self-host the fonts (3.12) and the only remote dependency left is the analytics tag. |

---

## 7. Secrets & repo hygiene

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 7.1 | `.env` ignored, never committed | FUNCTIONAL | HIGH | — | code_verified | .gitignore:6 (`*.env`) | `git check-ignore -v .env` → `.gitignore:6:*.env` (the `*` matches the empty string, so bare `.env` is covered). File exists locally (78 bytes), `git log --all -- .env` empty. | Correct. | — | — |
| 7.2 | No tokens in history | FUNCTIONAL | HIGH | — | code_verified | — | `git grep -I -E "y0_[A-Za-z0-9_-]{15,}"` and `git grep -I "AQAAAA"` across **all 230 revisions** (`git rev-list --all`): zero hits. Verified the clone is complete first — not shallow (`.git/shallow` absent), no promisor/partial-clone filter, no alternates, all 2,023 objects local — so the grep genuinely covered full history. | Clean. | — | — |
| 7.3 | Token handling in scripts | PARTIAL | HIGH | P3 | code_verified | scripts/export_metrika_stats.py:27-28, 66-71, 161; scripts/create_metrika_goals.py:27, 147 | Env var → `--token` → repo-root `.env`. The docstring example uses a truncated placeholder (`y0_AgA...`), not a live token. (I did not read `.env`.) | Sensible precedence; `.env` correctly ignored. | `--token` on argv puts the secret in shell history and the process table. | Document env var / `.env` as the only supported paths; keep `--token` but note the hazard, or drop it. |
| 7.4 | Untracked root clutter | PARTIAL | HIGH | P3 | code_verified | `git status`: `logo (1).png` (56K), `logo-steam.png` (36K), `promo/` (2.9MB) | Three untracked items at the repo root, including a directory of promo screenshots and a Steam guide. | Not shipped, so no prod impact. | `logo (1).png` is a browser-download artifact with a space and a `(1)` in the name — pure noise, and it makes `git status` non-empty forever, which is how real changes get overlooked in a 3-parallel-session workflow. | Commit `promo/` (it contains the assets 4.3 needs for manifest `screenshots`) and `logo-steam.png`; delete `logo (1).png`. Either way `git status` should be clean. |
| 7.5 | Stale detached worktrees | PARTIAL | HIGH | P2 | code_verified | `git worktree list`; `du -sh .claude/worktrees/*` | Three registered detached worktrees plus an unregistered empty `sharp-borg` dir. Disk: `colonial-marines-universe` 15M, `substance-obtainability-items` 15M, **`ss14-resource-finder-b8159d` 1.3 GB** (the `cache_maps` copy). Working tree total `2.1 GB` vs `.git` 47M. | Isolated from main. | 1.3 GB of duplicated cache, and — the real hazard — these contain full stale copies of README.md and the extractors. My own `openpyxl` grep hit three worktree copies before the real file; a future search-and-fix can easily edit the wrong tree. | `git worktree remove` all three (or `prune` after deleting), delete the empty `sharp-borg`, and delete the 5 merged/gone local `claude/*` branches (`claude/d3-item-fill-on-main` is already `[gone]`). Reclaims ~1.3 GB. |
| 7.6 | `.claude/launch.json` tracked while `.claude/` is ignored | PARTIAL | HIGH | P3 | code_verified | .gitignore:7; `git ls-files .claude` → `.claude/launch.json` | Tracked before the ignore rule existed, so it stays tracked (gitignore doesn't affect tracked files). | The preview config is shared, which is genuinely useful. | Confusing by construction: edits to it show in `git status` while everything else under `.claude/` is invisible. | Make it explicit: add `!.claude/launch.json` after the `.claude/` rule so the intent is readable. |
| 7.7 | Repo size / growth per re-bake | PARTIAL | HIGH | P2 | code_verified | `git count-objects -vH`; `git log --oneline -- data.json` | 230 commits, 2,023 loose objects, 41.37 MiB, **`in-pack: 0` — `git gc` has never run in this clone**. Tracked artifacts: `maps/` 66M (63.4M JSON + 1.5M PNG), `data.json` 3.7M, `sprites/` 322K. `data.json` has **39 revisions**; `maps/` has been touched by 12 commits but only ~2 full bakes. | Under GitHub's 1 GB soft limit with room. | Growth is monotonic and unbounded: each `data.json` regen adds a ~400KB compressed blob permanently (39 so far ≈ 15MB), and a **full map re-bake would add roughly 10-15MB packed, forever** — JSON of item coordinates delta-compresses poorly across bakes. At a few re-bakes a year this is fine; a habit of re-baking maps per fork addition is not. | Run `git gc --aggressive` locally (reclaims space, speeds every git op). Before any full map re-bake, consider whether per-map JSON needs to be in git at all, or whether it belongs in a Pages-only artifact built at deploy time. Revisit if `.git` passes ~250MB. |
| 7.8 | SPDX headers inconsistent | PARTIAL | HIGH | P3 | code_verified | app.js:1-5 ✓, ordnance.js:1-5 ✓, i18n.js:1-4 ✓, style.css:1-5 ✓, config.py:1-4 ✓, index.html:1-6 ✓ — **maps.js:1 ✗, tutorial.js:1 ✗, sw.js:1 ✗** | 3 of 9 spot-checked source files carry no `SPDX-License-Identifier`. | LICENSE (GPL-3.0-only) and the majority of files are correctly marked. | For a GPL project, unmarked files weaken the licence claim on exactly the files most likely to be copied standalone (`sw.js` is a self-contained 83-line module). | Add the 4-line header to maps.js, tutorial.js, sw.js. |
| 7.9 | README fork count vs data | PARTIAL | HIGH | P3 | code_verified | README.md:29 vs README.md:45-65 and data.json `meta.forks` | README.md:29 says "21 SS14 forks" then lists **20 names** (Space Stories — Marine Corps Core is missing from the prose list). The table below it has 21 rows and its per-fork reagent counts **match `data.json` exactly** (vanilla 407, rmc14 113, cmu 22, rucm 0, stories_cm 18, goob 100, misfits 151, adt 100, trauma 44, deltav 124, funky 103, monolith 28, starlight 27, sunrise 37, …). | The table — the part that's hard to maintain — is accurate. | One-name omission in the prose. | Add the missing name at README.md:29. |
| 7.10 | Orphan tracked logo SVGs | PARTIAL | HIGH | P3 | code_verified + http_fetch_only | `git ls-files`: logo.svg, logo-header.svg, logo-antag.svg, logo-header-antag.svg | Four SVGs tracked in git, referenced by **nothing** (index.html inlines its 3 SVGs), absent from deploy.yml's cp list. Confirmed on prod: `logo.svg`, `logo-header.svg`, `logo-antag.svg` all **404**. | No user impact — nothing links to them. | Four tracked assets that look shipped and aren't. Anyone adding a `<img src="logo.svg">` gets a silent 404 the build guard *would* catch — but only because it's in index.html. | Either add them to the cp list or delete them. Pick one. |

---

## 8. Legal & privacy

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 8.1 | Metrika webvisor + clickmap, no consent gate | BROKEN | HIGH | P1 | code_verified | index.html:85 | `ym(108585248, 'init', {ssr:true, webvisor:true, clickmap:true, ecommerce:"dataLayer", … accurateTrackBounce:true, trackLinks:true})`. Grepped the entire app for `consent\|cookie\|privacy\|gdpr\|согласи` — **zero hits in index.html, app.js, i18n.js, style.css**. | Analytics work and drive real product decisions (the A1.1 tab removal was evidence-based). | **Webvisor is session replay**: it records DOM mutations, mouse movement, scrolling and keystrokes in input fields, and reconstructs a watchable video of the visit. Under GDPR that is not "anonymous usage analytics" — it requires prior, explicit, opt-in consent, which the site never requests, and it is set before any user interaction. The audience is ~15% DE/EU. The search box is a free-text input that users type into. Secondary point: the processor is a Russian company, so EU→RU transfer adequacy is also unaddressed. Grading this honestly: it is the most serious *non-technical* exposure in the project, and it is one config flag. | Cheapest compliant configuration, in order of preference: **(a) set `webvisor:false` and `clickmap:false`** — one line at index.html:85, removes the session-replay problem entirely while keeping goals, funnels and the core-loop analysis that actually informs decisions (nothing in the analytics work cited in ROADMAP depended on replay); **(b)** if replay is genuinely wanted, gate the whole `ym(...)` init behind an opt-in banner and default to off. Do **not** ship a banner that loads Metrika before the click — that is worse than (a), not better. |
| 8.2 | In-app privacy notice | MISSING | HIGH | P1 | code_verified | README.md:154-156; index.html:134-135 | The only disclosure lives in README.md on GitHub. The in-app disclaimer panel (index.html:134-135) covers the *fictional-chemistry* legal point thoroughly and links GitHub + Feedback — but says nothing about analytics, and there is no privacy link anywhere in the UI. | The fictional-data disclaimer itself is well written and prominent. | A visitor arriving from a Steam guide has no way to learn they are being session-recorded. The README text also under-describes it ("anonymous usage analytics") and omits the Google Fonts transfer (3.12) entirely. | Add a "Privacy" line to the existing disclaimer panel at index.html:135 (the container already exists, so this is ~4 lines of HTML + an i18n key) naming: Yandex.Metrika, what is collected, Google Fonts, and localStorage. Pairs naturally with 8.1(a) — after disabling webvisor the notice becomes short and easy to stand behind. |
| 8.3 | Google Fonts as an undisclosed third party | PARTIAL | HIGH | P2 | code_verified | style.css:13; NOTICES:32-37; README.md:154-156 | Disclosed in NOTICES as a licensing note; **not** disclosed in the README privacy section or anywhere in the app. | Licence attribution (OFL 1.1) is correct. | Every page load transmits EU visitor IPs to Google without notice or consent — the exact pattern German courts have ruled on. | Self-hosting (3.12) resolves the perf, offline *and* privacy issues in one change. Strongly preferred over adding it to a disclosure. |
| 8.4 | NOTICES fork coverage: 8 of 21 | PARTIAL | HIGH | P1 | code_verified | NOTICES:14-29 vs data.json `meta.forks` (21) | NOTICES lists 8 forks (Vanilla, RMC14, Goob, Funky, Delta-V, Starlight, Frontier, Dead Space). `data.json` ships reagents from 21, so **13 are unattributed**: cmu (22 reagents), rucm, stories_cm (18), trauma (44), omu (20), carpmosia (12), monolith (28), harmony (8), corvax (8), adt (100), sunrise (37), fish (10), **misfits (151 — the second-largest contributor in the entire database)**. | The 8 listed are accurate and the structure is right. | For a GPL-3.0 project that takes attribution seriously enough to ship `.github/ISSUE_TEMPLATE/attribution.yml`, omitting the source of 458 reagents — including the #2 contributor — is a straightforward compliance gap and, given the SS14 fork community, a reputational one. | Generate the NOTICES fork list from `config.py` `FORK_REGISTRY` at bake time so it can never drift again; ~15 lines in the extractor, and it turns a recurring manual duty into an invariant. |
| 8.5 | NOTICES asset licence claim | PARTIAL | HIGH | P1 | code_verified | NOTICES:11, 14-16, 27-29 | States `License: MIT` and "The sprite assets in sprites/ and the chemistry prototype data … are sourced from Space Station 14", then "All listed forks are MIT-licensed or MIT-compatible". | Correct for *code* and for YAML prototype data. | SS14's own licensing splits code (MIT) from **assets**: textures/RSI sprites are typically **CC-BY-SA-3.0** with per-directory `meta.json` attribution, and some are more restrictive. The repo vendors **134 sprite files** under a NOTICES that declares them MIT. CC-BY-SA is share-alike and attribution-bound — a materially different obligation from MIT, and misstating it is the kind of thing fork maintainers notice. I have not opened each upstream `meta.json`, so the exact licence per sprite is UNCERTAIN — but the blanket MIT claim is not defensible as written. | Split NOTICES §1 into "Prototype data (MIT)" and "Sprite assets (per-file licence from upstream `meta.json`, typically CC-BY-SA-3.0)", and have the sprite extractor carry each source's `meta.json` copyright/licence into a generated attribution block. Until then, soften the blanket claim. |
| 8.6 | GPL-3.0 compliance for the hosted site | FUNCTIONAL | HIGH | — | code_verified | LICENSE:1-3; index.html:135; index.html:72 (JSON-LD `license`) | GPL-3.0-only, source public, footer links the repo, JSON-LD points at LICENSE. GPL (unlike AGPL) imposes no network-use obligation, and nothing is vendored that conflicts. | Compliant. | Only the header gaps at 7.8. | — |
| 8.7 | ToS / accounts / GDPR data-subject rights | FUNCTIONAL | HIGH | — | code_verified | — | No accounts, no auth, no payments, no server-side storage; client state is `localStorage` only. Export/erasure obligations are therefore minimal — except for whatever webvisor captures (8.1). | Genuinely low-risk architecture. | — | Fixing 8.1 removes the only meaningful data-subject exposure. |
| 8.8 | Issue templates requiring a source link | FUNCTIONAL | HIGH | — | code_verified | .github/ISSUE_TEMPLATE/attribution.yml, strategy-inaccuracy.yml | Templates exist and enforce provenance for data corrections. | Good practice, consistent with the sources.py design. | — | — |

---

## 9. Process infrastructure

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 9.1 | Cache-bust discipline — no automated check | MISSING | HIGH | P1 | code_verified | ROADMAP.md:14; index.html:75,758-762; sw.js:6,11-21 | Six `?v=` numbers in index.html must match six in sw.js PRECACHE, plus a `CACHE` bump. Enforcement: a prose rule. | Currently in sync (verified programmatically). | Hand-maintained across 20 deploys in one day. The failure is invisible (4.5/4.6). | Preflight check below — ~12 lines. |
| 9.2 | Deploy whitelist for **runtime** fetches — unguarded | MISSING | HIGH | P1 | code_verified | deploy.yml:41-62 | The CI guard reads index.html only. `data.json`, `sw.js`, `sprites/`, `maps/`, `ordnance/` are unverified (1.2). | Index-referenced assets are guarded. | The `sw.js` half of incident #1 would still pass today. | Preflight check below — ~25 lines. |
| 9.3 | Analytics registry sync — no check | MISSING | HIGH | P2 | code_verified | scripts/create_metrika_goals.py (56 ids); track() call sites in app.js/maps.js/ordnance.js/tutorial.js | I diffed registry vs call sites. **Currently in sync** — the only apparent miss, `track('tab_' + tab)` (app.js:513), is a dynamic prefix whose 8 concrete goals *are* registered, and the one unused id (`ordnance_pareto_use`) is deliberately retained with an explanatory comment at create_metrika_goals.py:93-95. Discipline here is genuinely good. | In sync; the retired-goal comment shows real care. | Nothing enforces it, and per ROADMAP §0.3 Metrika **silently drops** conversions for unregistered goals — a new goal that never fires looks identical to a feature nobody uses, which then gets cut on bad data. | Preflight check below — ~15 lines, with an allowlist for the dynamic `tab_` prefix so it doesn't false-positive. |
| 9.4 | Regen determinism — no CI | MISSING | HIGH | P2 | code_verified | ROADMAP.md:17 (rule M1), ROADMAP.md §0 rule 6 | Byte-identical regen (minus `meta.generated`) is a stated invariant, verified only by hand on the owner's Windows box. | The rule is written down and the extractors are deterministic by design. | Unverifiable and unpinned (6.1/6.3). Upstream schema drift is named in ROADMAP.md:17 as silent data corruption. A CI regen would need network access to 21 fork repos — not free, and out of scope for preflight. | Don't put full regen in CI. Instead make the *local* run self-checking: have the extractor hash its output (excluding `meta.generated`) into `meta.outputHash` and warn when a regen with unchanged inputs produces a different hash. |
| 9.5 | ROADMAP §0 rule 2 is narrower than the real invariant | PARTIAL | HIGH | P2 | code_verified | ROADMAP.md:14 | Rule 2 reads: "при правке `app.js`/`style.css` — bump `?v=N` в `index.html`". It names **two** of the six versioned files and does **not mention `sw.js` PRECACHE or the `CACHE` bump at all**. | The rule exists and the practice is correct anyway. | The written process is a strict subset of the real one. Someone following the documented rule literally — after editing `i18n.js`, `maps.js`, `ordnance.js` or `tutorial.js` — ships a stale precache. Cross-session onboarding relies on this text. | Rewrite ROADMAP.md:14 to name all six files and both sw.js obligations. Then make it moot with 9.1. |
| 9.6 | Proposed `scripts/preflight.py` | MISSING | HIGH | P1 | runtime_verified (prototype executed) | prototype run against HEAD | I built and ran a working prototype (scratchpad `preflight_proto.py`). It performs three checks — (1) index.html `?v=` vs sw.js PRECACHE, (2) every JS `fetch()`/`src` literal resolved against the deploy.yml cp list, (3) `track()` goals vs the GOALS registry — and correctly reported HEAD as clean on (1) and (2), with one known-benign dynamic-prefix false positive on (3). | Concept proven against the real repo, not hypothetical. | It doesn't exist in the repo, so none of the invariants are enforced. | Ship it. Add `node --check` on the six JS files (1.7), an index.html/sw.js PRECACHE-vs-`_site` existence check (1.2/4.7), and an assertion that the meta/JSON-LD counts match `data.json` (3.11). **~140 lines total**, stdlib-only, runs in under a second. Wire it as a deploy.yml step before "Collect static files". **All three historical whitelist incidents — sw.js/manifest (2c062f6), i18n.js (3408bcd), ordnance.js (6a6cde8) — would have been caught at build time**, including the sw.js one the current guard still misses. |

---

## 10. Growth-lens wins, ranked by impact ÷ effort

Context used for weighting: acquisition is Steam guides, SEO ≈ 0, search visits ≈ 0, mobile 29% of users (but only 10% of visits — a retention signal, not just a traffic one), RU 34% of users, 78% returning.

| Rank | Change | Effort | Impact | Why it ranks here | Ref |
|---|---|---|---|---|---|
| 1 | `window.onerror` + `unhandledrejection` → `track()` | ~6 lines | Retention | You cannot improve what you cannot see. 5,649 lines of untested JS ship to prod 20×/day with zero error signal. Every other item on this list is guesswork until this exists. | 5.1 |
| 2 | Branded `404.html` with navigation back into the app | ~30 lines | Acquisition | Steam guides are the *entire* funnel, and a stale or mistyped guide link currently dead-ends on GitHub's own error page. The only item that recovers visitors already on the doorstep. | 1.8 |
| 3 | `hreflang` + `og:locale:alternate` + head-script `lang` stamping | ~5 lines | Acquisition (RU) | 34% of users are Russian and the RU surface is invisible to every crawler and link unfurler. Cheapest possible reach gain for the largest identifiable segment. | 3.9, 3.10 |
| 4 | Self-host the three Google fonts | ~1h | Retention (mobile) + privacy | Removes a render-blocking third-party round trip from the critical path on the 29% mobile cohort, fixes the offline PWA's appearance, and eliminates a GDPR-relevant transfer. One change, three wins. | 3.12, 8.3 |
| 5 | `webvisor:false` (+ a privacy line in the disclaimer panel) | 1 line + ~4 lines HTML | Risk elimination | Removes the only serious legal exposure in the product at essentially zero analytical cost — nothing in the recorded analytics decisions depended on session replay. | 8.1, 8.2 |
| 6 | `scripts/preflight.py` in the deploy workflow | ~140 lines | Reliability | Catches all three historical whitelist incidents plus the class the current guard misses; makes 20-deploys-a-day safe rather than lucky. | 9.6 |
| 7 | Map cache → own unversioned cache + LRU + quota `catch` | ~20 lines | Retention (mobile) | Fixes the most plausible mobile failure mode (quota eviction taking `data.json` with it) and stops maps being re-downloaded after every deploy. Mobile at 10% of visits vs 29% of users suggests mobile sessions are already not sticking. | 4.10 |
| 8 | Manifest `screenshots` + `shortcuts` + `id` | ~25 lines JSON | Acquisition (mobile) | Upgrades Android's install prompt from the minimal infobar to the rich card. The screenshots already exist, untracked, in `promo/`. | 4.3, 7.4 |
| 9 | Generated sitemap: tabs × 2 languages + top reagent deep links | ~40 lines | Acquisition | Turns a 1-URL sitemap into a few hundred. Honest caveat: value is capped while content is client-rendered, so it ranks below 3. | 3.8 |
| 10 | Post-deploy smoke check workflow (curl + JSON parse + `?v=` assert) | ~30 lines | Reliability | Converts the green-build-broken-site failure mode — the one that actually happened — into a filed issue. | 5.2 |
| 11 | Client-side `schemaVersion` guard | ~10 lines | Reliability | Closes the only silent data-corruption path reaching users (new code, old cached data). | 4.9 |
| 12 | "New version available" toast on SW activate | ~15 lines | Retention | Removes the mid-session version-skew window and gives returning users (78%!) a reason to trust the app is current. | 4.8 |
| — | *Not recommended:* compressing `data.json` | — | — | **Already done** — Pages serves it gzipped at 416,316 bytes from 3,686,598 (8.9×), automatically. Brotli is not offered and is not controllable. Don't spend a sprint here. | 3.4, 3.5 |
| — | *Deferred:* splitting `data.json` per fork | ~1 day | Marginal | The compressed payload is 406KB — already acceptable on mobile. `reagents` is 64% of the bytes, so a split would help, but it complicates the fork-ancestry model (app.js `forkVisible` needs vanilla + the whole ancestry) and the offline story for a modest win. Revisit only if error tracking (#1) shows data-load failures concentrated on mobile. | 3.4 |
| — | *Deferred:* Lighthouse CI budget | ~2h | Marginal now | Real value, but it measures what #4 and #7 already fix. Add it after those land so the budget encodes the improved baseline rather than the current one. | 1.7 |

---

## Files examined

Repo (read):
- `D:/Space Station Recipes/.github/workflows/deploy.yml`
- `D:/Space Station Recipes/.github/workflows/check-links.yml`
- `D:/Space Station Recipes/sw.js`
- `D:/Space Station Recipes/manifest.json`
- `D:/Space Station Recipes/robots.txt`
- `D:/Space Station Recipes/sitemap.xml`
- `D:/Space Station Recipes/index.html` (head 1-90, asset refs, disclaimer 134-135)
- `D:/Space Station Recipes/app.js` (100-145, 513, 721, 2163, 2938, 2958, 3264)
- `D:/Space Station Recipes/maps.js` (1-5, 35, 80-81, 250)
- `D:/Space Station Recipes/ordnance.js` (1-5, 319, 579-580, 2261-2262)
- `D:/Space Station Recipes/i18n.js` (1-4, 7, 26)
- `D:/Space Station Recipes/tutorial.js` (1-20, 150, 167, 187, 336)
- `D:/Space Station Recipes/style.css` (1-13, font-family rules)
- `D:/Space Station Recipes/scripts/check_sources.py`
- `D:/Space Station Recipes/scripts/create_metrika_goals.py` (27, 50-95, 147-152)
- `D:/Space Station Recipes/scripts/export_metrika_stats.py` (25-28, 66-71, 161-172) — `.env` itself NOT read
- `D:/Space Station Recipes/sources.py` (83-93, 248-300, 337-344, SOURCES import)
- `D:/Space Station Recipes/README.md` (26-65, 78-100, 154-156)
- `D:/Space Station Recipes/ROADMAP.md` (1-60)
- `D:/Space Station Recipes/NOTICES`, `D:/Space Station Recipes/LICENSE`, `D:/Space Station Recipes/.gitignore`
- `D:/Space Station Recipes/data.json` (meta, key sizes, fork counts — via `python -c`)
- `D:/Space Station Recipes/maps/` (file size census), `D:/Space Station Recipes/cache/link_health.json`
- `D:/Space Station Recipes/ss14_chem_extractor.py:253`, `D:/Space Station Recipes/ss14_map_extractor.py:610,728,961`, `D:/Space Station Recipes/ss14_ordnance.py:602` (PIL imports only)

Scratch (created, outside the repo):
- `<scratch>/preflight_proto.py` — the §9.6 prototype, executed read-only against HEAD

Git (read-only): `log`, `show`, `grep` across all 230 revisions, `worktree list`, `branch -v`, `ls-files`, `check-ignore`, `count-objects -vH`, `rev-list`, `cat-file --batch-all-objects`

Network (read-only GET/HEAD):
- `https://mikameo.github.io/space-station-recipes/` + `/data.json` (gzip and identity) `/sw.js` `/manifest.json` `/robots.txt` `/sitemap.xml` `/og-image.png` `/maps/index.json` `/maps/vanilla/Bagel.{png,json}` `/ordnance/stories_cm.json` `/favicon.svg` `/favicon-192.png` `/apple-touch-icon.png` `/logo.svg` `/logo-header.svg` `/logo-antag.svg` `/definitely-not-a-real-path-xyz`
- `api.github.com`: issues, deploy.yml runs, check-links.yml runs, 8 × `git/ref/tags`
- `https://forum.spacestation14.com/` (HEAD, replicating the checker's request)

## Summary counts

**Classification** (60 rows): FUNCTIONAL 21 · PARTIAL 26 · BROKEN 5 · MISSING 8 · UNCERTAIN 0
*(4.12 iOS/Safari is PARTIAL with an explicitly UNCERTAIN sub-claim — untested on real hardware; 8.5 is PARTIAL with an UNCERTAIN per-sprite licence determination.)*

**Priority** (39 non-FUNCTIONAL rows): P0 0 · P1 14 · P2 17 · P3 8

BROKEN rows: 2.1 (auto-commit no-op), 2.4 (forum URLs are the site root), 3.11 (stale meta counts), 4.7 (SW install atomicity + silent failure), 5.5 (SW failure invisibility), 8.1 (webvisor without consent) — 4.7 and 5.5 are the same defect seen from the deploy and the observability side.

## Top findings

1. **A 93-minute total PWA outage already happened and left no trace** — `316b2d5`→`6a6cde8` on 2026-09-10 shipped 7 green production deploys where `ordnance.js` 404'd, making `sw.js:29` `addAll` reject so the service worker never installed for anyone, with the failure swallowed by `app.js:126` `.catch(() => {})`. The guard added afterwards (deploy.yml:41-62) reads index.html only and would still miss the same failure for `data.json` or `sw.js`.
2. **Yandex.Metrika runs session replay (`webvisor:true`, index.html:85) with no consent gate and no in-app privacy notice** for an audience ~15% EU — the only disclosure is README.md:154-156, which no visitor sees. Cheapest compliant fix is one line: `webvisor:false`.
3. **Zero error observability** — no `window.onerror`, no `unhandledrejection`, no error goal among 56 Metrika goals, no uptime check; ~6 lines reusing the existing `track()` at app.js:100 would close it.
4. **The link-rot workflow is largely theatre**: its auto-commit step has been a silent no-op for 32 runs (`cache/` is gitignored, so `git status` sees nothing), its three forum "sources" point at the forum homepage (sources.py:261/270/279 — the deep links never existed), and its 10% threshold on n=23 means **two permanently dead sources never alert**.
5. **NOTICES attributes 8 of 21 forks and declares 134 vendored sprites MIT** (NOTICES:11-29) — 13 forks including misfits (151 reagents, the #2 contributor) are unattributed, and SS14 textures are typically CC-BY-SA-3.0, a materially different obligation.
6. **Map data is cached into the versioned shell cache with no bound and no error handling** (sw.js:66-74) — 63.4MB across 114 maps, largest single file 5.1MB (the code comment says "~1MB"), wiped on every `CACHE` bump (20× on 2026-09-10), and an unhandled `QuotaExceededError` on mobile can evict `data.json` with it — precisely the failure sw.js:7-10 records as already user-reported.
7. **Growth is being left on the table for the price of a few lines**: no `404.html` (GitHub's own error page terminates every stale Steam-guide link), no `hreflang` for the 34% RU audience, a 1-URL sitemap, and meta/JSON-LD advertising "1369 reagents … 20 forks" against an actual 1387/1104/21.
