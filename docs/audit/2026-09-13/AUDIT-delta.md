# ИНКРЕМЕНТАЛЬНЫЙ АУДИТ NanoTrasen ChemDB — серия T после T4 (тактическая карта)

**Дата**: 2026-09-13, 17:36 → 18:05 по Москве (HEAD `cf388d2`; на origin/main — до `7eff175`, четыре коммита T4 не отправлены)
**Режим**: quick (сокращённый `/project-audit` после четырёх инкрементов, без чекпоинтов — по мандату владельца «действуй самостоятельно»)
**Скоуп**: коммиты серии T `60a6ce3` (T1), `3b54afc` (T2), `1c85029` (T3), `24c912f`+`aa7aea5` (T4), `cf388d2` (карточка «Разделов») и `7eff175` (fix(maps)); коммиты ordnance параллельной сессии (`38d8af0`…`a94d8f3`) — вне скоупа
**Методология**: T1 «данные и целостность» (осторожный DBA) · T2 «логика и безопасность» (от первых принципов) · U1 «нетерпеливый новый пользователь» (браузер); оркестратор — самопроверка покрытия, дельта к базовому аудиту
**Модели**: Fable 5.1 (все агенты и оркестратор)
**Браузерный уровень**: Browser pane (CDP: клики, клавиатура, JS, консоль), локальное превью `http://localhost:60968` == рабочее дерево
**Companion Skills**: reverse-prompt — пропущен (quick); consensus — не применялся (по одному агенту на домен); war-room — не сработал (P0 нет, конфликтов между отделами нет); tasks — отключены (реестр задач = ROADMAP)
**Приложения**: [T1.md](T1.md), [T2.md](T2.md), [U1.md](U1.md) — полные отчёты агентов
**Оговорка**: инкрементальный аудит. Полное покрытие проекта — в базовом [AUDIT.md](../../../AUDIT.md) от 2026-09-11; для полного покрытия запустите полный аудит.

---

# ИЗМЕНЕНИЯ С ПОСЛЕДНЕГО АУДИТА (2026-09-11)

| Категория | Количество |
|---|---|
| Исправлено (было BROKEN/PARTIAL → FUNCTIONAL) | **0** — серия T не трогала пункты базового аудита |
| Регрессии (было FUNCTIONAL → BROKEN/PARTIAL) | **0** |
| Новые проблемы | **2 P2 + 1 major UX** в новом коде; P3 — 28 (см. приложения) |
| Без изменений (по-прежнему открыто) | **3**, поверхность выросла |

| Пункт базового аудита | Состояние после серии T |
|---|---|
| B20 — guard деплоя видит только `src\|href` страниц | Открыт; серия T добавила runtime-запросы `tactical/index.json`, `tactical/<fork>/<planet>.json\|png`, `promo/cards/tactical.webp`. Смягчение: `cp -r tactical` в cp-list, офлайн-проверка `check_tactical.py` в CI и ручная репетиция `_site` в сессии (52 ссылки, 3 страницы). Автоматики по-прежнему нет. |
| B21 / D1 — ручные инварианты `?v=` ↔ PRECACHE ↔ CACHE | Открыт: `home.js?v=5` / `sections.json?v=5` / `chemdb-v104` подняты вручную и синхронно, но `feedback.js?v=4` в `tactical.html` разошёлся с `?v=3` в `index.html`, `library.html`, `sw.js` (T2 #, P3) — ровно тот класс ошибки, который preflight должен ловить. |
| C7 — `--selfcheck`/`--verify` пишут в отслеживаемые файлы | Открыт для старых экстракторов; новый `ss14_tactical.py --verify` собран по рекомендации базового аудита — во временный каталог, сравнение без записи (T1 подтвердил: `git status` чист после прогона). |

---

# ЧАСТЬ 0: КРИТИЧЕСКИЕ НАХОДКИ (PRODUCTION)

**P0 нет.** Продакшен не менялся: четыре коммита T4 не отправлены, коммит `7eff175` прошёл CI (`Series T checks`, asset-guard 52/52). Данные детерминированы: `ss14_tactical.py --verify` — `verify: OK` на обоих форках, эталоны LV-624 сходятся.

---

# ЧАСТЬ I: КЛАССИФИКАЦИЯ (сводно; полные таблицы — в приложениях)

| Отдел | FUNCTIONAL | PARTIAL | BROKEN | MISSING | UNCERTAIN | P0 | P1 | P2 | P3 |
|---|---|---|---|---|---|---|---|---|---|
| T1 данные и пайплайн | 28 | 7 | 0 | 0 | 2 | 0 | 0 | 4 | 5 |
| T2 логика и безопасность | 55 | 19 | 0 | 2* | 3 | 0 | 0 | 1 | 23 |
| U1 браузер (OK / DEFECT) | 18 OK | 10 DEFECT (major 1, minor 6, cosmetic 3) | 0 | 0 | — | — | — | — | — |

\* MISSING у T2 — запланированные инкременты (карточки T5–T7, реестр целей Метрики T12), не дефекты.

## A. Подтверждено (главное)
- Математика `tactical/logic.js` сверена T2 с кодом игры в `cache_tactical/rmc14/repo` (`MortarSystem.cs`, `SharedMortarSystem.cs`, `RangefinderSystem.cs`, `AreaSystem.cs`, `RMCPlanetSystem.cs`): `floor(центр)+сдвиг`, ошибка `⌊|Δ|/20⌋` от центра тайла, дальность `M−64…M+65` (строгие сравнения), дрожание `[-1,0,0,1]` после валидации, лазер без ошибки/смещения/дрожания, порядок отказов, `IsRoofed ≤ range`, вариация ±500 включительно.
- Радиусы снарядов: формула `intensity_to_radius` совпадает с `ExplosionSystem.cs:214-228`; 5,35 (HE) и 3,87 (Frag) пересчитаны T1 вручную.
- Контракт данных: ключи флагов = `DataField` `AreaComponent.cs`; RLE/bounds/PNG/`h` — инварианты на всех 20 планетах; `7eff175` меняет у шести карт только `bounds`/`px`, остальное побайтово равно.
- Безопасность страницы: `esc()` на каждом `innerHTML`, строгая миграция `localStorage`, без утечек слушателей; `data-no-autopopup` не регрессирует `index.html`/`library.html`; L10N en/ru 118/118.
- Браузер (U1): первый экран понятен; клик при «вписать» приближает, второй выбирает; вставка всех трёх форматов; переставленные числа ловятся сверкой; миномёт/цель/копирование; слои, снаряд, радиус и раунд переживают перезагрузку; клавиатура с фокус-кольцом; 375 px без горизонтальной прокрутки; карточка «Тактическая карта» ведёт на страницу; консоль чистая.

## B/C. Что требует работы (P2 и major)

| # | Источник | Находка | Приоритет | Где |
|---|---|---|---|---|
| 1 | T2 #8 | Разрешение зоны проверяется на точке прицела, а игра — на `цель + ошибка + смещение` (`MortarSystem.cs:110-118` → `ValidateTargetCoordinates`): у кромки зоны посадки/накрытой зоны при `e ≥ 1` страница говорит «✓ примет», игра может отказать | **P2** | `tactical/logic.js:185-213`, `tactical/tactical.js:999` |
| 2 | U1-013 | Баннер «Тот же раунд?», поднятый вводом точки вне планеты внизу панели, рендерится вверху панели вне прокрутки и без `aria-live` — пользователь его не видит | **major** | `tactical/tactical.js` (`renderPanel`, `actions.find`) |
| 3 | T1 | Docstring `ss14_tactical.py:13` заявляет Stories «verified», но `REVIEWED_BLOBS` хранит только блобы RMC-14 → 9 REVIEW у `stories_cm` (T1 продиффил: формулы идентичны, отличия — добавки Stories); страница `review` не показывает | P2 | `ss14_tactical.py:13, 115-135` |
| 4 | T1 | Ошибки разбора прототипов, незагруженные сущности и отсутствующие термины — только WARNING: стена из неразобранного файла молча выпадет из масок | P2 | `ss14_tactical.py:353-355, 681-683, 708-710, 545-546` |
| 5 | T1 | `RMCMortarShellFrag` (Stories) помечен `he` 3,87, но несёт 60 осколков (`ProjectileGrenade`) — зона поражения шире круга; поля в контракте нет | P2 | `ss14_tactical.py:814-820`, Stories `mortars.yml:146-164` |
| 6 | T1 (UNCERTAIN) | `cache_maps/` без снимка для ~83 карт 11 форков; скан текущего апстрима нашёл 11 карт с `id 0 ≠ Space` (rmc14/Amber, deltav/Academy, deadspace ×3, frontier/Amber, funky ×3, omu/Amber, carpmosia/Oasis) — их перепечка = смешение версий; владелец отложил | P2 | `maps/`, `cache_maps/` |

Заметные P3 (полный список — в приложениях): `feedback.js?v=4` только в `tactical.html:85` (T2); при несовпадении `h` селектор планеты расходится с картой, офлайн — тупик из-за `ignoreSearch` (`tactical.js:485-488`, `sw.js:86`); «243 - 226» теряет знак молча (`logic.js:77`); недостижимая ветка `errorMayUndercutMinRange` без теста (`logic.js:210`); `MAX_RADIUS` 30 vs миграция 0…100; hover в режиме переноса миномёта считает дальность от старой позиции (`tactical.js:861-868`); сообщения «Найти»/«Позиция» без `aria-live`; три `aria-label` без перевода; сравнение `constants` с `MIRROR` в CI пропускается (на раннере нет PyYAML/Pillow); Sorokyne — 0 тайлов `landingZone` на обоих форках (апстрим флаг не ставит, игра читает то же поле — данные честны, игроков удивит); UI-слой `tactical.js` (1500 строк) без автотестов. U1 minor: режимные подсказки только в панели, `#tacStatus` на карте пуст; три числа в поле — пустая подсказка и молча неактивная кнопка; после Enter фокус теряется; масштаб не переживает F5; «Новый раунд» без подтверждения; цели касания 26–36 px, чекбоксы 13 px, hover-подсказка закрывает 43 % карты на 375 px; дубль «Откалибровать заново» после ✗; два «High explosive» у Stories (исправлено в `cf388d2`? — нет: подпись с именем прототипа добавлена в `aa7aea5`, U1 тестировал сборку до неё — проверить); тайл до калибровки показывается в карточке «Цель»; Stories на EN даёт «ДОЛГОТА/ШИРОТА» без пояснения — выглядит как недоперевод.

---

# ЧАСТЬ II: RUNTIME UX-ТЕСТ (U1, Browser pane)

Проверено: RU десктоп 1535×710 — все 12 потоков сценария (первый заход, калибровка со сверкой ✗→«Другой тайл»→✓, неверный ввод, миномёт с карты и по координатам, цель и наведение, «Найти» вне планеты, копирование (Clipboard API в панели работает), слои/снаряд/радиус с перезагрузкой, «Тот же раунд?» → Да / Новый раунд, клавиатура, карточка «Разделов»); mobile 375×812; EN с восстановленным состоянием; Space Stories на EN.
Не проверено: реальный Ctrl+V из системного буфера, панорама мышью и колесо, две вкладки, 20-минутный простой, «Сверить тайлом» из баннера, «Показать на карте», другие планеты и предупреждение ±500, тач-события, скринридер.

---

# ЧАСТЬ IV: GIT

Ветка `main`, рабочее дерево чисто (кроме нетрекаемых `promo/shot-*.png`, `logo*.png`, `promo/steam-guide-*` другой сессии). Не отправлено: `24c912f`, `aa7aea5`, `662f2bc`, `cf388d2`. Worktrees: 3 старых (`colonial-marines-universe`, `ss14-resource-finder`, `substance-obtainability-items`) — по-прежнему п.5 репо-гигиены базового аудита.

---

# ЧАСТЬ VI: РЕКОМЕНДАЦИИ

**Сразу после аудита (Tier 1, один коммит):**
1. `logic.js` `mortarFireChecks`: предупреждение `errorMayHitRefusedArea` обходом прямоугольника ошибки (без дрожания — валидация в игре до него) + тест; текст в карточке цели.
2. `tactical.js`: баннер «Тот же раунд?» с `role="alert"`, при появлении из-за точки вне планеты — `scrollIntoView` (U1-013).
3. `feedback.js?v=4` в `index.html`, `library.html`, `sw.js` (PRECACHE) + `CACHE chemdb-v105`.
4. `deploy.yml`: `pip install pyyaml pillow` перед `check_tactical.py`, чтобы сравнение с `MIRROR` шло и в CI.

**В следующие инкременты серии T:**
- T5: поле осколков у снаряда (`shards`) и подпись «плюс осколки» у Frag; «Упало здесь» уже сравнивает с `impactBox`.
- T8 (повторная сверка кода Stories/CMU): закрыть REVIEW-список Stories блобами, докстринг `ss14_tactical.py:13`; политика «WARNING → ошибка» для неразобранных файлов прототипов.
- T9/T11: масштаб и центр в `sessionStorage`; hover-подсказка на 375 px — компактный режим.
- Backlog серии Q (базовый аудит): preflight `?v=`↔PRECACHE↔CACHE и runtime-fetch ↔ cp-list — рост поверхности в серии T делает Q5 приоритетнее.

---

# ЧАСТЬ VII: ИТОГО

| | |
|---|---|
| Файлы в скоупе | 29 рукописных + 53 файла данных |
| Покрытие | **100 %** — каждый рукописный файл скоупа прочитан ≥1 агентом (T1: пайплайн, CI, данные скриптом; T2: все файлы страницы и общие; U1: runtime), данные — инвариантами по всем 20 планетам и шести перепечённым картам |
| P0 / P1 | 0 / 0 |
| P2 | 6 (2 в коде страницы/экстрактора, 3 в данных/политике экстрактора, 1 — кеш карт) |
| Ключевые риски | ручные инварианты версий (уже разошлись на `feedback.js?v=`); проверка разрешений на точке прицела вместо точки прилёта; правила CMU/Stories прочитаны по коду, в раунде не наблюдались (контрольный раунд владельца до анонса) |

# ЧАСТЬ VIII: ЖУРНАЛ

- 17:36 — старт, скоуп из `git diff 81416cc..HEAD`, три агента параллельно (T1, T2, U1), браузер занят U1, оркестратор — дельта к базовому аудиту.
- 17:49 — U1 завершил (67 действий в браузере, viewport сброшен); 17:50 — T1 (в т.ч. `--verify` OK, `gh run view 34762126810`); 17:51 — T2.
- 17:55 — самопроверка покрытия по секциям «Покрытие» трёх отчётов; конфликтов между отделами нет; war-room не требуется.
- 18:05 — отчёт; исправления п. 1–4 Части VI — следующим коммитом.
