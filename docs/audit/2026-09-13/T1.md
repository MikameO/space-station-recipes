# T1 — БД и целостность данных (быстрый инкрементальный аудит, Серия T + фикс карт)

**Дата:** 2026-09-13 · **HEAD:** cf388d2 · **Диапазон:** 81416cc..cf388d2 · **Фрейминг:** adversarial DBA («если инвариант не в схеме — данные будут испорчены»)
**Домен:** детерминизм и воспроизводимость (SHA, `h`, `--verify`), контракт данных `tactical/`, политика отказа экстрактора, зеркало констант и review-list, CI-проводка, фикс `ss14_map_extractor.py` и перепечка шести карт, кеши и регенерация, секреты.
**Режим:** только чтение. Ничего не коммитил и не правил; `--selfcheck`/`--verify` ордананса не запускал. Запускал: `python scripts/check_tactical.py`, `node scripts/test_tactical_logic.js`, `python scripts/check_sections.py --no-git`, **`python ss14_tactical.py --verify`** (сборка во временный каталог; результат `verify: OK`, exit 0; `git status` области после запуска чист), `gh run view` лога CI-прогона 34762126810 (коммит 7eff175), собственные скрипты анализа в scratchpad (инварианты 20 планетных файлов через PIL, глубокое сравнение шести перепечённых карт по `git show`).

**Типы свидетельств:** `code_verified` — прочитано в коде/данных; `runtime_verified` — получено запуском проверки, скрипта или из лога CI.

**Итог:** FUNCTIONAL 28 · PARTIAL 7 · BROKEN 0 · MISSING 0 · UNCERTAIN 2. P0 — 0, P1 — 0, P2 — 4, P3 — 5.

---

## 1. Детерминизм и воспроизводимость

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 1.1 | Закрепление SHA коммита на форк | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:284-295`; `scripts/check_tactical.py:163`; `tactical/index.json` (`source.sha`) | `Checkout.pin` делает `fetch --depth 1 --filter=blob:none origin <sha>` и падает, если `FETCH_HEAD` не начинается с запрошенного SHA (292-293); CI требует полный 40-hex SHA | rmc14 `57112b96…`, stories_cm `024d853a…` записаны; `--verify` пересобрал оба форка ровно на этих SHA | — | — |
| 1.2 | Хеш содержимого `h` | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:766-767`; `scripts/check_tactical.py:126-130` | `h = sha1(JSON без h + PNG)[:12]`, дублируется в файле планеты и в индексе; CI пересчитывает из закоммиченных байтов и сверяет оба места | 20/20 планет: `check_tactical` «tactical data OK» локально и в CI | — | — |
| 1.3 | `--verify` (пересборка во временный каталог) | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:937-960` | Пересборка каждого форка на записанном SHA, побайтовое сравнение JSON+PNG и полей `family/locale/source/constants/terms/planets` | Запущено в этой сессии: `verify: OK`, exit 0; `golden ok` на LV-624 у обоих форков; область в `git status` не изменилась | Поле `review` намеренно не сравнивается (947) — см. 4.2 | Добавить в ROADMAP/README фразу «--verify нужен сетевой git» — уже есть в docstring 38-39 |
| 1.4 | Детерминизм порядка обхода | FUNCTIONAL | HIGH | — | code_verified | `ss14_tactical.py:316` (файлы sorted), `:718` (area_ids sorted), `:754` (labels sort), `:829` (shells sorted), `:904` (порядок планет = порядок обнаружения из отсортированных файлов) | Все коллекции, влияющие на байты, упорядочены явно; «последний закреплённый MinimapColor побеждает» (703) детерминирован порядком сущностей в YAML | Подтверждено побайтовым совпадением при `--verify` | — | — |
| 1.5 | Воспроизводимость PNG между окружениями | PARTIAL | MEDIUM | P3 | code_verified | `ss14_tactical.py:742` (`img.save(..., optimize=True)`); локально Pillow 12.1.1 / Python 3.14.3 | Байты PNG (а значит `h` и результат `--verify`) зависят от версии Pillow/zlib. `check_tactical` в CI сверяет `h` с закоммиченными байтами, поэтому деплой не пострадает; но `--verify` на другой машине даст ложные `MISMATCH …png` | На одной машине воспроизводимо (1.3) | Кросс-версионная воспроизводимость не доказана и нигде не зафиксирована | В `--verify` сравнивать PNG по декодированным пикселям (PIL `Image.open(...).tobytes()`), а байты — только как предупреждение; либо зафиксировать версию Pillow в docstring/`requirements` |
| 1.6 | `write_index` при устаревшем ключе форка | PARTIAL | HIGH | P3 | code_verified | `ss14_tactical.py:922-923` | `order.index(f["key"])` бросит `ValueError` (не `TacticalError` → трассировка вместо `ERROR:`), если в `index.json` остался форк, удалённый из `TACTICAL_FORKS` | Пока форки только добавляются | Некрасивое падение при удалении форка | `sort(key=lambda f: order.index(f["key"]) if f["key"] in order else len(order))` или отбрасывать неизвестные форки с предупреждением |

## 2. Контракт данных (`docs/design/2026-09-13-tactical-map.md:316-346`)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 2.1 | RLE-строки сетки и масок покрывают ширину | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:640-652`; `scripts/check_tactical.py:60-75,118-120`; `tactical/logic.js:88-100` | `rle_rows` всегда закрывает строку последней парой; CI проверяет число строк = высоте, сумму длин = ширине, значения в `0..len(areas)`; страница бросает исключение при недоборе | 20/20 планет × (grid + 2 маски) — ok в CI и локально | — | — |
| 2.2 | Биты флагов зон | FUNCTIONAL | HIGH | — | code_verified + runtime_verified | `ss14_tactical.py:239-240,723`; спека `:338`; `check_tactical.py:35`; `logic.js:14`; кеш `Content.Shared/_RMC14/Areas/AreaComponent.cs` (`[DataField("CAS")]`, `[DataField("OB")]`, остальные — camelCase по умолчанию) | Ключи `OB/CAS/mortarFire/mortarPlacement/supplyDrop/landingZone/lasing` совпадают с именами DataField; все bool-флаги в C# по умолчанию `false`, поэтому `fields.get(key)` (отсутствует ⇒ 0) семантически верен; `area_fields` сливает по цепочке родителей (407-414) | Эталоны LV-624 (OB 38 850, CAS 19 392, MortarFire 19 381, MortarPlacement 15 982, SupplyDrop 19 381) совпали на обоих форках | — | — |
| 2.3 | `bounds` ↔ размер PNG, пиксель (0,0) = (`minX`,`maxY`) | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:740,745-746`; спека `:337`; `check_tactical.py:105-109` (IHDR); `tactical.js:490-491` (`naturalWidth/Height`) | Экстрактор пишет `px[x-minX, maxY-y]`; CI читает IHDR без Pillow; страница сверяет размер загруженной картинки | Мой PIL-прогон: 20/20 PNG = `bounds`; число непрозрачных пикселей = числу тайлов сетки у 18 планет (см. 2.4 про Sorokyne) | — | — |
| 2.4 | Семантика `0` в `grid` («нет тайла») | PARTIAL | HIGH | P3 | runtime_verified | `ss14_tactical.py:748-749`; спека `:339`; `logic.js:121-126` | Экстрактор пишет `0` и для отсутствующего тайла, и для тайла без зоны. На Sorokyne (оба форка) 44 288 закрашенных пикселей против 43 414 тайлов с индексом зоны — **874 тайла есть, но зоны нет**. Страница трактует `0` как «нет зоны» → отказ `notArea`, что совпадает с игрой (`rmc-mortar-target-not-area`) | Поведение страницы верно; `check_tactical` не может сверить эталон `tiles` (в его GOLDEN его нет, в GOLDEN экстрактора — есть) | Формулировка спеки неточна; число тайлов не проверяется офлайн | Уточнить спеку `:339` («0 — нет тайла или тайл без зоны»); добавить в JSON планеты поле `tiles` (или проверять `png_opaque` в `check_tactical` по IHDR+IDAT — сложнее), чтобы эталон `tiles: 38850` жил и в CI |
| 2.5 | Таблица зон `[proto, name, color, flags]` | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:718-731`; `check_tactical.py:112-117` | `color` может быть `null` (у Solaris 41 из 63 зон без `minimapColor`), CI это допускает; все индексы зон используются (unusedAreas = 0 на 20 планетах) | Формат и диапазоны проверены | Спека не говорит, что `color` бывает `null` — мелочь | Дописать «`color` — `null`, если у зоны нет `minimapColor`» |
| 2.6 | Подписи `[text, x, y]` в мировых координатах | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:694-695,754`; `check_tactical.py:121-125` | Координаты округлены до 2 знаков, отсортированы | Все подписи всех 20 планет внутри `bounds` (мой прогон); LV-624: 21 = эталон | — | — |
| 2.7 | `constants.shells` — радиус HE по зеркалу `ExplosionSystem.IntensityToRadius` | FUNCTIONAL | HIGH | — | code_verified + runtime_verified | `ss14_tactical.py:793-802,805-829`; спека `:329`; кеш rmc14 `Content.Server/Explosion/EntitySystems/ExplosionSystem.cs:214-228` (`RadiusToIntensity` конус `:198`); stories `:228-242,212` | Зеркало повторяет C#: `r0 = max/slope`, `v0 = slope·π/3·r0³`, конус `∛(3T/(slope·π))` при `T ≤ v0`, иначе усечённый конус `r0·(√(12T/v0−3)/6+0,5)`. Пересчитал вручную: HE 800/5/30,1 → 5,35; Frag 96/1,6/4,8 → 3,87 (ветка усечённого конуса) — совпадает с индексом. Формулы в блобах RMC14 и Stories идентичны | Значения в индексе; CI требует хотя бы один `he` с радиусом (`check_tactical.py:170-175`) | Безобидное отличие: Python возвращает конус при `max_intensity ≤ 0` (`:800`), где C# дал бы NaN — для реальных снарядов не достижимо | — |
| 2.8 | Осколочный снаряд Stories (`RMCMortarShellFrag`) в `shells` | PARTIAL | MEDIUM | P2 | code_verified | `ss14_tactical.py:814-820`; `tactical/index.json` stories `shells[0]`; кеш stories `Resources/Prototypes/_RMC14/Entities/Objects/Weapons/mortars.yml:146-164` (`ProjectileGrenade`, `fillPrototype: RMCMortarShellFragShrapnel`, `capacity: 60`); stories `Content.Server/_RMC14/Mortar/MortarSystem.cs` (`OnProjectileGrenadeLand` → `_trigger.Trigger`) | Снаряд классифицирован как `he` с радиусом взрыва 3,87, но у него ещё 60 осколков-снарядов, срабатывающих при падении (только в Stories): реальная зона поражения шире круга взрыва, а данные этого не сообщают | Радиус взрыва верен | Круг у курсора занижает опасную зону Frag; контракт не имеет поля для осколков | Добавить в `shells[]` `fragments: {proto, count}` (или `kind: "frag"`), а страница пусть подписывает круг «взрыв; осколки дальше» |
| 2.9 | Полнота индекса: `source`, `review`, `terms`, `planets`, `file` своего форка | FUNCTIONAL | HIGH | — | runtime_verified | `check_tactical.py:93,163,181-187`; `tactical/index.json` | По 10 планет у каждого форка, `terms` 24/24 у обоих, `file = <fork>/<id>`, нет сирот и нет отсутствующих файлов в `tactical/` (мой прогон) | Всё в наличии | — | — |
| 2.10 | `landingZone` = 0 тайлов на Sorokyne (оба форка) | FUNCTIONAL | HIGH | — | code_verified | `tactical/rmc14/sorokyne.json`; кеш rmc14 `Resources/Prototypes/_RMC14/Entities/Areas/areas_strata.yml:79-95` (родители `…LandingZones` «Do not use.» задают только `minimapColor`), у `RMCAreaStrata…Lz1/Lz2/NearLz*` нет `landingZone: true`; игра читает то же статическое поле `area.Comp.LandingZone` (`Content.Server/_RMC14/Mortar/MortarSystem.cs:180-183`); Stories `areas_strata.yml` — 0 вхождений | В карте `sorokyne.yml` 1 970 тайлов лежат в зонах LZ, но апстрим не ставит флаг — значит и игра не откажет «нельзя бомбить LZ» на Sorokyne. Экстрактор отражает апстрим честно | Данные верны источнику | Ничего не сломано; поведение удивит игроков | Пометить в спеке/на странице как известную особенность апстрима; при T7/T5 не «чинить» данными вручную |
| 2.11 | Термины форка (`terms`) | FUNCTIONAL | HIGH | — | runtime_verified | `ss14_tactical.py:200-226,531-547`; `tactical/index.json` | Stories: `refuseTooFar` = «…слишком близко…» и `overwatchLatitude` = «ШИРИНА» — ошибки самой локали форка; по решению 12 цитируются как есть | 24/24 ключа у обоих форков | Отсутствие ключа — только WARNING (см. 3.2) | — |
| 2.12 | Каждый форк владеет своими файлами | FUNCTIONAL | HIGH | — | runtime_verified | решение §2 (`docs/decisions/2026-09-13_tactical-map.md:32-37`); `check_tactical.py:93` | rmc14 и stories_cm различаются там, где различается апстрим: grid у Solaris/Shiva/Sorokyne, маски у Shiva/Trijent, подписи у Fiorina; остальные 5 планет побайтово равны (мой прогон) | Нет кросс-форковых ссылок `file` | — | — |

## 3. Политика отказа экстрактора («лучше упасть, чем опубликовать неверное», спека `:308-310`)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 3.1 | Жёсткие отказы | FUNCTIONAL | HIGH | — | code_verified | `ss14_tactical.py:268` (git), `:292-293` (не тот SHA), `:309` (нет файла), `:383` (нет прототипа в цепочке), `:562` (путь карты), `:575` (дубли id), `:601-602` (tile id вне tilemap), `:607-608` (чанк ≠ 256), `:618,622,625` (MapGrid/AreaGrid/areas), `:636` (цвет), `:715` (нет прототипа тайла), `:471` (константы), `:828` (нет HE), `:861-862` (< 10 планет), `:896` (эталон) | Все пункты политики из спеки реализованы как `TacticalError` → `ERROR:` и exit 1 (`:976-978`) | Покрывает формат карты, прототипы, константы, эталоны | — | — |
| 3.2 | Мягкие места: только WARNING | PARTIAL | MEDIUM | P2 | code_verified + runtime_verified | `ss14_tactical.py:353-355,857-858` (файл прототипов не разобран), `:681-683,708-710` (прототип сущности не загружен → сущность пропущена), `:545-546` (нет строки локали) | Стена или дверь, чей файл прототипов не разобрался, молча выпадет из масок `blocked`/`hardWall` и из цветов; отсутствующий термин оставит пустое поле в `terms`, а `check_tactical` проверяет только «`terms` не пуст» (`:181`) | Сейчас: 0 ошибок разбора; незагружены только одежда/оружие (RMCWeaponShotgunHG3712 ×7 на LV-624/Kutjevo, RMCCoatPaP/RMCJumpsuitSPPPaP на Sorokyne — лог `--verify`) — на маски не влияют | Гарантии нет: следующий апстрим-коммит с YAML-ошибкой в файле стен пройдёт с предупреждением | Падать при `protos.errors`; падать, если незагруженный прототип закреплён (`anchored`) — именно такие формируют маски; в `check_tactical` требовать все 24 ключа `TERM_KEYS` |
| 3.3 | Эталоны только на исследовательских SHA | FUNCTIONAL | HIGH | — | code_verified | `ss14_tactical.py:230-237,879,891-897`; `check_tactical.py:27-34,131-140`; решение §11 | Эталон LV-624 сверяется, когда сборка идёт на `57112b967`/`024d853a1`; на новом SHA эталона нет — по решению (эталон от того же парсера ловит изменение, а не истину) | Обе таблицы GOLDEN согласованы между собой | — | При регене на новый SHA перенести эталоны вручную после осмотра |

## 4. Зеркало констант и review-list

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 4.1 | `FIELD_CHECKS` + CVar против C# на закреплённом SHA | FUNCTIONAL | HIGH | — | code_verified + runtime_verified | `ss14_tactical.py:139-193,431-472`; кеш rmc14 `MortarComponent.cs:21,24,45,48,51,54,57,93`, `TileFireOnTriggerComponent.cs:12`, `RMCCVars.cs:89` | 25 полей + `rmc.planet_coordinate_variance` читаются регэкспом `\bИмя\s*=\s*…;` (первое вхождение в своём файле; `\b` не даёт `Range` совпасть с `WarnRange`), парсятся `TimeSpan.FromSeconds`, массивы, int/float; расхождение — `TacticalError` | Прошло для обоих форков (иначе `--verify` не собрал бы); значения в кеше совпадают с `MIRROR` | — | — |
| 4.2 | Честность review-list для Stories | PARTIAL | HIGH | P2 | code_verified + runtime_verified | `ss14_tactical.py:13` (docstring: «space-stories-cm14 024d853, verified 2026-09-13»), `:115-135` (`REVIEWED_BLOBS` — только блобы RMC-14), `:832-838`; `tactical/index.json` stories_cm `review` — 9 файлов; `tactical/tactical.js` — поле `review` нигде не читается | Docstring заявляет, что Stories сверен, а список просмотренных блобов его не содержит → сборка честно печатает 9 REVIEW, но никто их не «закрыл». Я продиффил 9 блобов: **зеркальные формулы идентичны** (ошибка прицела `floor(|Δ|/TilesPerOffset)` — `SharedMortarSystem.cs:177-180` vs `:205-208`; разброс ОБ `Next(-3, 3)` — `OrbitalCannonSystem.cs:707-708` vs `:724-725`; `IsRoofed` — идентичен; `IntensityToRadius` — идентичен; `SupplyDrop` — только форматирование); отличия — добавки Stories (анти-гриф кадетов, `OrdnanceCasing`-снаряды, `ProjectileGrenade` при падении, `XenoHiveSetupRestriction`, CVars техники) | Данные Stories корректны; механизм REVIEW работает как задумано | Список не закрыт человеком, docstring противоречит `REVIEWED_BLOBS`; страница не показывает игроку, что формулы форка не сверены | После чтения добавить 9 блобов Stories в `REVIEWED_BLOBS` (или исправить docstring: «константы сверены, формулы — pending»); показывать бейдж «формулы форка не сверены» при непустом `review` |
| 4.3 | Сверка `constants` индекса с `MIRROR` в CI | PARTIAL | HIGH | P3 | runtime_verified | `scripts/check_tactical.py:154-160,176-180`; лог CI 34762126810: «note mirror comparison skipped (ModuleNotFoundError: No module named 'PIL')» | Импорт `ss14_tactical` тянет Pillow/PyYAML, которых нет на раннере, поэтому сравнение `offsetVariance/mortar/ob` с зеркалом в CI молча пропускается; работает только локально | Локально сравнение проходит | В CI — нет; ручная правка `index.json` пройдёт деплой | Вынести `MIRROR` в модуль без зависимостей (`tactical_mirror.py`) или в `tactical/mirror.json`, либо `pip install pyyaml pillow` в шаге workflow; `MORTAR_KEYS` дополнить `tileFireRange` (`:36-37`) |
| 4.4 | Разброс ОБ `[-3, 2]` | FUNCTIONAL | HIGH | — | code_verified | `ss14_tactical.py:151`; кеш `OrbitalCannonSystem.cs:707-708` (`_random.Next(-3, 3)`, верхняя граница исключается) | Зеркало учитывает полуоткрытый интервал | Не в `FIELD_CHECKS` (формульный уровень — покрыт review-list) | — | — |

## 5. CI-проводка (`.github/workflows/deploy.yml`)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 5.1 | Шаг «Series T checks» до сборки `_site` | FUNCTIONAL | HIGH | — | runtime_verified | `deploy.yml:40-43` | `node scripts/test_tactical_logic.js` + `python scripts/check_tactical.py`, оба офлайн | CI 34762126810 (7eff175): «all tactical logic tests passed», «tactical data OK», оба LV-624 «matches the research numbers»; локально то же | Сравнение с зеркалом пропущено (4.3) | — |
| 5.2 | Все runtime-fetched файлы попадают в `_site` | FUNCTIONAL | HIGH | — | runtime_verified | `deploy.yml:48` (`tactical.html`), `:55` (`cp -r tactical _site/tactical`), `:56` (`promo/cards`), `:68` (страницы asset-guard); `tactical.js:420,482` (`tactical/index.json`, `<fork>/<planet>.json|png?h=`) | Индекс и 40 планетных файлов лежат внутри копируемого каталога; `check_tactical.py:96-99` до сборки требует наличия файлов для каждой записи индекса — «белый список» для данных не нужен; asset-guard: «52 references checked across 3 pages, all present» | Нет пробела из памяти «runtime-fetched JSON needs curl check» | — | — |
| 5.3 | `sections.json` / карточка `tactical` | FUNCTIONAL | HIGH | — | runtime_verified | `deploy.yml:29`; `scripts/check_sections.py --no-git` → «12 sections, 0 errors» | Скриншот `promo/cards/tactical.webp` существует и копируется | — | — | — |
| 5.4 | `sw.js` | FUNCTIONAL | HIGH | — | code_verified | `git diff 81416cc..HEAD -- sw.js` (только `CACHE` v103→v104 и версии других серий) | Файлы `tactical/` не прекешируются — по решению §4 | — | — | — |

## 6. Фикс `ss14_map_extractor.py` (7eff175) и перепечка шести карт

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 6.1 | Пустота тайла по имени `Space`, неизвестный id — ошибка | FUNCTIONAL | HIGH | — | code_verified | `ss14_map_extractor.py:667-696` (новые `:671-674`, `:690-693`) | `tilemap` приводится к `{int: str}`; `tile_name is None` → `RuntimeError`; пропуск только при `== "Space"`. Тот же контракт продублирован в `ss14_tactical.parse_map:584-606` | Устраняет закраску целых чанков космоса и потерю пола на id 0 | — | Вынести общий разбор `tilemap` в одну функцию, чтобы два экстрактора не разошлись |
| 6.2 | Перепечка: только PNG и `bounds` | FUNCTIONAL | HIGH | — | runtime_verified | `git show 7eff175^:… / 7eff175:…` для `maps/{starlight/StarlightCluster, starlight/StarlightCog, trauma/Amber, trauma/Cog, monolith/Amber, monolith/Core}.json`, `maps/index.json` | Глубокое сравнение JSON без `bounds` — идентичны у всех шести (items/beacons/vias/offgrid: Cluster 2198/63/144/0, Cog 2637/87/205/0, trauma Amber 2427/68/180/0, trauma Cog 2371/80/181/0, monolith Amber 2049/65/171/1, Core 2007/71/136/1); в `maps/index.json` изменились только `px` этих шести (114 карт до и после) | Заявление коммита «byte-for-byte» подтверждено для JSON | Пиксели PNG независимо не перевыведены (`--selfcheck` запрещён: переписывает `maps/vanilla/Bagel.*`) | — |

## 7. Кеши, регенерация, секреты

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 7.1 | Снимок `cache_maps/` для остальных 108 карт | UNCERTAIN | MEDIUM | P2 | runtime_verified | `cache_maps/<fork>/Resources/Maps/**.yml` (mtime) vs `git log -1 --format=%cs -- maps/<fork>/` | YAML карт есть под всеми 15 форками, но у 11 форков (vanilla, omu, rmc14, cmu, rucm, goob, deltav, deadspace, frontier, funky, carpmosia) файлы датированы 2026-09-13, а их запечка — 2026-07-28: снимок, из которого пекли, перезаписан. Снимок 2026-07-23 сохранился только у starlight/trauma/monolith/misfits (29 карт) — это и есть «6 + 25 кешированных» из сообщения коммита; ~83 карты не имеют снимка, соответствующего запечке | Шесть карт перепечены из сохранённого снимка корректно | Неизвестно, есть ли среди 83 карт без снимка такие же (`id 0 ≠ Space`) — по запечённым данным это не определить (в JSON нет `tilemap`); их реген подтянет текущий апстрим с посторонним дрейфом предметов | Реген этих форков — только в worktree с диффом `items/beacons` (см. память «Worktree regen workflow»); при регене печатать `tilemap[0]` каждой карты, чтобы отделить эффект фикса от дрейфа |
| 7.2 | `cache_tactical/` — частичные клоны с закреплёнными SHA | FUNCTIONAL | HIGH | — | runtime_verified | `.gitignore:23-24`; `cache_tactical/{rmc14,stories_cm,cmu}/repo` (HEAD = 57112b96 / 024d853a / 9753baf6, `core.sparseCheckout=true`) | Кеш не отслеживается; `--verify` перепиновывает записанный SHA, поэтому устаревший или удалённый кеш — не риск корректности, только времени/сети | Подтверждено `--verify` | Разведочный клон `cmu` содержит только Sorokyne — вне выпуска | — |
| 7.3 | Секреты | FUNCTIONAL | HIGH | — | runtime_verified | `.gitignore:6` (`*.env`), `:24`; `git ls-files` (нет env/secret/token); grep по `ss14_tactical.py`, `check_tactical.py`, `deploy.yml`, `tactical/index.json` — только `id-token: write` (штатное разрешение Pages) | Ничего чувствительного не закоммичено; клон идёт по анонимному HTTPS без токена | — | — | — |
| 7.4 | Формулировка T1 в ROADMAP | PARTIAL | HIGH | P3 | code_verified | `ROADMAP.md:712` («raw по SHA, кеш блобов») vs `ss14_tactical.py:9-11`, спека `:232-237`, решение §1 | ROADMAP описывает первый черновик (raw-загрузка по SHA), реализован sparse partial clone | Суть (SHA на форк, кеш) верна | Термин «raw по SHA» вводит в заблуждение при следующем регене | Заменить на «sparse partial clone на SHA в `cache_tactical/`» |

## 8. DoD T1–T4 (данные) — сверка с `ROADMAP.md:711-721`

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 8.1 | T1: `--verify` зелёный; эталоны LV-624; 10 планет у каждого форка; отчёт разведки CMU | FUNCTIONAL | HIGH | — | runtime_verified | `ROADMAP.md:711-712`; лог `--verify` (эта сессия); `check_tactical`; спека `:278-296` | Все четыре пункта DoD подтверждены: `verify: OK`, `golden ok` ×2, 10+10 планет, отчёт разведки CMU (9753baf6d) в спеке | — | — | — |
| 8.2 | T2 (данные/деплой): `deploy.yml` — копирование, проверка ссылок, тесты | FUNCTIONAL | HIGH | — | runtime_verified | `ROADMAP.md:714-715`; `deploy.yml:40-43,48,55,68`; CI 34762126810 | Проверка ссылок и тесты зелёные в CI; все файлы `tactical/` в `_site` | — | — | — |
| 8.3 | T4 (данные): радиусы снарядов в `constants.shells`; слой = эталонные количества | FUNCTIONAL | HIGH | — | runtime_verified | `ROADMAP.md:720-721`; коммит 24c912f; `tactical/index.json` (`shells`); `tactical/rmc14/lv624.json` | HE 5,35 / Frag 3,87 / зажигательный 5 / осветительный `null`; в данных `mortarFire` 19 381 и `mortarPlacement` 15 982 = эталоны | Осколки Frag не отражены (2.8) | — |

---

## Покрытие

**Изучено (полностью):** `ss14_tactical.py` (982 строки), `scripts/check_tactical.py`, `.github/workflows/deploy.yml`, `tactical/index.json`, `docs/decisions/2026-09-13_tactical-map.md`, разделы спеки «Экстрактор», «Формат данных», «Верификация», «Серия T», «Риски» (`docs/design/2026-09-13-tactical-map.md:226-593`), `ROADMAP.md:707-751,781-783`, diff 7eff175 (`ss14_map_extractor.py:667-696`, шесть `maps/**.json`, `maps/index.json`), `git diff 81416cc..HEAD -- sw.js`, `.gitignore`, `config.py` (записи `rmc14`, `stories_cm`).
**Изучено (данные, скриптом):** все 20 файлов `tactical/{rmc14,stories_cm}/*.json|png` — инварианты RLE, bounds↔PNG, непрозрачные пиксели, подписи внутри границ, счётчики флагов/масок, попарное сравнение форков; сироты/отсутствующие файлы.
**Изучено (C# и YAML в `cache_tactical/`):** `ExplosionSystem.cs` (RMC14 и Stories), `AreaComponent.cs`, `AreaSystem.cs` (`IsRoofed`), `MortarComponent.cs`, `SharedMortarSystem.cs`, `MortarSystem.cs`, `OrbitalCannonSystem.cs`, `SharedSupplyDropSystem.cs`, `RMCCVars.cs`, `TileFireOnTriggerComponent.cs`, `areas_strata.yml`, Stories `mortars.yml`.
**Изучено (частично, только точки контракта):** `tactical/tactical.js:395-510` (загрузка индекса/планеты, `?h=`, проверка `h` и размера PNG), `tactical/logic.js:14,85-130` (флаги, RLE, `areaAt`).
**Запущено:** `check_tactical.py`, `test_tactical_logic.js`, `check_sections.py --no-git`, `ss14_tactical.py --verify` (OK), `gh run view 34762126810 --log`.
**Пропущено с причиной:** `tactical/tactical.js` целиком, `mapview.js`, `tactical.css`, `tactical.html` (UI — вне домена T1); содержание тестов `scripts/test_tactical_logic.js` (только запуск, логика — домен T2); пиксельная перевыверка шести PNG карт (`--selfcheck` запрещён — переписывает отслеживаемые файлы); разведочный клон `cache_tactical/cmu` (нет выпуска, T8); точный список карт без снимка в `cache_maps/` (по id не сопоставить без прогона экстрактора карт — см. 7.1 UNCERTAIN).
