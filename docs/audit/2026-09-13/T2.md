# T2 — API & Business Logic + security (quick audit, Серия T «Тактическая карта»)

- **Дата:** 2026-09-13 · **HEAD:** cf388d2 · **Scope:** коммиты 3b54afc, 1c85029, aa7aea5, cf388d2 + shared-файлы
- **Framing:** penetration tester (gray-box) + first-principles: инструмент обещает «числа как в коде игры» — проверяю каждую формулу против апстрима, каждый ввод как attack surface, каждый тихий catch как укрытие.
- **Метод:** только чтение кода и безопасные проверки. `node scripts/test_tactical_logic.js` — 36/36 ok, exit 0; `test_home_logic.js`, `test_feedback_logic.js` — all passed; `node --check` на logic.js, mapview.js, tactical.js, home.js, feedback.js, sw.js — чисто. Рабочее дерево по файлам scope без незакоммиченных правок (`git diff HEAD --stat` пуст).
- **Ключевой источник истины:** локальный кеш апстрима `cache_tactical/rmc14/repo` (RMC14 @ 57112b9, gitignored) — формулы сверены с `Content.Server/_RMC14/Mortar/MortarSystem.cs`, `Content.Shared/_RMC14/Mortar/SharedMortarSystem.cs`, `MortarComponent.cs`, `Rangefinder/RangefinderSystem.cs`, `Areas/AreaSystem.cs`, `Rules/RMCPlanetSystem.cs`. Ниже «игра:» = ссылка на эти файлы.
- **Evidence Type:** `code_verified` — прочитан код/тест; `test_verified` — подтверждено node-тестом; `game_verified` — сверено с кодом апстрима. Браузерных (runtime) проверок у этого агента нет — они у UX-агента.

**Итог (79 пунктов):** FUNCTIONAL 55 · PARTIAL 19 · MISSING 2 (оба — запланированные инкременты T5–T7 и T12) · BROKEN 0 · UNCERTAIN 3. P0 — 0, P1 — 0, P2 — 1, P3 — 23.

Самая весомая находка — #8 (P2): проверка разрешений зоны выполняется на точке прицела, тогда как игра проверяет точку `цель + ошибка + смещение`; у кромки зоны посадки/накрытой зоны страница говорит «✓ примет», а игра может отказать после переброса ошибки. Остальное — P3: рассинхрон `?v=` у `feedback.js`, рассинхрон селектора планеты при `h`-mismatch, недостижимая ветка предупреждения, потеря знака при «243 - 226», нюансы a11y/i18n.

---

## 1. Математика против механики игры (`tactical/logic.js`)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | Мир ↔ игра: `worldToGame`, `gameToWorld`, `offsetFrom` | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:20-25; test:88-93 | Дальномер показывает `floor(центр тайла) + сдвиг` (игра: RangefinderSystem.cs:187-189 `Position.Floored()` + `TryGetOffset`), поэтому индекс тайла + сдвиг = показание. | Пара мир `31 −78` ↔ игра `243 −226` → сдвиг `212 −148` и обратно. | — | — |
| 2 | Сдвиг раунда в ±500 (`calibrationIssues`) | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:27-31; test:95-98 | Игра: RMCPlanetSystem.cs:64-66 `_random.Next(-v, v+1)` — границы включительно; код флагует только `> variance`. | ±500 принимается, 501 — `outOfVariance`. | — | — |
| 3 | Дискриминирующий проверочный тайл (`isDiscriminating`, `CHECK_STEPS`, `pickCheckTile`) | FUNCTIONAL | HIGH | — | code_verified, test_verified | logic.js:36-57, 361-371; test:100-121, 338-359 | Требует `dx≠0, dy≠0, |dx|≠|dy|`; кандидаты в радиусе 5, ближние первыми; пропуск стен, вне планеты, уже пробованных. Перестановка X/Y ловится тогда и только тогда, когда `dx≠dy`; условие `|dx|≠|dy|` строже необходимого (исключает и `dx=−dy`), вреда нет — кандидатов достаточно. | Тест: переставленное показание даёт ожидаемые числа, не совпадающие с дальномером даже при обратном чтении. | — | — |
| 4 | Дальность: центр тайла миномёта → целочисленная точка цели, строгие неравенства | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:142-149, 190-191; test:179-194 | Игра: MortarSystem.cs:148-156 `distance < MinimumRange` → too close, `distance > MaximumRange` → too far; позиция — `GetMapCoordinates(mortar).Position` закреплённой сущности (центр тайла). Код: `target − (tile + 0.5)`, `<`/`>`. | Спан `M−64…M+65` при 65; `M+15` — too close, `M−15` — ok; CMU 8/165 по константам форка. | — | — |
| 5 | Ошибка прицела `⌊|Δ|/20⌋` по оси, зона без ошибки `M−19…M+20` | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:151-154, 170-175; test:196-204 | Игра: SharedMortarSystem.cs:170-180 — `offset = target − planetOffset`, `xOffset = (int)Floor(Abs(offset.X − position.X) / tilesPer)`, `Offset = (Next(−x, x+1), …)` — включительно `[−e, e]`. Код повторяет дословно от центра тайла. | Макет: миномёт `20 −98`, цель `62 −62` → `[2, 1]`; `40` → 0, `41` → 1; `1` → 0, `0` → 1. | — | — |
| 6 | Куда может лечь снаряд (`impactBox`): ±e + дрожание, лазер без всего | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:159-168; test:388-396 | Игра: MortarSystem.cs:197-209 `ApplyRandomOffset` — после валидации, `_random.Pick(FireRandomOffset)` независимо по осям; `FireRandomOffset = {−1, 0, 0, 1}` (MortarComponent.cs:93); в лазерном режиме пропускается. Код: `min/max` дрожания по осям, лазер → `[0,0]` и `[0]`. | Бокс `59…65 × −64…−60` для макета; лазер — точка. | — | — |
| 7 | Порядок отказов `mortarFireChecks` (первый = попап игры) | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:185-200; tactical.js:867, 999; test:212-228 | Игра (MortarSystem.cs:126-195): планета → дальность (близко/далеко) → тайл не космос → есть зона → не LZ → `CanMortarFire`. Код: `tooClose, tooFar, noArea, landingZone, covered`; UI показывает `reasons[0]` — то, что игрок увидит в попапе. `noCas/noLasing` — требования целеуказателя (RangefinderSystem.cs:136-137). | LZ → `landingZone`; зона без `MortarFire` → `covered`. | — | — |
| 8 | Разрешения зоны проверяются на точке прицела, а игра — на `цель + ошибка + смещение` | PARTIAL | MEDIUM | **P2** | game_verified, code_verified | logic.js:185-213 (192-200 area у `target`; 201-211 — только дальность с ошибкой); tactical.js:999 «✓ Миномёт примет эту цель» | Игра: MortarSystem.cs:110-118 `target = Target + Offset + Dial; target −= planetOffset` → и только потом `ValidateTargetCoordinates` (LZ, `CanMortarFire`, космос) на смещённой точке. Страница проверяет флаги на точке прицела и предупреждает с учётом ошибки только о дальности. При `e ≥ 1` (≥ 20 тайлов по оси) цель в ≤ e+1 тайлах от границы LZ или накрытой зоны получает «✓ примет», хотя игра откажет с ненулевой вероятностью после переброса ошибки. Пунктирный контур поверх красной подсветки (tactical.js:684-713, 715-721) показывает это только визуально и только при включённых слоях. | Точка прицела, дальность с ошибкой. | Текстовое «✓» у кромки LZ/накрытой зоны может расходиться с игрой; частый боевой случай — цели у периметра LZ. | Добавить предупреждение `errorMayHitRefusedArea`: обход целых точек `impactBox` через `flagsAt` (LZ или нет `MortarFire`, либо `areaAt == null`) → warn с числом «плохих» точек; тест на синтетической планете с LZ-границей; строка в L10N en/ru. |
| 9 | Предупреждения по дальности с ошибкой (`errorMayExceedMaxRange` / `errorMayUndercutMinRange`) | PARTIAL | HIGH | P3 | code_verified, test_verified | logic.js:201-211; test:230-235 | Максимум/минимум расстояния по четырём углам бокса ошибки — корректно (мортира вне бокса при `e ≥ 1`, экстремумы в углах). Ветка `errorMayUndercutMinRange` недостижима при нынешних константах: `e ≥ 1` требует `|Δ| ≥ 20`, ближайшая точка ≥ 19 > minRange 15 (RMC) и 8 (CMU). Теста на неё нет. | `[63, 0]` → `bounds [3,0]`, `errorMayExceedMaxRange`. | Мёртвая ветка без теста; сработает лишь при `minRange ≥ 20` у будущего форка. | Оставить, но добавить тест с синтетическими константами (`minRange: 25`), чтобы ветка была под контролем. |
| 10 | Размещение миномёта (`placementCheck`) | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:177-181; test:206-210 | Игра: AreaSystem.cs:269-278 `CanMortarPlacement` = зона есть, не под крышей, `MortarPlacement`. Крыши раунда страница знать не может (задокументировано в спеке §Крайние случаи). | `indoors` / `noArea` с текстом отказа форка (tactical.js:975). | — | — |
| 11 | Лазерный режим: без ошибки/дрожания, нужны CAS + Lasing | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:188-199; test:220-228 | Игра: MortarSystem.cs:92-107 (цель = точка целеуказателя), 199-201 (нет дрожания); RangefinderSystem.cs:136-137 (`CanCAS`, `CanLase` в режиме Designator). | Логика верна; режим попадает в UI только через `mortar.mode` из хранилища (logic.js:453). | Переключателя режима в панели нет — план T5. | — (см. #14) |
| 12 | Варианты огня `fireVariants`, `estimateError`, `impactPlausible` | FUNCTIONAL | HIGH | — | code_verified, test_verified | logic.js:221-269; test:242-284 | `E = падение − цель − смещение`, `D = цель − цель_выстрела − E`, отсечка `±maxDial`; сценарий макета проходит (`+2 +1` → `−2 −1`; `280 −214` → `+4 −5`; `262 −196` → вне ±10). `Math.round(−0.5) = −0` → `signed`/`formatCoords` печатают «0» — безвредно. | Чистая логика готова к T5. | — | — |
| 13 | ОБ/поставка/крыши/таймлайны (`obScatterBox`, `obChecks`, `supplyChecks`, `roofedFlags`, `*Timeline`, `timelineState`) | FUNCTIONAL | HIGH | — | game_verified (roof), test_verified | logic.js:273-354; test:288-334 | `roofedFlags`: игра AreaSystem.cs:339-366 `IsRoofed` — `distance <= roof.Range` от точки до позиции сущности; код `≤ range` от целого угла к центру тайла. Разброс ОБ `(misfuel+1) × [−3…+2]`. | Все тесты зелёные. | Не подключено к UI — план T6/T7. | — |
| 14 | UI лазерного режима, карточка «Смещение», «Выстрел», «Упало здесь», ОБ/поставка | MISSING | HIGH | P3 | code_verified | tactical.js:945-953 (кнопки ОБ/поставка `disabled`, «в следующем обновлении»); ROADMAP T5–T7 | Запланированные инкременты; логика уже есть (#11-13), UI нет. Не дефект аудируемых коммитов. | Честная плашка «в следующем обновлении». | — | Вести по ROADMAP. |
| 15 | Сессия раунда (`calibrationState`, `newSession`, `noteInput`, `confirmSameRound`) | FUNCTIONAL | HIGH | — | code_verified, test_verified | logic.js:375-409; test:361-386 | 20 мин между вводами (не по visibility), перезагрузка (`fromStorage && !confirmed`), точка вне планеты; `checkMatch` подтверждает раунд (tactical.js:1156). | Все причины и сброс. | — | — |
| 16 | «Страница перезагружена» при возврате на планету в той же загрузке | PARTIAL | MEDIUM | P3 | code_verified | tactical.js:500 (`newSession(now(), !!state.store.calibration)` при каждой `loadPlanet`) | Сессия пересоздаётся при смене планеты, поэтому калибровка, сделанная минуту назад в этой же вкладке, после переключения планеты и обратно спрашивает «Тот же раунд?» с причиной `reload`. Вопрос уместен (другая планета — потенциально другой сервер), причина — нет. | Безопасное поведение (переспрос). | Текст причины неточен. | Хранить сессии по `storeKey` в `Map`, либо причина `planetSwitch` с отдельной строкой. |
| 17 | Лимит `MaxTarget` 1000 применяется к показаниям дальномера при калибровке | UNCERTAIN | MEDIUM | P3 | game_verified (частично) | tactical.js:533, 587-591; logic.js:82; игра SharedMortarSystem.cs:401-402 `args.Target.X.Cap(MaxTarget)` | Игра ограничивает только *цель* миномёта; семантика `Cap` (обрезка или отказ) в кеше не найдена. Страница отвергает `|v| > 1000` и для *калибровочного показания*, которое игра не ограничивает. Сейчас безопасно: max `|world| = 255` (sorokyne) + 500 = 755 < 1000 для всех 20 планет индекса. | 10 планет ротации × 2 форка укладываются. | Планета с `|bounds| > 500` сломает калибровку молча (`outOfRange`). | В `draftParse` использовать лимит `max(|bounds|) + offsetVariance` вместо `maxTarget`; `maxTarget` оставить для «Найти» и цели. Уточнить `Cap`. |

## 2. Разбор ввода

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 18 | Дефисы U+2212/2012/2013/2014/FE63/FF0D → ASCII | FUNCTIONAL | HIGH | — | test_verified | logic.js:67; tactical.js:28, 581; test:129-145 | Одна таблица в logic и в UI (`DASHES`). | «−100 200» → `[-100, 200]`. | — | — |
| 19 | `Para-Cam (X):(Y)` | FUNCTIONAL | HIGH | — | game_verified, test_verified | logic.js:69; игра SharedMortarSystem.cs:385-390 (`rmc-mortar-camera-name` с `(int)x`, `(int)y`) | Регэксп терпим к пробелам и регистру. | Тест проходит. | — | — |
| 20 | Локализованное имя камеры у Stories/RuCM | UNCERTAIN | LOW | P3 | code_verified | logic.js:69; tactical/index.json `terms` (нет ключа для имени камеры) | Имя камеры берётся из локали форка (`rmc-mortar-camera-name`); для ru-RU форков шаблон может отличаться от «Para-Cam». Актуально с T5 («Упало здесь»). | RMC14/CMU en-US. | Неизвестно для ru-RU. | Экстрактору вытащить `rmc-mortar-camera-name` в `terms`, парсеру — принимать префикс форка. |
| 21 | Метки ДОЛГОТА/ШИРОТА/LONGITUDE/LATITUDE/X/Y, «широта»/«ширина» | FUNCTIONAL | HIGH | — | test_verified | logic.js:72-73; test:134-138 | Обе метки → X/Y независимо от порядка. | — | — | — |
| 22 | «243 - 226» (пробел после минуса) теряет знак | PARTIAL | HIGH | P3 | code_verified | logic.js:77 (`-?\d+`), tactical.js:581 | `nums = ['243', '226']` → `y = 226` вместо `−226`, без отказа. Реальный ввод редкий (копия дальномера без пробела), но ошибка тихая и даёт ложный сдвиг раунда на сотни тайлов. | Обычные форматы. | Тихая потеря знака. | В `parseCoords` нормализовать `-\s+(\d)` → `-$1` до поиска чисел; тест. |
| 23 | Отказы `tooFew`/`tooMany`/`outOfRange` | FUNCTIONAL | HIGH | — | test_verified | logic.js:78, 82; test:147-151 | Более двух чисел без меток — отказ, а не догадка. | — | — | — |
| 24 | `draftParse`: оба числа в одном поле; вставка разбивает по полям | FUNCTIONAL | HIGH | — | code_verified | tactical.js:585-592, 1292-1303 | `nx ≥ 2 && ny = 0` → разбор одного поля; `paste` c ≥ 2 числами → `preventDefault` + заполнение обоих полей + `onCalInput`. | Логика ветвлений полна (`tooMany` при 2+1). | Runtime не проверен (UX-агент). | — |
| 25 | Enter в полях = действие с повторной валидацией | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1305-1311, 1131-1137, 1216-1222, 1236-1242 | `saveCalibration` сам проверяет `selected && p.ok` и пишет `calMessage` — обход `disabled` кнопки не ведёт к записи мусора. | — | — | — |
| 26 | Поле радиуса: `parseFloat` с запятой, `0…30`, пустое = сброс | PARTIAL | HIGH | P3 | code_verified | tactical.js:27, 1034, 1266-1273; logic.js:436 (`0…100`) | Границы UI (30) и миграции prefs (100) расходятся: сохранённое другой версией 50 пройдёт миграцию и нарисует круг в 50 тайлов, поле покажет 50 при `max=30`. | Ввод в UI ограничен. | Рассинхрон констант. | Вынести `MAX_RADIUS` в `TacticalLogic` и использовать в обоих местах. |

## 3. Безопасность DOM и хранилища

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 27 | `innerHTML` + `esc()` для данных форка (labels, planet names, `area[1]`, `terms.*`, shell names, `source.sha/date`) | FUNCTIONAL | HIGH | — | code_verified | tactical.js:273-277 (esc), 383, 434-436, 440-443, 832, 837-846, 864-874, 886-897, 901-921, 929-942, 947-952, 956-978, 984-1017, 1021-1038, 1065-1075 | Прошёл каждый `innerHTML`: все строки из JSON и все `fmt(...)` обёрнуты в `esc()`, включая атрибутные контексты (`value=`, `title=`, `<option value=>`); `esc` экранирует `& < > " '`. Внутренние `data-action`/`data-weapon`/`data-layer`/`id` — константы кода. В данных есть имена с `&` и `'` («Showers & Laundry», «Gonzo's hide-out») — экранируются. | XSS через форк-данные/термины невозможен. | — | — |
| 28 | `fmt()` подставляет сырые значения, `{…}` в имени снаряда | FUNCTIONAL | HIGH | — | code_verified | tactical.js:278-280, 1031 (`fmt(shellLabel(x), {name})`) | Результаты `fmt` всегда идут в `esc`/`textContent`. Имя снаряда с фигурными скобками было бы искажено `fmt` (не XSS, косметика). | — | — | `shellLabel` уже возвращает `s.name` для `other` — убрать внешний `fmt` (см. #61). |
| 29 | `localStorage`: `JSON.parse` в `try/catch`, строгая миграция | FUNCTIONAL | HIGH | — | code_verified, test_verified | tactical.js:297-317, 1483, 1491; logic.js:415-474; test:398-426 | `isTile` — целые конечные числа; согласованность `offset = reading − tile` и `expect = tile + offset` проверяется, иначе калибровка отбрасывается; `result ∈ {null, match, mismatch}`; `tried` фильтруется; `mode` нормализуется. Чужой `__proto__` в `hitRadius` — присваивание числа игнорируется движком, объекты отсекает `typeof`. | Мусор → пустое состояние без исключений. | `shots`/`markers` проходят как есть и переписываются обратно `saveStore()` (документировано, T5/T6). | При T5/T6 валидировать элементы массивов. |
| 30 | `c.at` не-конечное число → «NaN h NaN min ago» | PARTIAL | MEDIUM | P3 | code_verified | logic.js:457 (`typeof c.at === 'number'`), 408; tactical.js:574-578 | `NaN`/`Infinity` проходят проверку типа, возраст становится `NaN`; `formatAge(NaN)` печатает «NaN h NaN min». Только чужие данные. | Обычные числа. | Нечитаемая строка при порче хранилища. | `isFinite(c.at)` в миграции. |
| 31 | Событие `storage` из другой вкладки | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1478-1496 | Ключ сверяется с `PREFS_KEY`/`storeKey`; `newValue` → `JSON.parse` в `try` → миграция; свежая калибровка другой вкладки (`at` изменился) подтверждает раунд, `newRound` там (calibration → null) не подтверждает. `e.key === null` (clear) → игнор. | Синхронизация без доверия к формату. | — | — |
| 32 | Гонка: другая вкладка обнулила калибровку между рендером и кликом | PARTIAL | LOW | P3 | code_verified | tactical.js:1155, 1162, 1169, 1171 (`calibration().check…`, `calibration().tile`) | `actions.checkMatch/checkMismatch/showCheck/recalibrate` разыменовывают `calibration()` без проверки; после `storage`-события с `calibration: null` до перерисовки (она синхронна, окно — микрозадача) клик даст `TypeError`. Практически недостижимо. | — | Теоретический `TypeError`. | Guard `var c = calibration(); if (!c || !c.check) return;`. |
| 33 | `location.hash` и `history.replaceState` | FUNCTIONAL | HIGH | — | code_verified | tactical.js:389-398, 1443-1451 | Регэксп `[\w-]+`; ключи берутся из `index.json`, а не из хеша; неизвестный форк → первый. | — | — | — |
| 34 | Нет `eval`/`Function`, секретов; Metrika-параметры только fork/planet | FUNCTIONAL | HIGH | — | code_verified | tactical.js:284-293; grep по scope | Координаты и текст меток в аналитику не уходят (спека §Аналитика). `new Function` только в node-тесте. | — | — | — |
| 35 | `fetch` same-origin, относительные пути, `cache: 'no-cache'` | FUNCTIONAL | HIGH | — | code_verified | tactical.js:402-407, 420, 482 | Никаких пользовательских URL. | — | — | — |
| 36 | Текст на canvas (`label[0]`) | FUNCTIONAL | HIGH | — | code_verified | tactical.js:723-730 | `fillText`, не HTML. | — | — | — |

## 4. Обработка ошибок и загрузка данных

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 37 | `loadToken` против устаревших ответов | FUNCTIONAL | HIGH | — | code_verified | tactical.js:478, 483, 516 | Проверка в `then` и в `catch`; быстрая смена планет не подменяет состояние. | — | — | — |
| 38 | `index.json`: schemaVersion, retry, `console.error` | FUNCTIONAL | HIGH | — | code_verified | tactical.js:418-431 | Ошибка не глотается; кнопка «Повторить» пересоздаётся вместе с `innerHTML` (нет накопления слушателей). Пустой `forks[]` → `TypeError` в `then` → тот же `catch` → общее сообщение (приемлемо). | — | — | — |
| 39 | Планета: `h` mismatch → `staleData` без retry; селектор и карта расходятся | PARTIAL | MEDIUM | P3 | code_verified | tactical.js:485-488 (return без обновления `state.meta/planet/storeKey`, без `writeHash`); sw.js:86 (`ignoreSearch: true`) | `<select>` уже показывает новую планету, карта и `storeKey` остались от старой; нет кнопки повтора. Отдельный сценарий: офлайн SW отдаёт кешированный `<planet>.json?h=OLD` на `?h=NEW` → именно `staleData`, а совет «перезагрузите страницу» офлайн не поможет (офлайн не обещан — decision 4). | Явное сообщение, данные старой версии не подменяют новые. | Рассинхрон UI, тупик офлайн. | При `staleData` вернуть `els.planet.value = state.meta.id` (или дать retry) и упомянуть офлайн в тексте. |
| 40 | PNG размер ≠ `bounds` → ошибка с retry | FUNCTIONAL | HIGH | — | code_verified | tactical.js:490-492, 515-519 | `throw` внутри `then` → `catch` → `loadFailed` + retry. | — | — | — |
| 41 | Осознанные «тихие» catch: аналитика, probe/quota хранилища, `:focus-visible`, `setSelectionRange` | FUNCTIONAL | HIGH | — | code_verified | tactical.js:284-288, 297-316, 812-814, 1057 | Каждый прокомментирован и не скрывает пользовательских сбоев; при заблокированном хранилище — плашка `noStorage` (1067). | — | — | — |
| 42 | Копирование: Clipboard API → `execCommand` → выделение + toast | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1331-1373 | Отказ обоих путей заканчивается понятным сообщением и выделенными числами. Runtime — у UX-агента. | — | — | — |
| 43 | `f.source.sha.slice` при повреждённом индексе роняет всю панель | PARTIAL | LOW | P3 | code_verified | tactical.js:1075 | `renderPanel` — одна конкатенация; исключение здесь оставит панель пустой. Контракт защищён `scripts/check_tactical.py` в CI. | CI-гарантия. | Нет рантайм-защиты. | `String(f.source && f.source.sha || '').slice(0, 9)`. |

## 5. Сессия, слушатели, перерисовка, canvas

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 44 | Делегирование событий панели, `renderPanel` без навешивания слушателей | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1254-1311, 1061-1078 | Пять делегированных слушателей на `els.panel` один раз; `innerHTML` заменяет узлы без обработчиков — утечек нет. | — | — | — |
| 45 | Документные capture-слушатели ×4, `setInterval`, `ResizeObserver`/`resize` | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1468-1470, 1475-1476; mapview.js:227-228 | Регистрируются один раз на модуль, `passive: true`, не конфликтуют с `wheel {passive:false}` на canvas (mapview.js:171-176). | — | — | — |
| 46 | Сохранение фокуса/каретки и черновиков при перерисовке | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1046-1059, 1261-1265, 1087-1097 | `tick()` раз в 30 с перерисовывает панель при смене возраста; поля восстанавливаются из `state.draft/findDraft/mortarDraft`, которые синхронизируются на `input`. | Ввод не теряется. | Исключение — #47. | — |
| 47 | `tacRadius` (type=number) с промежуточным невалидным значением при `tick()` | PARTIAL | LOW | P3 | code_verified | tactical.js:1266-1273, 1034 | `el.value === ''` для «1.» → `delete hitRadius` → перерисовка подставит радиус прототипа посреди набора. Раз в минуту, редко. | — | Потеря промежуточного ввода. | Не перерисовывать панель, если `document.activeElement` — `tacRadius`; либо хранить `radiusDraft`. |
| 48 | Отложенная перерисовка после 20-мин паузы (`RENDER_AFTER_INPUT_MS`) | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1455-1467 | Клик, разбудивший страницу, успевает сработать до появления баннера; `pointerdown` на «Да, тот же раунд» не перерисовывает под курсором. | — | — | — |
| 49 | `tick` по `visibilitychange` | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1472-1476 | Возраст обновляется при возврате во вкладку. | — | — | — |
| 50 | Hover в режиме «Указать/переставить миномёт» показывает дальность от старой позиции | PARTIAL | MEDIUM | P3 | code_verified | tactical.js:861-868 | `renderHover` берёт `fireInfo(t)` при любом `pickMode`, тогда как `drawHoverZone` корректно ограничен `pickMode() === 'target'` (796). Во время переноса миномёта строка «✗ слишком близко» относится к стрельбе со старой позиции. | Зона у курсора не рисуется. | Вводящая в заблуждение строка. | `var info = weapon()==='mortar' && pickMode()==='target' ? fireInfo(t) : null`. |
| 51 | mapview: порог 4 px, pointer capture, `pointercancel`, `pointerleave` | FUNCTIONAL | HIGH | — | code_verified | mapview.js:15, 178-225 | Клик не срабатывает после перетаскивания/отмены; capture держит drag за пределами canvas; `pointerleave` во время drag не сбрасывает hover. | — | — | — |
| 52 | Геометрия tile ↔ screen, PNG (0,0) = (minX, maxY) | FUNCTIONAL | HIGH | — | code_verified | mapview.js:71-87; logic.js:115-119 | `worldToScreen(x, y+1)` — верхний левый угол тайла; `cellIndex` — строка `maxY − y`. Согласовано с `check_tactical.py` (bounds = размер PNG). | — | — | — |

## 6. Общие файлы (home.js, feedback.js, sections.json, deploy, карточки)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 53 | `data-no-autopopup` → `companion` в `decide()`/`ctxNow()` | FUNCTIONAL | HIGH | — | code_verified, test_verified (косвенно) | home.js:384; feedback.js:434; tactical.html:41; test_home_logic.js:85; test_feedback_logic.js:61 | Диф 3b54afc — одна строка в каждом файле: `|| document.body.hasAttribute('data-no-autopopup')`. На index/library атрибута нет → `companion` как прежде; чистая логика тестирует `companion: true` как blocked context. Ручные кнопки (`home.js:404 open('manual')`, `feedback.js:421-422`) не зависят от `decide`. Само DOM-сопоставление атрибута тестом не покрыто (только код). | Регрессии на других страницах нет. | — | — |
| 54 | Карточка `tactical` в `sections.json` | FUNCTIONAL | HIGH | — | code_verified | sections.json:58-63; cf388d2 (`promo/cards/tactical.webp` добавлен) | `target: page:tactical.html`, `added/updated` 2026-09-13, `whatsNew` en/ru, `inside` 4 пункта, `shot` существует. | — | — | — |
| 55 | `home.js` на `tactical.html`: `whenReady`, `pageName()`, `versioned()` | FUNCTIONAL | HIGH | — | code_verified | home.js:157-158, 212, 373, 396-399 | Нет `#loadingOverlay` → `decide` сразу (и блокируется #53); клик по карточке на самой странице — no-op; `ASSET_V` из `home.js?v=5` → `sections.json?v=5` = sw.js:28. | — | — | — |
| 56 | `scripts/shoot_cards.mjs`: seed хранилища | FUNCTIONAL | HIGH | — | code_verified | shoot_cards.mjs:52-73, 99 | Seed согласован с `migrateStorage`: `243−31 = 212`, `−226+78 = −148`; проверочный `[29,−79]` дискриминирующий, `expect [241,−227] = worldToGame` → миграция сохранит; ждёт `.tac-offset`, кликает «Да, тот же раунд». | — | Bounds LV-624 захардкожены (56) — дублируют `mapview.fit`; при пересборке планеты с другими bounds зум уйдёт мимо цели (только скриншот, P3). | Читать bounds из `tactical/rmc14/lv624.json` в prep. |
| 57 | `scripts/build_cards.py` | FUNCTIONAL | HIGH | — | code_verified | build_cards.py:24-35 | Выбор карточек по аргументам, проверка отсутствующих, порог 25 KB. | — | — | — |
| 58 | `deploy.yml`: копирование `tactical.html` + `tactical/`, asset-guard, Series T checks | FUNCTIONAL | HIGH | — | code_verified | .github/workflows/deploy.yml:39-43, 48, 55, 68 | `node scripts/test_tactical_logic.js` + `python scripts/check_tactical.py` до сборки; страница в списке проверки ссылок. | — | — | — |
| 59 | Цели Метрики `tactical_*` в реестре | MISSING | HIGH | P3 | code_verified | tactical.js:513, 1116, 1124, 1151, 1158, 1164, 1182, 1200, 1363; ROADMAP T12 | Девять `reachGoal` без регистрации на счётчике — план T12 «по ок владельца» (память: export/goals только с явного разрешения). | Вызовы безопасны (`try`). | Цели не считаются до T12. | По плану. |

## 7. Кеш-бастинг

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 60 | `home.js?v=5`, `sections.json?v=5`, `style.css?v=77`, `i18n.js?v=44`, `CACHE chemdb-v104` | FUNCTIONAL | HIGH | — | code_verified | index.html:859; library.html:83; tactical.html:26, 80, 84; sw.js:6, 14-15, 23, 28 | Все три страницы и PRECACHE согласованы; бамп CACHE в cf388d2 сбрасывает runtime-кеш. | — | — | — |
| 61 | `feedback.js`: `?v=4` в tactical.html против `?v=3` в index.html, library.html, sw.js | PARTIAL | HIGH | P3 | code_verified | tactical.html:85 vs index.html:860, library.html:84, sw.js:24; `git log -S'feedback.js?v=4'` → только 3b54afc | `feedback.js` изменён в 3b54afc, но версия поднята только на новой странице. Функционально безвредно (изменение действует лишь при атрибуте), но нарушает правило «бампать вместе» (память проекта) — на index/library HTTP-кеш держит старый файл до истечения `max-age` Pages. | SW network-first онлайн отдаёт свежий файл. | Рассинхрон версий трёх страниц и PRECACHE. | Поднять до `?v=4` в index.html, library.html, sw.js одним коммитом (или вернуть v=3 в tactical.html). |
| 62 | `tactical/*.js?v=`, `tactical.css?v=` вне PRECACHE; планеты `?h=` | FUNCTIONAL | HIGH | — | code_verified | tactical.html:27, 81-83; tactical.js:482; sw.js:11-30 | По decision 4: runtime-кеш network-first; первый офлайн-заход невозможен и не обещан. `?h=` + `cache:'no-cache'` исключают устаревшие планеты онлайн. | — | — | — |

## 8. Доступность (уровень кода)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 63 | `<label for>` у всех полей, `<output>` у значений, `aria-labelledby` у секций, `role=group`/`aria-pressed` у оружия | FUNCTIONAL | HIGH | — | code_verified | tactical.js:895-897, 963-965, 971, 991-992, 1012-1014, 1023, 1029-1034, 947-951, 1069-1073 | Разметка панели семантична; кнопки с текстом; зум-кнопки с `aria-label` (html 65-66, обновляются 367-368). | — | — | — |
| 64 | Live-регионы: `tacCalHelp` (`aria-live`), `#tacStatus` (`role=status`), toast (`role=status`) | PARTIAL | HIGH | P3 | code_verified | tactical.js:892 (id только у `tacCalHelp`), 966, 1015, 1134; tactical.html:72; tactical.js:1320 | Ошибки «вне планеты» / «слишком много чисел» после Enter в «Найти»/«Позиция» (`findMessage`, `mortarMessage`) и ошибка сохранения калибровки (`calMessage` kind `error` — попадает в `tacCalHelp`, ok) рендерятся как `<p class="tac-msg">` без `aria-live`. | Подсказка калибровки и статус загрузки озвучиваются. | Сообщения «Найти»/«Позиция» — нет. | Передавать `id` в `msg()` для `tacFindMsg`/`tacMortarMsg` (там уже `aria-live="polite"`). |
| 65 | Статические `aria-label`/`title` только на английском | PARTIAL | HIGH | P3 | code_verified | tactical.html:56 («Map»), 70 («Planet map»), 75 («Tactical panel»); tactical.js:362-370 (`applyStaticText` их не трогает) | В RU-режиме имена регионов остаются английскими; шапка (`Sections`, `Back to ChemDB`) — под словарём `i18n.js` как у library.html (не проверял покрытие словаря — вне scope). | Заголовок и подписи тулбара переводятся. | Три aria-строки. | Добавить `mapLabel/panelLabel/canvasLabel` в L10N и выставлять в `applyStaticText`. |
| 66 | Клавиатура на canvas: `tabindex=0`, стрелки/±/0/Enter/Space/Escape, кольцо фокуса | FUNCTIONAL | HIGH | — | code_verified | tactical.js:1425-1441; tactical.css:87, 234, 317 | Все действия мыши имеют клавиатурный путь; перекрестие рисуется только при `:focus-visible` (816-827). Runtime — UX-агент. | — | — | — |
| 67 | Hover-only информация дублируется в панели после пика | FUNCTIONAL | HIGH | — | code_verified | tactical.js:849-876 (hover без aria, `pointer-events:none`), 981-1018 (та же информация в «Цель») | Enter под перекрестием даёт ту же карточку. | — | — | — |
| 68 | Состояния чипов не только цветом | FUNCTIONAL | HIGH | — | code_verified | tactical.css:124-126 | `.no` — `line-through`. | — | — | — |

## 9. i18n

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 69 | Полнота `L10N.en`/`L10N.ru` | FUNCTIONAL | HIGH | — | code_verified (скрипт) | tactical.js:32-269 | Скрипт (scratchpad `l10n_diff.js`): 118/118 ключей, расхождений нет, наборы плейсхолдеров `{…}` совпадают по каждому ключу; все `T.*` в коде определены; неиспользуемых верхних ключей нет. | — | — | — |
| 70 | Термины форка независимо от языка страницы | FUNCTIONAL | HIGH | — | code_verified | tactical.js:529-531, 623-628, 983, 991-997; index.json `terms` (24 ключа у обоих форков) | ДОЛГОТА/ШИРОТА, «Target Coordinates»/«Target X», тексты отказов — из локали форка (decision 12). | — | — | — |
| 71 | Fallback `'Dial'`, `'LONGITUDE'`, `'LATITUDE'` без перевода; `check_tactical.py` не проверяет ключи `terms` | PARTIAL | MEDIUM | P3 | code_verified | tactical.js:530-531, 997; scripts/check_tactical.py:181-182 (только непустой dict) | При отсутствии ключа у будущего форка (CMU/RuCM, T8) страница молча покажет английские заглушки. | Оба текущих форка полны. | Нет контрактной проверки. | В `check_tactical.py` — список обязательных ключей `terms`. |
| 72 | `T.shellKinds.other = '{name}'` — мёртвая строка | PARTIAL | HIGH | P3 | code_verified | tactical.js:142, 260, 568-572 | `shellLabel` возвращает `s.name` для `other` напрямую; внешний `fmt(..., {name})` в 1031 из-за этого лишний (см. #28). | — | Мёртвый ключ. | Удалить ключ и `fmt` в 1031, либо использовать ключ в `shellLabel`. |
| 73 | Переключатель языка перезагружает страницу (`LANG` читается один раз) | FUNCTIONAL | HIGH | — | code_verified | i18n.js:25-26, 49-66; tactical.js:16 | `location.replace(?lang=…)` — полная навигация, `window.I18N_LANG` и `document.documentElement.lang` выставляются до `defer`-скриптов. | — | — | — |
| 74 | Русская плюрализация «{d} тайлов», «{m} мин назад» | PARTIAL | HIGH | P3 | code_verified | tactical.js:236, 266, 189 | «1 тайлов», «54.6 тайлов» — косметика; в hover дробное число, в панели тоже. | — | Грамматика. | «тайл.» как сокращение или `Intl.PluralRules`. |

## 10. Покрытие тестами

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 75 | `scripts/test_tactical_logic.js` — 36 кейсов, CI | FUNCTIONAL | HIGH | — | test_verified | test:1-432; deploy.yml:42 | Все зелёные локально; числа макета и константы из кода игры. | — | — | — |
| 76 | Пробелы покрытия чистой логики | PARTIAL | HIGH | P3 | code_verified | logic.js:88-113 (rows ≠ height не валидируется), 201-211 (#9), 65-84 (#22, «-0», метка на одной оси), 403 (`session` null), 338-354 (неизвестный `proto`), 449-474 (`mode: 'laser'`, `at` не число — #30), 427-443 (`weapon: 'ob'` из prefs при `disabled` кнопках → панель без секций миномёта, tactical.js:1071-1073), 310-325 (`outside/total` не проверяются) | Перечисленные ветки не имеют теста; `decodeRows` полагается на `check_tactical.py:118-119` для высоты — в рантайме лишняя/недостающая строка даёт `undefined` без исключения (areaAt → null), но `buildTints` (tactical.js:464) при индексе зоны вне таблицы бросит `TypeError` — тоже только CI-гарантия. | Основные формулы покрыты. | Крайние ветки. | Добавить кейсы: `decodeRows` высота; `minRange: 25` для #9; «243 - 226»; `mode: 'laser'` и `at: NaN`; `weapon: 'ob'` → UI-ветка; `roofedFlags` неизвестный proto; `obChecks.outside`. |
| 77 | UI-слой `tactical/tactical.js` (1500 строк) без автотестов | UNCERTAIN | HIGH | P3 | code_verified | tactical.js целиком | Ожидаемо для DOM-кода; `data-no-autopopup` тоже проверяется только чтением. Рантайм — у UX-агента (превью). | Логика вынесена в `logic.js`. | Ветки #24, #42, #46-48 не подтверждены запуском. | Рассмотреть smoke-тест через `puppeteer-core` (уже есть в `shoot_cards.mjs`) на сценарий макета. |

## 11. Документация/контракт (в пределах scope)

| # | Feature | Classification | Confidence | Priority | Evidence Type | File:Line | Description | What Works | What Fails | Recommendation |
|---|---|---|---|---|---|---|---|---|---|---|
| 78 | Спека §Хранилище vs реализация | PARTIAL | HIGH | P3 | code_verified | docs/design/2026-09-13-tactical-map.md §Страница «Хранилище» (`calibration: {offset, at, lastUsed, points}`, `mortar: {tile, level, mode}`) vs logic.js:449-474 (`{tile, reading, offset, at, check}`, `{tile, mode}`) | Дрейф спеки относительно кода (формат `check`, отсутствие `level`/`lastUsed`/`points`). | Код и `shoot_cards.mjs` согласованы между собой. | Спека устарела. | Обновить абзац спеки при T5 (там же появятся `shots`). |
| 79 | Спека «`sw.js` не меняется» vs cf388d2 | FUNCTIONAL | HIGH | — | code_verified | docs/design §Интеграция; sw.js:6, 23, 28 | Бамп касается `home.js`/`sections.json`/`CACHE` (Серия H, карточка), не PRECACHE tactical — соответствует decision 4. | — | — | — |

---

## Покрытие

Прочитаны полностью: `tactical.html` (87), `tactical/logic.js` (519), `tactical/mapview.js` (232), `tactical/tactical.js` (1500), `tactical/tactical.css` (351), `scripts/test_tactical_logic.js` (432), `scripts/shoot_cards.mjs` (110), `scripts/build_cards.py` (38), `sw.js` (92), `docs/decisions/2026-09-13_tactical-map.md` (135).
Прочитаны по scope: `home.js` (157-158, 212, 360-426 + diff 3b54afc), `feedback.js` (415-481 + diff 3b54afc), `sections.json` (50-70), `index.html`/`library.html` (строки `?v=`), `.github/workflows/deploy.yml` (30-80), `scripts/check_tactical.py` (структура + 170-200), `i18n.js` (15-80), `docs/design/2026-09-13-tactical-map.md` (§Механика игры, §Сценарий в бою, §Формат данных, §Страница, §Крайние случаи, §Дополнения war-room, §Верификация, §Серия T), `ROADMAP.md` §Серия T (707-751), `tactical/index.json` (константы, термины, снаряды обоих форков), 20 планетных JSON (заголовки, bounds, имена зон с `&`/`'`).
Апстрим (кеш): `MortarSystem.cs` 60-215, `SharedMortarSystem.cs` 155-195, 385-432, `MortarComponent.cs` 24-57, 93, `RangefinderSystem.cs` 92-137, 180-196, `AreaSystem.cs` 247-280, 339-366, `RMCPlanetSystem.cs` 50-66, 125-152.
Выполнено: три node-теста, `node --check` ×6, `git show --stat` четырёх коммитов, `git diff HEAD --stat` по scope, `git log -S` по версиям, скрипт сверки L10N.
Не делалось: браузер/превью, правки tracked-файлов, коммиты, экспорт Метрики.
